# 커넥션센스 — 작업 규약

이 저장소에서 작업하는 모든 사람(과 에이전트)이 지켜야 할 규칙. 세션이
바뀌어도 이 파일은 자동으로 읽히므로, **구두로 합의한 것은 반드시 여기에
적어야 다음 사람이 안다.**

프로젝트 현재 상태와 할 일은 [`docs/planning/HANDOFF.md`](docs/planning/HANDOFF.md),
결정의 배경과 시간순 기록은 [`docs/planning/backlog.md`](docs/planning/backlog.md)에 있다.

---

## 1. 소통

- **사용자와의 모든 대화는 한글로 한다.** 질문 선택지도 한글로 쓴다.
- **영문 용어·도구 이름이 나오면 그것이 어떤 작업이고 왜 필요한지 한글로
  함께 설명한다.** 예: "CI" → "커밋할 때마다 자동으로 테스트를 돌려 회귀를
  막는 장치".
- 파일 경로, 코드 식별자, 패키지명은 원문 그대로 둔다(번역하면 검색이 안 된다).

## 2. Git — 브랜치와 커밋

**`main`에 직접 커밋하지 않는다.** 문서 한 줄 수정이라도 브랜치를 파서 PR로
올린다. 자동 검사(아래 3번)를 건너뛰는 경로를 만들지 않기 위함이다.

```
git checkout -b feat/<짧은-영문-설명>
# 작업 → 커밋 → push
gh pr create --base main
```

**커밋 메시지는 한글로 쓰고, "무엇을 했는지"보다 "왜 그렇게 했는지"를 담는다.**
이 저장소의 커밋 메시지는 나중에 원인을 추적하는 1차 자료로 쓰인다.

- 제목 한 줄 + 빈 줄 + 본문
- 본문에 **문제 상황 → 원인 → 선택한 해결책 → 대안을 버린 이유** 순으로
- 버그 수정이라면 **증상과 원인을 구분해서** 적는다. 예: 주소 검색 버그에서
  화면에 뜬 `ERR_UNKNOWN_URL_SCHEME`은 증상이었고 원인은 `file://` origin
  이었다 — 이 구분이 커밋 메시지에 없으면 다음 사람이 같은 함정에 빠진다.
- 마지막 줄: `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

**커밋은 성격별로 나눈다.** 코드 변경과 문서 갱신, 서로 다른 기능은 각각
따로. 되돌릴 때 한 덩어리로 묶여 있으면 곤란해진다.

**사용자가 요청하기 전에 커밋하지 않는다.**

## 3. 커밋 전 확인

⚠️ **처음 받은 기계라면 `cmake`부터 설치한다** — `brew install cmake`.

`dartcv4`(명함 테두리 검출, 추가 273)가 **`flutter test`에서도 cmake를 요구한다.**
없으면 **테스트가 통째로 실패**하고, 그 원인이 코드가 아니라는 것을 알기까지
시간이 걸린다. ✅ CI(`ubuntu-latest`)에는 기본으로 있다(2026-08-17 확인).

🚨 **「처음 받은 기계」는 사람 노트북만이 아니다 — Xcode Cloud 머신이 매번
그것이다**(2026-09-04에 하루를 여기에 썼다). Xcode Cloud 의 `Archive - iOS`
가 계속 실패했는데, 로그를 받아 보니 원인이 **바로 이 줄**이었다:

```
Exception: Failed to find cmake version: latest
  Building assets for package:dartcv4 failed.
Command PhaseScriptExecution failed with a nonzero exit code
```

📌 **위 문장이 이미 답이었는데 아무도 그 연결을 안 했다.** *"처음 받은
기계"* 가 **사람이 새로 산 노트북**으로만 읽혔기 때문이다. **빌드 머신은
빌드마다 새것이다.**

⚠️ **그리고 로컬에서는 절대 재현되지 않는 종류다.** 개발 노트북에는 cmake 가
이미 있어서, 그날 **같은 `xcodebuild archive` 명령 · 같은 방식으로 설치한
Flutter SDK · 같은 Xcode 버전**으로 돌린 재현이 **전부 성공했다.** 후보를
다섯 개 지우고서야 로그를 받았다.

⭐ **그래서 규칙은 「로컬에서 재현 안 되면 로그부터 받는다」다.** 재현은
「환경이 같을 때만」 답을 준다 — 환경이 다른 것이 원인이면 재현은 **영원히
조용하다.** 지금은 `ios/ci_scripts/ci_post_clone.sh` 가 cmake 를 깔고, 진단
줄에 그 유무를 찍는다.

**로컬에서 먼저 돌린다. 자동 검사는 안전망이지 1차 검사가 아니다.**

```
flutter analyze   # info 11건은 기존 잔재라 통과, error·warning은 반드시 0
flutter test      # 현재 1,944건(+4 건너뜀) — **늘어나는 것은 정상, 줄면 회귀다**
```

`analyze`는 CI에서 `--no-fatal-infos`로 돈다. 남아 있는 info를 늘리지 말 것.

⚠️ **테스트 수는 2026-09-04에 다시 쟀다** — `main`(`2735c41`)의 CI 로그 마지막 줄
`+1944 ~4: All tests passed!` 이다. 1,916건이 **1,944건**이 돼 있었다(+28).
info 11건은 그대로였다(2026-09-02의 `6324fbe3` 이후 두 번째 확인).

📌 **무엇으로 쟀는지 함께 적는다**(추가 664) — **CI 로그**다. 그래서 이 숫자는
**`ubuntu-latest` · Flutter 3.44.8** 기준이고, **맥에서 돌린 값이 아니다.**
⚠️ **CI 가 못 보는 것**: 로컬 macOS 에서만 나는 실패(`cmake` 없음, 워크트리 서명),
그리고 **다른 Flutter 버전에서 달라지는 것.** 로컬 숫자가 이와 다르면 **먼저
`flutter --version` 을 대 보라** — CI 는 3.44.8 에 고정돼 있다.

⚠️ **아래 문단의 `15e9fd57` 은 2026-08-30에 잰 그때의 기록이다.** 그
전에는 **`19건`·`535건`** 이라고 적혀 있었는데 둘 다 낡아 있었다 — 실제로는
**11건·1,821건**이었다. 📌 **낡은 기준선은 조용히 사람을 속인다**: info가 11건
나오면 *"8건이나 줄었네"* 로, 테스트가 1,796건 나오면 *"세 배 늘었네"* 로
읽힌다. **둘 다 「그대로였다」가 정답이다.**

🚨 **그러니 이 숫자가 안 맞으면 코드를 의심하기 전에 이 줄을 의심하라.** 그리고
**맞춰 놓았으면 그 자리에서 고쳐라** — 다음 사람이 또 속는다.

📌 **고치는 도중에 두 번 더 낡았다.** 처음 잰 값은 **1,796건**이었는데, PR을
올려 둔 사이에 다른 세션의 인식 수정이 병합되며 **1,813건**(#703, +17)이 됐고
다시 **1,821건**(#706, +8)이 됐다. 🚨 **「낡은 기준선을 고치는 문서」가 병합 전에 낡은 것이다.** 그래서 이
줄에는 **잰 커밋(`15e9fd57`)을 함께 적는다** — 숫자만 있으면 언제 잰 것인지
알 수 없고, 그때부터 다시 속는다.

⚠️ **테스트 수는 낡는 것이 정상이다** — PR마다 늘어난다. **정확히 맞을 필요는
없고, 「줄었는지」만 보면 된다.** 그래서 이 숫자를 맞추려고 매번 고칠 것은
없다. 다만 **크게 벌어졌으면 그때 다시 재서 커밋과 함께 고쳐 둔다.**

### 자동 검사(GitHub Actions)가 도는 조건 — 2026-08-16에 좁혔다

| 언제 | 도나 |
|---|---|
| **`main`으로 가는 PR** (코드 포함) | ✅ |
| `main`에 직접 push (코드 포함) | ✅ |
| **문서만 바뀐 PR**(`**.md`·`docs/**`) | ❌ 건너뛴다 |
| **브랜치에 push** (PR 열기 전) | ❌ 안 돈다 |

⚠️ **그러니 PR을 열기 전에는 자동 검사 결과가 없다.** 로컬에서 돌린 결과를
**PR 본문에 숫자로 적는다**(`analyze 19 · test 513` 형식). 안 적으면 나중에
*"이건 검증됐나?"*에 답할 수 없다.

**왜 좁혔나**: 여러 세션이 붙어 일하면서 **3일 만에 무료분이 바닥나 CI가 통째로
멈췄다**(추가 257). 문서만 고친 PR에도 테스트 513건이 다 돌았고, `backlog.md`
충돌을 풀어 다시 푸시할 때마다 처음부터 다시 돌았다(하루 여섯 번).

⚠️ **잡이 결제로 멈추면 빨간 X가 뜨는데, 그건 "실패"가 아니라 "돌지 않음"이다.**
`gh run view <id> --json jobs`로 **단계 수가 0이면** 결제 문제다 — 코드에서
원인을 찾지 마라. 확인은 <https://github.com/settings/billing>.

## 4. 이 프로젝트에서 특히 조심할 것

### 가짜 데이터를 만들지 않는다

사용자가 명시적으로 요구한 원칙이다. 화면을 채우려고 예시 인물·가짜 알림·
스톡 사진을 넣지 않는다. 데이터가 없으면 **비어 있는 상태를 그대로 보여준다.**
과거에 프로필 사진 선택이 실제로는 스톡 사진 4장을 순환시키는 가짜 구현이었던
전례가 있다(backlog 추가 49).

### 코드 리뷰로는 안 잡히는 결함이 반복해서 나왔다

이 프로젝트에서 실제로 터진 결함들은 **코드는 맞는데 실물이 틀린** 유형이었다.

| 결함 | 코드 상태 | 어떻게 잡혔나 |
|---|---|---|
| 서버에 명함 개인정보 평문 3건 | 정상 | Firestore 실물 조회 |
| 앱 삭제 후 로그인 정보 잔존(iOS) | 정상 | 사용자 실사용 |
| 재시도 로직이 죽어 있음 | 서비스는 정상, 부르는 쪽이 없음 | 기기 저장소 덤프 |
| 주소 선택이 안 됨 | 정상 | 사용자 실사용 |
| 같은 주소 묶음이 **안 닫혀** 낱개까지 묶음으로 읽힘(F-15) | 정상 — 숫자도 맞았다 | **같은 주소 명함을 실제로 넣어 본 사용자** |
| 재연락 문구가 *"오늘 전 연락"*으로 깨짐(F-10) | 규칙 테스트 전부 통과 | 실기기 화면 |
| 광고 동의 화면에서 **뒤로가기**를 누르면 동의한 것처럼 넘어감(2026-08-30) | **정상 — 저장은 안 된다**(코드·실기기 확인) | **사용자가 눌러 봄**(자동 검사 1,873건 전부 통과한 뒤) |

⚠️ **표의 마지막 셋(F-15·F-10·광고 동의)은 앞의 넷과 결이 다르다.** 앞의 넷은 *"코드는
맞는데 실물이 틀린"* 것이고, 이 둘은 **로직은 맞는데 화면이 틀린** 것이다.
F-15는 머리글 숫자까지 정확했고, F-10은 규칙 테스트가 촘촘히 덮고 있었다.

> **자동 테스트는 규칙을 보고 사람은 화면을 본다. 둘은 다른 것을 본다.**

📌 **광고 동의 건(2026-08-30)은 그중에서도 「나가는 길이 둘인데 하나만 화면에
있던」 경우다.**

```
화면에 있던 길   아무것도 안 고르고 「시작하기」  →  「거부」로 답이 기록된다
화면에 없던 길   뒤로가기                      →  답 없이 넘어간다 · 안내 없음
```

**저장은 실제로 안 됐다** — 로직은 맞았고 **화면이 그 사실을 말해 주지 않은 것**이
틀린 부분이었다. ⚠️ 그래서 고칠 때도 **막지 않고 말하는 쪽**으로 갔다: 뒤로가기를
막으면 광고 동의가 **필수처럼 보여 「자유로운 동의」가 아니게 되고**, 「거부」로
저장하면 **답한 적 없는 사람을 답한 것으로 적게** 된다.

🚨 **같은 날 하나 더 나왔다 — 「자동으로 확인했다」가 확인이 아니었던 자리다.**
카카오 리다이렉트 URI 가 등록됐는지 커맨드라인으로 물었더니 **후보 셋이 다 로그인
화면으로 갔다**(카카오는 `redirect_uri` 검사를 로그인 뒤로 미룬다). **실기기에서
눌러야 `KOE006` 이 나온다.** 자동으로 물어서 통과한 것을 통과로 읽으면 안 된다.

📌 **F-15는 표본이 없으면 안 나온다.** 같은 주소 명함이 **둘 이상 실제로 있어야**
묶음이 그려지고, 그래야 안 닫힌 것이 보인다. 기기에 표본이 없어 못 본 것을
**"괜찮다"로 읽을 뻔했다.**

따라서 **"테스트 통과 = 동작함"이 아니다.** 저장·복원·마이그레이션·재설치에
관련된 변경은 반드시 실기기와 서버 실물로 확인한다. 절차는 `docs/planning/sessions/2026-08.md` "0-2"에
있다(Firebase CLI 토큰으로 Firestore 관리자 조회, Android `adb run-as`).

#### 결이 하나 더 있다 — **판단이 틀렸고 실물이 잡았다** (2026-08-16)

위 표는 전부 *코드는 맞는데 실물이 틀린* 경우다. 그런데 하루에 **정반대 유형이
셋** 나왔다 — **코드는 아직 없고, 사람이 낸 판단이 틀렸던** 것이다.

| 틀린 판단 | 얼마나 그럴듯했나 | 무엇이 잡았나 |
|---|---|---|
| 커버 화면 크롭은 1,353px이라 축소를 건너뛴다 | 자릿수·형식이 맞았다(실제 1,805px) | 사용자가 명함을 **직접 등록** |
| 캐시의 UUID 폴더는 남의 것이다 | *"각 40KB PNG 하나뿐"*이라는 **수치까지 있었다** | `image_picker` **플러그인 소스** |
| 회전 기능은 파일을 하나만 만든다 | 코드 리뷰를 통과했다 | 기기 캐시 **실측** |

**그래서 "계산했다"를 "확인했다"라고 부르지 않는다.**

> **계산은 틀려도 그럴듯해 보인다. 실측은 틀리면 티가 나는데 계산은 안 난다.**

셋 다 **재는 쪽이 잡았고, 코드 리뷰로는 하나도 안 나왔다.** 특히 셋째는
**사람이 실제로 쓰는 경로**여야 나왔다 — 자동 조작이 매끄러웠다면 회전 버튼을
누를 일이 없었고, 그러면 평문 1MB가 캐시에 남는 것도 안 드러났다.

**적용**: 어떤 수치를 근거로 결정을 내릴 때는 **그 수치가 잰 것인지 센 것인지
문서에 적는다.** 계산이면 "계산", 실측이면 "실측"이라고 쓴다. 그리고 **틀린
계산이 드러나면 결론만 고치지 말고 경위를 남긴다** — 결론만 고치면 다음 사람이
같은 자리에서 같은 실수를 한다.

#### 그런데 「센 것」도 틀린다 — **세는 도구가 못 보는 것** (2026-09-02)

위는 *"계산인지 실측인지 적어라"* 였다. 그런데 **센 것도 틀린다.** 하루에 세
세션이 각자 물렸고, **여덟 건이 전부 같은 뿌리였다.**

> **세는 도구가 무엇을 못 보는지를 안 물었다.**

| 무엇을 세었나 | 도구가 못 본 것 | 결과 |
|---|---|---|
| `Semantics(` 를 grep | `ExcludeSemantics` — **반대말** | *"삭제 못 한다"* 오보 |
| `minimumSize` 를 grep | `_InputPadding` — **감싸는 것** | *"48dp 미만 15건"* 이 전부 허수 |
| `gh pr list --limit 60` | **기본 한도 30 · 실제 PR 805개** | 병합된 브랜치를 33개로 셈(실제 367개) |
| 회사 재등장률을 사진 419장으로 | **중복 사진 238장** | 91% 가 실제로는 55% |
| 정답지 중복을 **묶음 단위**로 | 한 묶음 안의 **섞임** | 15행이 실제로는 18행 |
| 인수 기준을 요약으로 셈 | 스펙 §8 의 나머지 일곱 | **13개가 6개로** 줄어 회귀 둘을 놓칠 뻔 |
| 방침 문구를 계획서에서 인용 | 원문의 **다른 문서** | 방침이 아니라 `app-permissions.html:102` 였다 |
| *"660·661·662가 나갔다"* — **전한 쪽이 뭉개고 받은 쪽이 안 물음** | 「배정」인지 「병합」인지 | *"664가 다음 빈 번호"* 로 보고 → 정정 |
| PR 상태를 조회해 *"열려 있다"* 로 씀 | **조회한 순간 이후의 변화** | 그 사이 병합돼 있었다 |

⚠️ **결이 셋이다.** 위 다섯은 **센 것**, 다음 둘은 **받아 적은 것**, 마지막은
**본 순간의 상태**다. 마지막은 **여럿이 동시에 일할 때만 생긴다** — 조회한
값은 **조회한 순간의 것**이고, 그 사이에 남이 바꾼다.

📌 **처방은 하나가 아니라 둘이다. 어느 쪽인지 먼저 가른다.**

```
① 원본으로 돌아간다        요약이 원본을 덮은 경우
                          → 다시 「보면」 드러난다

② 세는 법을 바꾼다         다시 세도 같은 답이 나오는 경우
                          → 도구가 못 보는 것을 먼저 묻는다
```

🚨 **②가 더 나쁘다 — 몇 번을 세도 같은 오답이 나오기 때문이다.** 정답지 중복은
**세 번을 세도 15행**이었고, **실제로 정답지에 표시를 달다가** 18행이 됐다.

> **세어만 봤을 때는 안 나왔고, 손을 대다가 나왔다.**

⭐ **이 규약을 쓴 날, 쓴 사람이 ②에 또 물렸다** — `gh pr list --limit 60` 으로
브랜치를 세다 **339개를 「미완일 수 있다」로 보고 손을 못 댈 뻔했다.** 기본
한도가 30 이고 실제 PR 은 805개였다(추가 667). 📌 **규약을 적는 것과 그 규약을
지키는 것은 다른 일이다.**

⚠️ **`grep` 은 「있는 것」만 찾는다** — 반대말·감싸는 것·기본값은 안 걸린다.
**「없다」를 grep 으로 말하지 마라.**

⚠️ **요약은 세션 사이를 건너며 원본 행세를 한다.** 인수 기준이 13에서 6이 된 날
**PM 도 담당 세션도 「여섯」으로 옮겨 적었고 아무도 스펙 §8 을 다시 안 열었다.**
🚨 **같은 문서 안에서도 일어난다** — `HANDOFF` 87행은 「열셋」으로 정정됐는데
**121행 할 일 블록은 「여섯」 그대로**였고, **내일 아침 세션이 읽는 것은
121행**이었다(추가 668).

⚠️ **옮겨 적을 때는 「출처」까지 함께 옮긴다.** 같은 날, **기록을 읽고 그것을
「전해 준 세션이 한 일」로 적은 일**이 났다 — 읽은 것은 맞는데 **누가 한 것인지가
바뀌었다.** 📌 **한 번 잘못 붙은 출처는 그다음부터 아무도 안 의심한다.**
그리고 **넘기는 쪽도 뭉개지 말아야 한다** — *"660·661·662가 나갔다"* 는
**「배정」인지 「병합」인지**를 안 갈라 상대가 「병합」으로 읽었다.

⭐ **그리고 이 여덟은 「숫자가 틀린 것」과 「할 일이 사라진 것」으로 갈린다.**
숫자는 다시 재면 되지만, **사라진 할 일은 아무도 안 찾으면 영영 안 한다.**
인수 기준 건이 그것이었고, **빠진 일곱 중 둘이 전날 고친 자리와 겹쳤다.**

**적용**: 세어서 말한 숫자에는 **무엇으로 셌는지**를 함께 적는다(`grep`,
요약본, 표본 N장, `--limit`). 그리고 **그 도구가 무엇을 못 보는지**를 한 번
묻는다 — 안 물으면 **같은 도구로 몇 번을 세도 같은 오답**이 나온다.

⚠️ **「재보지도 않고 적은 것」에는 규칙을 늘리지 않는다.** *"리멤버는 몇 시간
걸린다"*(실측 69초) 같은 것은 **바로 위 「계산인지 실측인지 적는다」가 이미
덮는다.** 그 문장이 안 지켜진 사례로 읽으면 된다 — **규약은 늘릴수록 안 읽힌다.**

### 그렇다고 수정할 때마다 전체 테스트를 돌리지 않는다

위 원칙을 과잉 적용해서 **수정 하나마다 전체 테스트(실기기 18항목, 1시간)를
처음부터 다시 돌린 적이 있다**(2026-08-12). 텍스트 정렬 하나 고치는 데 릴리스
빌드·설치·로그인·계정삭제·저장소 덤프를 전부 다시 했고, 그동안 기능 개발이
멈춰 대기만 쌓였다. 검증이 개발을 돕지 않고 개발을 대기열에 세웠다.

#### 검증을 부르는 이름을 셋으로 구분한다 (2026-08-12 사용자 확정)

말이 뒤섞이면 같은 사고가 반복된다. **아래 이름 그대로 쓴다.**

| 이름 | 무엇을 하나 | 언제 | 대략 |
|---|---|---|---|
| **전체 테스트** | 말 그대로 **전 항목** 실기기 검증 | **배포 직전 한 번** | ~1시간 |
| **부분 테스트** | **수정한 곳 + 그 수정이 영향을 주는 곳**까지 | 수정할 때마다 | 수 분 |
| **자동 테스트** | `flutter test`(코드 레벨), `flutter analyze` | 커밋 전 항상 | 수 초 |

⚠️ **"단위 테스트"라는 말은 쓰지 않는다.** 이 프로젝트에서 그 말은 원래
`flutter test`를 가리켰는데, "수정한 곳만 확인"이라는 뜻으로도 쓰이기
시작해 **같은 말이 두 가지를 가리키게 됐다.** 그래서 위처럼 **부분 테스트**
(사람이 화면·동작을 보는 것)와 **자동 테스트**(코드가 도는 것)로 갈랐다.

**부분 테스트의 범위 = 고친 것 + 영향받는 것.** 예: "안내 문구를 왼쪽
정렬로" 수정 → 고친 화면 2곳을 열어 정렬 확인 + 같은 스타일을 쓰는 다른
화면이 깨지지 않았는지까지. **계정 삭제·명함 스캔 같은 무관한 항목은 보지
않는다.**

**무엇을 고쳤을 때 어디까지 보나:**

| 무엇을 고쳤나 | 필요한 검증 |
|---|---|
| 텍스트·색·아이콘·정렬 등 겉모습 | **부분 테스트** — 그 화면 + 같은 스타일 쓰는 화면 |
| 로직 분기·계산·파싱 | **자동 테스트**(해당 파일) + 그 로직이 보이는 화면 **부분 테스트** |
| 저장·복원·마이그레이션·탈퇴·결제 | **전체 테스트**(실기기) |

**전체 테스트는 수정할 때마다가 아니라 배포 직전에 한 번 몰아서 한다.**
그때까지의 수정들은 각자 부분 테스트만 통과시키고 쌓아 둔다.

#### 테스트를 시작하기 전에 반드시 이 표를 사용자에게 보여주고 고르게 한다

사용자 요청(2026-08-12): **"내가 잊을 수 있으니까 항상 테스트 진행 시 이 표를
리마인드해서 내가 선택하게 해줘."**

그러므로 검증을 시작하기 전에 **묻지 않고 바로 돌리지 않는다.** 아래 형식으로
셋을 나란히 제시하고, 어느 것으로 갈지 사용자가 고르게 한다(권장안은 표시하되
강요하지 않는다).

```
이번 수정: <무엇을 고쳤는지 한 줄>

  전체 테스트 — 전 항목 실기기 (~1시간)      [배포 직전용]
  부분 테스트 — <이번에 볼 화면·영향 범위>   (~N분)  ← 권장
  자동 테스트 — flutter test/analyze (~수 초)

어느 것으로 할까요?
```

**예외**: 사용자가 이미 "전체 테스트 돌려"처럼 명시했거나, 배포 직전 단계임이
대화에서 확정된 경우에는 다시 묻지 않는다.

**실기기 QA를 돌리는 동안에는 그 브랜치에 커밋하지 않는다** — QA가 검증하던
커밋이 발밑에서 바뀐다. 병행이 필요하면 QA를 `git worktree`로 특정 커밋에
고정해서 돌린다(2026-08-12에 실제로 겪음).

### 개인정보를 다루는 앱이다

이 앱은 **이용자 본인이 아닌 제3자(명함 주인)의 개인정보**를 저장한다.

- 로그·디버그 출력에 이름·전화번호·이메일·주소를 남기지 않는다. 필요하면
  "값이 있는지 없는지"만 찍는다.
- 암호화되지 않는 저장소(`shared_preferences` 일반 키)에 개인정보 원문을
  넣지 않는다. 해시나 플래그만 쓴다.
- 새 데이터를 서버로 보내거나 외부 API로 전송하는 코드를 추가하면
  **`docs/legal/privacy-policy.html`도 함께 고쳐야 한다.** 방침과 구현이
  어긋나는 것 자체가 법적 리스크다(BYOK 서술 불일치로 이미 한 번 겪었다).
- 과장하지 않는다. 암호화 키가 서버에 함께 있으므로 "완전한 종단간 암호화"나
  "제로-지식"이라고 쓰면 안 된다.

### 문서를 코드와 함께 갱신한다

작업이 끝나면 `docs/planning/backlog.md`에 "추가 N" 형식으로 기록하고,
현재 상태가 바뀌었으면 `docs/planning/HANDOFF.md`도 고친다. 이 두 문서가
실제 상태보다 뒤처지면 다음 세션이 잘못된 전제로 작업한다 — 실제로
"Apple 로그인 미구현"이라는 낡은 기록 때문에 이미 구현된 기능을 다시
검토한 적이 있다.

### 화면설계는 셋으로 나뉜다 — 어디에 두는지 (2026-08-27 사용자 확정)

```
① 화면설계 모음        단위 화면을 그려 쌓는 곳 — 작업장
       ↓  사용자가 보고 확정
② 화면설계서 · 계획     확정됐으나 아직 안 만든 것
       ↓  🚨 실기기에서 확인했을 때
③ 화면설계서 · 구현     실제로 동작이 확인된 것 — **지금 이렇게 생겼다**
       ↓  🚨 스토어 출시 이후
④ 최종본               바뀔 일이 다 끝난 뒤에 만든다
```

⚠️ **③은 「완성」이 아니다** — 구현된 뒤에도 바뀐다(내보내기 화면은 구현 후
**세 번 더** 바뀌었다). 📌 **디자이너에게 넘길 때 쓰는 것도 ④가 아니라 ③이다**
— 넘기면 거기서 또 바뀌므로 **최종본은 스토어 출시 이후**에 만든다.

🚨 **②→③ 은 「코드를 짰을 때」가 아니라 「실기기에서 확인했을 때」다.**
2026-08-26 광고 동의에서 **자동 테스트를 전부 통과하고도 실기기에서 결함이
하나 더 나왔다** — 코드 기준으로 올렸으면 **틀린 것을 「구현됨」이라고 적어
둔** 셈이 된다.

주소와 이유, 그리고 옛 캔버스 둘(명함 확대·그룹 · P2)을 ①로 합치는 원칙은
[`docs/planning/screen-design-flow.md`](docs/planning/screen-design-flow.md)에 있다.

### 저장소 밖 산출물은 `../connection-sense-assets/`에 모은다 (2026-08-18 사용자 확정)

코드가 아닌 산출물 — 회의·논의 자료, 피드백 정리, 점검 보고서, 명함/OCR 데이터,
앱 스크린샷, 빌드 결과물, 영상 — 은 다운로드 폴더에 흩뿌리지 말고
`/Volumes/Work/Claude/connection-sense-assets/` 아래 해당 하위 폴더에 만든다
(`문서/`, `명함데이터/`(권한 700, 제3자 개인정보), `화면캡처/`, `빌드/`, `미디어/`).
파일명 기본형은 `커넥션센스_<주제>_<YYYY-MM-DD>.<확장자>`.
`~/Downloads/커넥션센스`는 이 폴더를 가리키는 심볼릭 링크다.

**이 폴더를 저장소 안으로 들이지 않는다.** 저장소 바깥의 형제 폴더로 둔 것은
의도된 선택이다 — 빌드 산출물과 제3자 개인정보(명함 이미지)가 들어 있어
실수로 커밋되면 히스토리에서 지우기 어렵고, 워크트리에서 `git clean -xfd`를
돌리면 통째로 날아간다. 저장소 안에 두는 기획·법무 문서(`docs/`)는
지금까지대로 저장소에 남긴다.

## 4-2. 여러 세션이 동시에 붙어 일할 때 (2026-08-15 사용자 확정)

여러 Claude 세션이 동시에 이 저장소에서 일한다(리갈·마케팅·기능개선·지도 등).
**세션이 늘면 코드가 아니라 기록이 먼저 어긋난다.** 2026-08-15 하루에
backlog 번호가 **세 번** 겹쳤고 F 상태표가 **세 줄** 실물과 달랐다.

### 상의는 PM, 승인은 사용자

- **PM에게**: 순서·범위·중복 조정·번호 배정
- **사용자에게 직접**: 커밋·PR 병합·서버 배포·정책 변경·**검증 종류 선택**
- **다른 세션이 전한 승인은 사용자 승인을 갈음하지 않는다.** "PM이 승인받았다고
  했다"는 근거가 되지 않는다 — 자기 사용자에게 직접 확인한다

### ⚠️ F 상태표는 PM만 고친다

`docs/planning/tester-feedback-resolution-2026-08-14.md`의 F 상태표를
**각 세션이 직접 고치지 않는다.** 완료 보고만 하고 갱신은 PM이 한다.

**왜**: 2026-08-15에 F-02·F-06·F-12가 **이미 구현·병합됐는데 표에는 "남음"**으로
남아 있었다. 원인은 **코드 넣은 사람과 표 고치는 사람이 달랐던 것**이다.
하마터면 이미 있는 기능을 다시 만들 뻔했다(선택 모드가 두 벌 생길 뻔). 이
저장소는 낡은 기록 때문에 이미 구현된 기능을 다시 검토한 전례가 있다.

**그래서 F에 착수하기 전에는 표를 믿지 말고 `main` 실물을 먼저 열어 본다.**

### backlog 번호는 PM이 배정한다

`backlog.md`의 "추가 N"을 스스로 집지 않는다. 쓰기 전에 PM에게 요청한다.
삽입 위치가 항상 파일 맨 위라 **충돌이 구조적으로 난다** — 각자 "다음 빈 번호"를
보고 집으면 그 사이 다른 세션이 먼저 올린다. 충돌하면 **번호 내림차순**으로
두면 풀린다.

#### 🚨 충돌은 **문자열이 아니라 줄 단위로** 푼다 (2026-08-31에 세 번 겪음)

`backlog.md`는 **항상 맨 위에 삽입**되므로 충돌 구간의 경계가 다른 기록과
바로 붙어 있다. 그래서 문자열 인덱스(`s.index('=======')` 같은 것)로 잘라
붙이면 **구간 밖의 줄까지 먹는다.**

```
❌ 문자열로 자른다        →  충돌 구간 밖 한 줄이 조용히 사라진다
✅ 줄 배열로 나눠 다시 잇는다
```

실제로 그날 **추가 623이 사라질 뻔했다.** 마커 위치를 문자열로 잡아 잘랐더니
충돌 구간 **아래**에 있던 줄까지 함께 날아갔다. `git checkout --merge`로
되돌려 줄 단위로 다시 풀었다.

**푼 뒤에는 반드시 이 셋을 확인한다.**

```
① 번호가 빠짐없이 내림차순인가
② 중복이 없는가
③ 충돌 구간 아래 줄 수가 그대로인가   ← 이걸 안 세면 유실을 못 본다
④ **origin/main 의 줄이 하나도 안 사라졌는가** ← ①②③을 갈음한다
```

🚨 **④는 「번호」가 아니라 「줄 전체」를 본다.** ①②는 번호만 보므로 **각주나
설명 줄이 사라지면 못 잡는다.** 재는 법은 다중집합(multiset) 차다.

```python
from collections import Counter
lost  = list((Counter(main_lines) - Counter(cur_lines)).elements())   # 사라진 줄
added = list((Counter(cur_lines) - Counter(main_lines)).elements())   # 더한 줄
```

⭐ **사라진 줄이 있으면 그 줄을 그대로 보여 준다** — 「몇 줄 어긋났다」보다
훨씬 빨리 고칠 수 있다.

🚨 **③은 잘못 재질 수 있다.** 번호 순서를 함께 바로잡으면 **충돌 구간의 경계
자체가 움직여** 비교 기준이 달라진다. 2026-09-01에 `10158 → 10136` 으로 나와
유실인 줄 알았는데 **재는 법이 틀린 것**이었다. **④를 돌렸으면 그 자리에서
「사라진 줄 0개」가 나왔다.**

📌 **그러니 ③이 어긋나면 먼저 ④를 돌려 본다.** ①②는 눈으로 훑는 용도로 남긴다.

🚨 **④가 없으면 앞의 셋은 「내 쪽 줄이 살았는지」만 본다.** 상대 쪽에서
한 줄이 조용히 빠져도 **셋 다 통과한다** — ③이 세는 것은 충돌 구간
**밖**이기 때문이다.

```
comm -23 <(main 의 번호) <(푼 뒤의 번호)   →  비면 정상, 나오면 그게 유실이다
```

⚠️ **①②를 잴 때 「어디까지」를 먼저 정한다 — 여기서 두 번 헛통과했다.**

```
너무 넓게   파일 전체로 재면 아래쪽 **전체 색인**의 번호가 섞여
            같은 번호가 두 번 나온다 → main 조차 실패로 나온다
너무 좁게   「첫 하강이 깨지는 지점까지」로 자르면, 어긋남이 맨 앞에 있을 때
            머리가 1건짜리가 된다 → **1건은 언제나 내림차순이라 통과한다**
```

🚨 **둘째가 더 나쁘다.** 어긋남이 있을 때 오히려 **조용해지기 때문이다.**
2026-09-01에 `645 · 646` 순으로 어긋난 상태에서 ①이 그렇게 통과했다 —
**통과한 것이 아니라 잴 것이 없었다.** 표 머리 뒤로 이어지는 **항목 줄 전체**를
잡아 재고, 구간의 끝을 눈으로 확인한다.

📌 **④는 집합 비교라 색인이 섞여도, 구간을 잘못 잡아도 그대로 성립한다** —
①②보다 오히려 다루기 쉽다. **구간을 못 정하겠으면 ④부터 돌려라.**

⚠️ **비어 있는 번호를 「유실」로 오해하지 말 것.** 다른 세션의 PR에 들어 있어
아직 병합되지 않은 것일 수 있다. **그때는 커밋 메시지에 "N은 어느 PR에 있고
유실이 아니다"를 적어 둔다** — 안 적으면 다음 사람이 메우려 든다.

📌 **날짜도 함께 본다.** 자정 가까이 작업하면 하루 앞선 날짜를 적기 쉽다.
그날 실제로 `09-01`로 적힌 것을 `08-31`로 되돌린 일이 있었다 — 날짜가 하루
앞서면 나중에 경위를 좇을 때 순서가 어긋난다.

### 다른 세션이 잡고 있는 파일을 건드리지 않는다

실기기 QA 중인 브랜치에 커밋하지 않는 것과 같은 이유다(4절). 어느 세션이
어느 파일을 잡고 있는지는 PM에게 물어 확인한다.

#### 🚨 `git checkout` 실패를 **파이프가 삼킨다** (2026-08-31에 두 번 겪음)

남이 잡은 브랜치는 체크아웃이 막힌다.

```
fatal: 'X' is already used by worktree at '...'
```

**그런데 이 실패를 못 보고 다음 명령이 그대로 돈다.**

```
❌ git checkout -q B 2>&1 | tail -2   →  종료 코드가 tail 것이라 항상 0
   git merge origin/main               →  ★ 엉뚱한 브랜치에 병합된다
```

실제로 그날 **이미 병합이 끝난 남의 브랜치에 `main`을 병합**했다. 즉시
`merge --abort`로 되돌려 유실은 없었지만, 되돌리기 전까지 **본인은 옳은
브랜치에 서 있다고 믿고 있었다.**

**그래서 브랜치를 바꾼 뒤에는 반드시 어디에 서 있는지 다시 본다.**

```
git checkout B            ← 파이프를 걸지 않는다
git branch --show-current ← 그리고 눈으로 확인한다
```

🚨 **「바꾼 뒤」로는 모자란다 — 커밋·푸시 직전에도 본다** (2026-09-01)

작업 중에 **남이 내 발밑에서 브랜치를 뺄 수 있다.** 시작할 때 확인했어도
커밋할 때는 다른 브랜치일 수 있다.

```
❌ 시작할 때만 확인      →  그 사이 바뀐 것을 못 잡는다
✅ 커밋·푸시 직전에 한 번 더
```

실제로 그날 한 세션이 자기 커밋이 **로컬 `main` 에 얹힌 것**을 푸시가 실패하고
나서야 알았다(`src refspec ... does not match any`).

#### 🚨 `gh pr merge --delete-branch` 는 남의 워크트리를 튕겨낸다 (2026-09-01)

그 사고의 원인이다.

```
그 브랜치가 체크아웃된 워크트리가 있는데 --delete-branch 로 병합하면
→ 브랜치가 지워지면서 git 이 그 워크트리를 main 으로 옮긴다
→ 거기서 일하던 세션은 자기가 브랜치를 바꾼 적이 없으므로 원인을 못 찾는다
```

**그래서 병합은 아무 브랜치도 안 걸린 곳에서 한다.**

```
git -C <워크트리> checkout --detach origin/main
gh pr merge <번호> --squash --delete-branch
```

⚠️ **분리 상태에서는 `gh pr merge` 가 브랜치를 못 읽어 실패할 수 있다**
(*"could not determine current branch"*). 그때는 **어느 브랜치에도 안 걸린
다른 워크트리**에서 하거나, 병합 전에 본체를 `main` 으로 옮겨 둔다.

📌 **병합한 뒤에는 본체가 어디 서 있는지 다시 본다** — 방금 지운 브랜치에
서 있으면 다음 병합에서 같은 일이 난다.

⚠️ 남이 잡은 브랜치라면 **가져오지 말고 그 세션에 넘긴다.** 충돌을 대신 풀어
주려다 사고가 났다. 급하면 **그 세션의 워크트리에서** 풀되, 먼저 알린다.

📌 이것은 4-2절 첫머리의 *"`git status`는 남이 쓰는지를 알려 주지 않는다"*와
같은 뿌리다 — **셋 다 「내가 어디 서 있는지 확인하지 않았다」였다.**

### 🚨 같은 지시가 여러 세션에 동시에 간다 (2026-09-02 사용자 확인)

**사용자는 하나의 지시를 여러 세션에 동시에 내린다.** 그러므로 지시를 받으면
**"다른 세션도 같은 지시를 받았다"고 가정하고 움직인다.**

📌 **이것이 아래 절들의 전제다.** 이 전제가 없으면 **둘 다 규약을 지켜도
겹친다** — 실제로 그날 그렇게 됐다.

```
「을로 진행」이 PM 과 다른 세션에 둘 다 갔다
  PM     바로 착수 → PR 을 올렸다
  세션    「준비만」 하고 착수 알림은 PR 올릴 때 하려고 미뤘다
결과     같은 파일을 둘이 고쳤다 (일괄 스캔 내보내기의 부서 열)
```

⚠️ **둘 중 누구도 규약을 어기지 않았다.** PM 은 지시대로 움직였고, 세션은
「점유하면 알린다」를 지키려던 참이었다. **어긋난 것은 서로가 같은 지시를 받은
줄 몰랐다는 것 하나뿐이다.**

#### ⭐ 그래서 알리는 시점을 앞당긴다 — **파일을 열기 전**

```
❌ 지시 받음 → 준비 → PR 올릴 때 알림
✅ 지시 받음 → 즉시 알림 → 그다음 파일을 연다
```

🚨 **「준비만 해 두자」는 안전한 선택이 아니다.** 손을 댄 순간 이미 점유이고,
알리지 않으면 남이 같은 자리를 만진다. **커밋하지 않았다는 것은 아무것도
보호해 주지 않는다.**

📌 **알리는 비용이 확인하는 비용보다 싸다.** *"이 지시 다른 세션에도 갔나요?"*를
PM 에게 묻고 답을 기다리느니, **한 줄 알리고 시작하는 편이 빠르다.**

#### 겹친 것을 알았을 때 — **잘못을 가리기 전에 어느 것이 나은지 정한다**

```
① 10분 안에 하나를 고른다        늦어질수록 둘 다 낡는다
② 기준은 「누가 먼저」가 아니라 「어느 쪽이 나은가」
③ 진 쪽이 자기 것을 닫는다       상대에게 닫아 달라고 하지 않는다
④ 겹치는 부분만 뺀다             그날은 코드를 빼고 backlog 줄은 남겼다
```

⭐ 그날은 **테스트가 있는 쪽**으로 정리했다. 한쪽은 열 개수를 한 번 세어
*"16 = 16"* 으로 확인했고, 다른 쪽은 **머리글과 값의 짝을 테스트로 고정**했다.
**전자는 지금만 맞고 다음에 열이 늘면 또 어긋난다** — 그 결함이 애초에 그렇게
생긴 것이었다.

⚠️ **「10분」은 예시가 아니라 그날 실제로 걸린 시간이다.** 숫자가 없으면
*"빨리"* 가 얼마나인지 다음 사람이 모른다.


### ⚠️ 워크트리는 **점유할 때도** 알린다 (2026-08-16 사용자 확정)

2026-08-16 하루에 **워크트리 사고가 두 번** 났다. 두 번 다 원인이 같다.

> **`git status`는 "남이 쓰는지"를 알려 주지 않는다.** 남이 방금 커밋했으면
> 깨끗하게 보인다. **작업 폴더가 깨끗한 것과 주인이 없는 것은 다르다.**

두 번째 사고에서는 갈아탄 세션이 `git stash` → 브랜치 생성 → `pop`을 하다
**다른 세션의 미커밋 문서 170줄을 자기 스태시로 끌어왔다.** 되돌려 유실은
없었지만, 아침에 **당한 쪽이었던 세션이 오후에 낸 사고**였다.

| 시점 | 무엇을 알리나 |
|---|---|
| **지시를 받았을 때** | 무엇을 시작하는지 + 만질 파일 — **파일을 열기 전에**(위 절) |
| **점유할 때** | 어느 워크트리에서 **어느 브랜치를 팠는지** ← 이게 빠져 있었다 |
| **반납할 때** | 손 뗐다 |
| **갈아타기 전** | `git status`가 아니라 **`git branch --show-current`** 로 점유자 확인 |

**점유 신호가 빠지면 PM의 장부에는 "비었다"는 기록만 쌓인다.** 실제로 그날
오간 신호는 전부 반납 신호(*"손 뗐습니다"*, *"쓰셔도 됩니다"*)뿐이었고,
다음 사람은 그 마지막 말만 기억했다.

⚠️ **기기와 같은 규칙이다**(위 절). 다른 점은 하나뿐 — **워크트리는 되돌릴 수
있고 기기는 못 되돌린다.** 그래서 기기가 더 무겁지만, 워크트리도 **알리지
않으면 남의 미커밋 작업이 사라진다.**

#### 워크트리에서는 `git checkout main` 이 안 된다 (2026-08-30 실측)

본체가 `main` 을 잡고 있으므로 **다른 워크트리에서는 같은 브랜치를 두 번 체크아웃할
수 없다.** 작업을 끝내고 브랜치를 반납할 때 이렇게 막힌다.

```
fatal: 'main' is already used by worktree at '/Volumes/Work/Claude/connection-trace-ai-v2-flutter'
```

**반납은 분리 상태로 한다:**

```
git -C <워크트리> fetch origin
git -C <워크트리> checkout --detach origin/main    # ← main 이 아니라 --detach
git -C <워크트리> branch -d <끝난-브랜치>
```

⚠️ **브랜치를 체크아웃한 채로 두면 그 브랜치는 아무도 못 지운다** — `gh pr merge
--delete-branch` 도 *"used by worktree"* 로 실패한다(2026-08-29·30 두 번 겪음).
**반납할 때 분리 상태로 두는 것까지가 반납이다.**

### ⚠️ 실기기도 워크트리처럼 배정받는다 (2026-08-16 사용자 확정)

**기기를 쓰기 전에 PM에게 묻는다.** 파일보다 기기가 더 무겁다 — 워크트리는
되돌리면 되지만 **실기기에는 제3자 개인정보가 실제로 저장된다.**

2026-08-16에 **네 세션이 폴드 하나를 나눠 썼다.** 결과:

- 검증 중인 세션의 화면 위에서 다른 세션이 조작할 뻔했다(그 세션이 스스로 멈춰
  막았다)
- 한 세션이 검증 도중 다른 세션의 빌드를 얹어 **앱이 강제 종료**됐고, 그때 남은
  임시 파일 2건이 **하마터면 결함으로 접수될 뻔했다**
- **명함이 16 → 19장**이 됐는데, 두 세션이 **서로 상대의 등록을 모른 채 각자
  기준선 16과 비교해 둘 다 "17장"이라고 보고**했다

**그래서 개수 변화로 판정하지 않는다.** 세션이 여럿이면 **파일명·크기·시각까지**
봐야 한다. "몇 개 늘었다"는 다른 세션이 손대는 순간 무의미해진다.

**PM이 정할 것**: 어느 세션이 언제까지 쓰는지, 끝나면 **누구에게 넘기는지**,
그리고 **지금 어느 빌드가 깔려 있는지**(다음 세션이 자기 빌드로 갈아야 하는지).

⚠️ **기기 반납 시 `adb logcat` 프로세스를 정리한다** — 붙어 있으면 다음 세션의
로그에 섞인다.

⚠️ **되돌릴 수 없는 조작(삭제·탈퇴·결제)은 자동 조작으로 하지 않는다.**
2026-08-16에 세 세션이 모두 `input tap` 자동화에 실패했고, 그중 하나는 **스크롤
제스처가 촬영 버튼을 눌러 실제 사람의 명함이 저장**됐다. 좌표가 어긋나면 엉뚱한
대상이 지워진다 — **사용자에게 눌러 달라고 부탁한다.**

### 🚨 그런데 목록으로는 못 막는다 — **누르기 전에 화면을 본다** (2026-08-26 사용자 확정)

2026-08-26에 자동 탭이 **광고 수신 동의 화면의 「시작하기」**를 눌렀다(추가 514).
위 목록에는 *"삭제·탈퇴·결제"*만 있고 **동의는 없었다.**

📌 **동의는 삭제만큼 눈에 안 띄지만 서버에 기록이 남고 법적 증빙이 된다.**
정보통신망법상 광고성 정보 수신 동의는 **이용자 본인의 의사**여야 한다 —
자동 조작이 만든 동의 기록은 **동의가 아니다.**

⚠️ **그렇다고 목록에 「동의」를 더하는 것으로는 안 된다.** 오늘은 동의 화면이
빠져 있었고 **내일은 또 다른 것이 빠진다.** 목록은 언제나 뒤늦다.

> **사고의 원인은 「누르면 안 되는 것을 눌렀다」가 아니라
> 「내가 아는 화면」과 「실제 화면」이 달랐다는 것이다.**

그날 사고는 **검증 중에 예상하지 못한 화면이 끼어들어** 좌표가 겹친 것이었다.
어떤 목록도 *"지금 화면이 내가 생각한 그 화면인가"*를 대신해 주지 못한다.

**그래서 규칙은 이것 하나다.**

```
자동 탭을 보내기 전에 그 화면을 먼저 확인한다.
   화면 캡처를 읽고, 누를 것이 거기 있는지 보고, 그다음 누른다.
```

📌 **느리다. 그런데 그 방식에서는 사고가 안 났다** — 사고 뒤 그렇게 바꾼
세션에서 같은 일이 다시 나지 않았다(추가 514).

⚠️ **화면을 봤는데도 되돌릴 수 없는 조작이면, 그때는 위 원칙대로 사용자에게
부탁한다.** 「먼저 본다」는 「자동으로 눌러도 된다」가 아니다.

## 5. 서브에이전트

이 프로젝트 전용 에이전트 4개가 `.claude/agents/`에 있다.

| 에이전트 | 맡는 일 |
|---|---|
| `flutter-planner` | PM·오케스트레이터. 스펙 작성, 다른 에이전트에 위임, 결과 검토 |
| `flutter-developer` | 로직·데이터·버그 수정 |
| `flutter-ui-designer` | 화면·색상·간격·접근성 |
| `flutter-qa` | 인수 기준 대비 실기기 검증, 결함 리포트(수정은 안 함) |

**`flutter-qa`에 넘기면 품질이 보장된다고 생각하면 안 된다.** QA는 "기대
동작이 이미 명시된 것"을 재확인하는 장치다. 스펙에 없는 영역(데이터 실물,
장기 상태, 정책 준수)은 QA를 몇 번 돌려도 안 나온다. 근거는 backlog 추가 81.

## 6. 빌드·배포

### ⚠️ 최상위 지침 — 과금은 스토어 출시 전에 테스트·배포하고, 최종 결정은 사용자다 (2026-08-14 사용자 확정)

**전 팀·전 세션에 적용된다.** 앱을 구글 플레이 스토어에 올리기 **전에**, 비용
(과금)에 관련한 부분을 **반드시 테스트하고 배포**하는 것을 하나의 관문으로
삼는다. 과금 기능이 검증 없이 출시에 딸려 나가서는 안 된다. 과금 **테스트·
배포·스토어 출시**의 go/no-go는 전부 **사용자 최종 결정**이다.

- 앱이 아직 스토어 미등록·테스트 중이고 테스터가 실제 Firebase 백엔드를 쓰므로,
  과금(`firestore.rules`·Cloud Functions·`config/billing.model` 활성화)을 섣불리
  배포·활성화하면 테스터의 AI 사용량·크레딧 동작을 건드린다. 돈이 오가는 부분이라
  검증 없이 나가면 되돌리기 어렵다.
- **"main 병합 / 서버 배포(`firebase deploy`) / 과금 활성화 / 앱·스토어 배포"는
  서로 다른 층이다.** 병합은 사용자에게 닿지 않지만, 나머지는 모두 사용자 최종
  결정이다.

### ⚠️ 2026-08-18 개정 — 안전장치는 "병합 보류"가 아니라 "플래그 잠금"이다

**옛 규칙은 이랬다**: *"지갑/과금 코드(`feat/ai-credit-wallet`)는 완성해도 main
병합·배포를 보류하고 브랜치에 보존한다 — main에 얹어두면 다른 이유의
`firebase deploy`에 딸려 배포될 위험이 있어 병합 자체를 미룬다."*

**2026-08-18에 사용자 결정으로 병합했다**(PR #299, `2a1158b`). 그러므로 위
문장은 더 이상 현재 상태가 아니다. 대신 이렇게 읽어야 한다.

📌 **딸려 배포돼도 켜지지 않는다.** 실물로 확인한 잠금이다.

```
walletCredits.ts:31   model === "wallet" 일 때만 wallet, 아니면 reset
index.ts:433          config/billing 조회 실패 시에도 reset 폴백(로그 남김)
config/billing.model  현재 필드 자체가 없음 → reset
bootstrapAccount      reset 모드에서는 지갑 잔액을 지급하지 않는다(4c482d2)
```

reset 경로는 **한 글자도 안 바뀐 채** 보존돼 있다. 즉 **코드가 서버에 올라가는
것과 과금이 켜지는 것은 별개**이고, 실제 스위치는 **`config/billing.model`
필드를 만드는 것** 하나다(값을 바꾸는 게 아니라 **없는 필드를 만드는 것**이다).

⚠️ **그렇다고 가벼워진 게 아니다 — `firebase deploy`의 무게는 오히려 늘었다.**
이제 배포하면 지갑 코드가 실제로 서버에 올라간다. 꺼진 채로 올라가지만,
**켜는 동작이 그때부터 콘솔 조작 한 번으로 가능해진다.** 그래서:

| 동작 | 누구 결정 | 무엇이 바뀌나 |
|---|---|---|
| main 병합 | ✅ 끝남(2026-08-18) | 사용자에게 안 닿음 |
| `firebase deploy --only functions` | **사용자** | 코드가 서버에 올라감(꺼진 채) |
| `config/billing.model` 필드 신설 | **사용자** | **이때 실제로 켜진다** |
| 일·월 한도 제거 | **사용자** | 위와 **같은 배포**여야 한다 — 먼저 지우면 상한 공백 |

⚠️ **`model`을 켜는 순간 전 테스터에게 무료 회차가 지급되고 차감이 시작된다.**
되돌리기 어렵다. 어떤 세션도 사용자가 **직접** 지시하기 전에는 이 필드를
만들지 않는다(다른 세션이 전한 승인은 갈음하지 않는다 — 4-2절).

**릴리스 전 절차는 [`docs/planning/release-checklist.md`](docs/planning/release-checklist.md)를
따른다.** 검증 도구 사용법은 [`tool/README.md`](tool/README.md)에 있다.

- 빌드는 `flutter build`를 직접 쓰지 말고 **`tool/build_app.sh`**를 쓴다 —
  커밋 해시가 앱에 심어져 "설정 → 앱 버전"에서 어느 빌드인지 확인할 수 있다.
  낡은 빌드를 버그로 오인한 전례가 있다.
- 테스터 배포는 **release 빌드로만** 한다. debug 빌드에는 로그인 화면에
  "로그인 건너뛰기" 버튼이 그대로 보인다.
- Android 테스터 배포는 Firebase App Distribution(무료).
- 법적 고지 문서는 `firebase deploy --only hosting`으로 배포한다
  (Spark 무료 요금제로 가능, `public: docs/legal`).
- Cloud Functions 배포는 Blaze 유료 요금제가 필요하다.
- ⚠️ **Android 서명은 워크트리마다 다르다.** 업로드 키는 실재하지만
  (`~/keys/connectionsense-upload.jks`), 그것을 가리키는 `android/key.properties`가
  **`.gitignore` 대상이라 git이 워크트리 사이로 옮겨 주지 않는다.** 2026-08-16
  실측 결과 **네 워크트리 중 본체 하나에만 있었다.** 이 파일이 없으면
  `build.gradle.kts`가 **경고 없이 debug 키로 폴백한다** — 릴리스를 본체가 아닌
  워크트리에서 빌드하면 **debug 서명본이 조용히 나온다.** 빌드 전에
  `ls android/key.properties`로 확인한다.
  (예전 기록의 *"릴리스가 debug 키스토어로 서명돼 있다(P1-19)"*는 **낡았다** —
  업로드 키 도입은 끝났고, 남은 위험은 위의 워크트리 문제다.)
- **서명 키·인증서는 저장소 밖(예: `~/keys/`)에 두는 것을 원칙으로 한다.**
  루트 `.gitignore`에 `*.jks`/`*.keystore`/`key.properties`/`*.p8`/`*.p12`/
  서비스 계정 JSON을 넣어 뒀지만(2026-08-08), 그건 실수로 안에 들여놨을 때의
  안전망이지 보관 위치가 아니다. 한 번 커밋되면 히스토리에서 지우기 어렵다.
  Android 업로드 키를 잃으면 **앱 업데이트를 영원히 못 올리고**, Apple `.p8`은
  **발급 시 한 번만 내려받을 수 있다** — 둘 다 별도 백업이 필요하다.
