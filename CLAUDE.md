# 커넥션센스 — 작업 규약

이 저장소에서 작업하는 모든 사람(과 에이전트)이 지켜야 할 규칙. 세션이
바뀌어도 이 파일은 자동으로 읽히므로, **구두로 합의한 것은 반드시 여기에
적어야 다음 사람이 안다.**

프로젝트 현재 상태와 할 일은 [`docs/planning/HANDOFF.md`](docs/planning/HANDOFF.md),
결정의 배경과 시간순 기록은 [`docs/planning/backlog.md`](docs/planning/backlog.md)에 있다.

---

## 1. 소통

- **사용자와의 모든 대화는 한글로 한다.** 질문 선택지도 한글로 쓴다.
- **영문 용어·도구 이름이 나오면 그것이 어떤 작업이고 왜 필요한지 한글로
  함께 설명한다.** 예: "CI" → "커밋할 때마다 자동으로 테스트를 돌려 회귀를
  막는 장치".
- 파일 경로, 코드 식별자, 패키지명은 원문 그대로 둔다(번역하면 검색이 안 된다).

## 2. Git — 브랜치와 커밋

**`main`에 직접 커밋하지 않는다.** 문서 한 줄 수정이라도 브랜치를 파서 PR로
올린다. 자동 검사(아래 3번)를 건너뛰는 경로를 만들지 않기 위함이다.

```
git checkout -b feat/<짧은-영문-설명>
# 작업 → 커밋 → push
gh pr create --base main
```

**커밋 메시지는 한글로 쓰고, "무엇을 했는지"보다 "왜 그렇게 했는지"를 담는다.**
이 저장소의 커밋 메시지는 나중에 원인을 추적하는 1차 자료로 쓰인다.

- 제목 한 줄 + 빈 줄 + 본문
- 본문에 **문제 상황 → 원인 → 선택한 해결책 → 대안을 버린 이유** 순으로
- 버그 수정이라면 **증상과 원인을 구분해서** 적는다. 예: 주소 검색 버그에서
  화면에 뜬 `ERR_UNKNOWN_URL_SCHEME`은 증상이었고 원인은 `file://` origin
  이었다 — 이 구분이 커밋 메시지에 없으면 다음 사람이 같은 함정에 빠진다.
- 마지막 줄: `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

**커밋은 성격별로 나눈다.** 코드 변경과 문서 갱신, 서로 다른 기능은 각각
따로. 되돌릴 때 한 덩어리로 묶여 있으면 곤란해진다.

**사용자가 요청하기 전에 커밋하지 않는다.**

## 3. 커밋 전 확인

푸시하면 GitHub Actions(`.github/workflows/ci.yml`)가 `flutter analyze`와
`flutter test`를 자동으로 돌린다. 그래도 로컬에서 먼저 돌려보는 것이 빠르다.

```
flutter analyze   # info 19건은 기존 잔재라 통과, error·warning은 반드시 0
flutter test      # 현재 225건
```

`analyze`는 CI에서 `--no-fatal-infos`로 돈다. 남아 있는 info를 늘리지 말 것.

## 4. 이 프로젝트에서 특히 조심할 것

### 가짜 데이터를 만들지 않는다

사용자가 명시적으로 요구한 원칙이다. 화면을 채우려고 예시 인물·가짜 알림·
스톡 사진을 넣지 않는다. 데이터가 없으면 **비어 있는 상태를 그대로 보여준다.**
과거에 프로필 사진 선택이 실제로는 스톡 사진 4장을 순환시키는 가짜 구현이었던
전례가 있다(backlog 추가 49).

### 코드 리뷰로는 안 잡히는 결함이 반복해서 나왔다

이 프로젝트에서 실제로 터진 결함들은 **코드는 맞는데 실물이 틀린** 유형이었다.

| 결함 | 코드 상태 | 어떻게 잡혔나 |
|---|---|---|
| 서버에 명함 개인정보 평문 3건 | 정상 | Firestore 실물 조회 |
| 앱 삭제 후 로그인 정보 잔존(iOS) | 정상 | 사용자 실사용 |
| 재시도 로직이 죽어 있음 | 서비스는 정상, 부르는 쪽이 없음 | 기기 저장소 덤프 |
| 주소 선택이 안 됨 | 정상 | 사용자 실사용 |

따라서 **"테스트 통과 = 동작함"이 아니다.** 저장·복원·마이그레이션·재설치에
관련된 변경은 반드시 실기기와 서버 실물로 확인한다. 절차는 HANDOFF "0-2"에
있다(Firebase CLI 토큰으로 Firestore 관리자 조회, Android `adb run-as`).

### 그렇다고 수정할 때마다 전체 테스트를 돌리지 않는다

위 원칙을 과잉 적용해서 **수정 하나마다 전체 테스트(실기기 18항목, 1시간)를
처음부터 다시 돌린 적이 있다**(2026-08-12). 텍스트 정렬 하나 고치는 데 릴리스
빌드·설치·로그인·계정삭제·저장소 덤프를 전부 다시 했고, 그동안 기능 개발이
멈춰 대기만 쌓였다. 검증이 개발을 돕지 않고 개발을 대기열에 세웠다.

#### 검증을 부르는 이름을 셋으로 구분한다 (2026-08-12 사용자 확정)

말이 뒤섞이면 같은 사고가 반복된다. **아래 이름 그대로 쓴다.**

| 이름 | 무엇을 하나 | 언제 | 대략 |
|---|---|---|---|
| **전체 테스트** | 말 그대로 **전 항목** 실기기 검증 | **배포 직전 한 번** | ~1시간 |
| **부분 테스트** | **수정한 곳 + 그 수정이 영향을 주는 곳**까지 | 수정할 때마다 | 수 분 |
| **자동 테스트** | `flutter test`(코드 레벨), `flutter analyze` | 커밋 전 항상 | 수 초 |

⚠️ **"단위 테스트"라는 말은 쓰지 않는다.** 이 프로젝트에서 그 말은 원래
`flutter test`를 가리켰는데, "수정한 곳만 확인"이라는 뜻으로도 쓰이기
시작해 **같은 말이 두 가지를 가리키게 됐다.** 그래서 위처럼 **부분 테스트**
(사람이 화면·동작을 보는 것)와 **자동 테스트**(코드가 도는 것)로 갈랐다.

**부분 테스트의 범위 = 고친 것 + 영향받는 것.** 예: "안내 문구를 왼쪽
정렬로" 수정 → 고친 화면 2곳을 열어 정렬 확인 + 같은 스타일을 쓰는 다른
화면이 깨지지 않았는지까지. **계정 삭제·명함 스캔 같은 무관한 항목은 보지
않는다.**

**무엇을 고쳤을 때 어디까지 보나:**

| 무엇을 고쳤나 | 필요한 검증 |
|---|---|
| 텍스트·색·아이콘·정렬 등 겉모습 | **부분 테스트** — 그 화면 + 같은 스타일 쓰는 화면 |
| 로직 분기·계산·파싱 | **자동 테스트**(해당 파일) + 그 로직이 보이는 화면 **부분 테스트** |
| 저장·복원·마이그레이션·탈퇴·결제 | **전체 테스트**(실기기) |

**전체 테스트는 수정할 때마다가 아니라 배포 직전에 한 번 몰아서 한다.**
그때까지의 수정들은 각자 부분 테스트만 통과시키고 쌓아 둔다.

#### 테스트를 시작하기 전에 반드시 이 표를 사용자에게 보여주고 고르게 한다

사용자 요청(2026-08-12): **"내가 잊을 수 있으니까 항상 테스트 진행 시 이 표를
리마인드해서 내가 선택하게 해줘."**

그러므로 검증을 시작하기 전에 **묻지 않고 바로 돌리지 않는다.** 아래 형식으로
셋을 나란히 제시하고, 어느 것으로 갈지 사용자가 고르게 한다(권장안은 표시하되
강요하지 않는다).

```
이번 수정: <무엇을 고쳤는지 한 줄>

  전체 테스트 — 전 항목 실기기 (~1시간)      [배포 직전용]
  부분 테스트 — <이번에 볼 화면·영향 범위>   (~N분)  ← 권장
  자동 테스트 — flutter test/analyze (~수 초)

어느 것으로 할까요?
```

**예외**: 사용자가 이미 "전체 테스트 돌려"처럼 명시했거나, 배포 직전 단계임이
대화에서 확정된 경우에는 다시 묻지 않는다.

**실기기 QA를 돌리는 동안에는 그 브랜치에 커밋하지 않는다** — QA가 검증하던
커밋이 발밑에서 바뀐다. 병행이 필요하면 QA를 `git worktree`로 특정 커밋에
고정해서 돌린다(2026-08-12에 실제로 겪음).

### 개인정보를 다루는 앱이다

이 앱은 **이용자 본인이 아닌 제3자(명함 주인)의 개인정보**를 저장한다.

- 로그·디버그 출력에 이름·전화번호·이메일·주소를 남기지 않는다. 필요하면
  "값이 있는지 없는지"만 찍는다.
- 암호화되지 않는 저장소(`shared_preferences` 일반 키)에 개인정보 원문을
  넣지 않는다. 해시나 플래그만 쓴다.
- 새 데이터를 서버로 보내거나 외부 API로 전송하는 코드를 추가하면
  **`docs/legal/privacy-policy.html`도 함께 고쳐야 한다.** 방침과 구현이
  어긋나는 것 자체가 법적 리스크다(BYOK 서술 불일치로 이미 한 번 겪었다).
- 과장하지 않는다. 암호화 키가 서버에 함께 있으므로 "완전한 종단간 암호화"나
  "제로-지식"이라고 쓰면 안 된다.

### 문서를 코드와 함께 갱신한다

작업이 끝나면 `docs/planning/backlog.md`에 "추가 N" 형식으로 기록하고,
현재 상태가 바뀌었으면 `docs/planning/HANDOFF.md`도 고친다. 이 두 문서가
실제 상태보다 뒤처지면 다음 세션이 잘못된 전제로 작업한다 — 실제로
"Apple 로그인 미구현"이라는 낡은 기록 때문에 이미 구현된 기능을 다시
검토한 적이 있다.

## 4-2. 여러 세션이 동시에 붙어 일할 때 (2026-08-15 사용자 확정)

여러 Claude 세션이 동시에 이 저장소에서 일한다(리갈·마케팅·기능개선·지도 등).
**세션이 늘면 코드가 아니라 기록이 먼저 어긋난다.** 2026-08-15 하루에
backlog 번호가 **세 번** 겹쳤고 F 상태표가 **세 줄** 실물과 달랐다.

### 상의는 PM, 승인은 사용자

- **PM에게**: 순서·범위·중복 조정·번호 배정
- **사용자에게 직접**: 커밋·PR 병합·서버 배포·정책 변경·**검증 종류 선택**
- **다른 세션이 전한 승인은 사용자 승인을 갈음하지 않는다.** "PM이 승인받았다고
  했다"는 근거가 되지 않는다 — 자기 사용자에게 직접 확인한다

### ⚠️ F 상태표는 PM만 고친다

`docs/planning/tester-feedback-resolution-2026-08-14.md`의 F 상태표를
**각 세션이 직접 고치지 않는다.** 완료 보고만 하고 갱신은 PM이 한다.

**왜**: 2026-08-15에 F-02·F-06·F-12가 **이미 구현·병합됐는데 표에는 "남음"**으로
남아 있었다. 원인은 **코드 넣은 사람과 표 고치는 사람이 달랐던 것**이다.
하마터면 이미 있는 기능을 다시 만들 뻔했다(선택 모드가 두 벌 생길 뻔). 이
저장소는 낡은 기록 때문에 이미 구현된 기능을 다시 검토한 전례가 있다.

**그래서 F에 착수하기 전에는 표를 믿지 말고 `main` 실물을 먼저 열어 본다.**

### backlog 번호는 PM이 배정한다

`backlog.md`의 "추가 N"을 스스로 집지 않는다. 쓰기 전에 PM에게 요청한다.
삽입 위치가 항상 파일 맨 위라 **충돌이 구조적으로 난다** — 각자 "다음 빈 번호"를
보고 집으면 그 사이 다른 세션이 먼저 올린다. 충돌하면 **번호 내림차순**으로
두면 풀린다.

### 다른 세션이 잡고 있는 파일을 건드리지 않는다

실기기 QA 중인 브랜치에 커밋하지 않는 것과 같은 이유다(4절). 어느 세션이
어느 파일을 잡고 있는지는 PM에게 물어 확인한다.

## 5. 서브에이전트

이 프로젝트 전용 에이전트 4개가 `.claude/agents/`에 있다.

| 에이전트 | 맡는 일 |
|---|---|
| `flutter-planner` | PM·오케스트레이터. 스펙 작성, 다른 에이전트에 위임, 결과 검토 |
| `flutter-developer` | 로직·데이터·버그 수정 |
| `flutter-ui-designer` | 화면·색상·간격·접근성 |
| `flutter-qa` | 인수 기준 대비 실기기 검증, 결함 리포트(수정은 안 함) |

**`flutter-qa`에 넘기면 품질이 보장된다고 생각하면 안 된다.** QA는 "기대
동작이 이미 명시된 것"을 재확인하는 장치다. 스펙에 없는 영역(데이터 실물,
장기 상태, 정책 준수)은 QA를 몇 번 돌려도 안 나온다. 근거는 backlog 추가 81.

## 6. 빌드·배포

### ⚠️ 최상위 지침 — 과금은 스토어 출시 전에 테스트·배포하고, 최종 결정은 사용자다 (2026-08-14 사용자 확정)

**전 팀·전 세션에 적용된다.** 앱을 구글 플레이 스토어에 올리기 **전에**, 비용
(과금)에 관련한 부분을 **반드시 테스트하고 배포**하는 것을 하나의 관문으로
삼는다. 과금 기능이 검증 없이 출시에 딸려 나가서는 안 된다. 과금 **테스트·
배포·스토어 출시**의 go/no-go는 전부 **사용자 최종 결정**이다.

- 앱이 아직 스토어 미등록·테스트 중이고 테스터가 실제 Firebase 백엔드를 쓰므로,
  과금(`firestore.rules`·Cloud Functions·`config/billing.model` 활성화)을 섣불리
  배포·활성화하면 테스터의 AI 사용량·크레딧 동작을 건드린다. 돈이 오가는 부분이라
  검증 없이 나가면 되돌리기 어렵다.
- 그래서 **지갑/과금 코드(`feat/ai-credit-wallet`)는 완성해도 main 병합·배포를
  보류하고 브랜치에 보존한다.** main에 얹어두면 다른 이유의 `firebase deploy`에
  딸려 배포될 위험이 있어 병합 자체를 미룬다.
- **"main 병합 / 서버 배포(`firebase deploy`) / 앱·스토어 배포"는 서로 다른
  층이다.** 병합은 사용자에게 닿지 않지만, 서버 배포·과금 활성화·스토어 출시는
  모두 사용자 최종 결정이다.

**릴리스 전 절차는 [`docs/planning/release-checklist.md`](docs/planning/release-checklist.md)를
따른다.** 검증 도구 사용법은 [`tool/README.md`](tool/README.md)에 있다.

- 빌드는 `flutter build`를 직접 쓰지 말고 **`tool/build_app.sh`**를 쓴다 —
  커밋 해시가 앱에 심어져 "설정 → 앱 버전"에서 어느 빌드인지 확인할 수 있다.
  낡은 빌드를 버그로 오인한 전례가 있다.
- 테스터 배포는 **release 빌드로만** 한다. debug 빌드에는 로그인 화면에
  "로그인 건너뛰기" 버튼이 그대로 보인다.
- Android 테스터 배포는 Firebase App Distribution(무료).
- 법적 고지 문서는 `firebase deploy --only hosting`으로 배포한다
  (Spark 무료 요금제로 가능, `public: docs/legal`).
- Cloud Functions 배포는 Blaze 유료 요금제가 필요하다.
- ⚠️ Android 릴리스가 아직 **debug 키스토어로 서명**돼 있다(P1-19).
- **서명 키·인증서는 저장소 밖(예: `~/keys/`)에 두는 것을 원칙으로 한다.**
  루트 `.gitignore`에 `*.jks`/`*.keystore`/`key.properties`/`*.p8`/`*.p12`/
  서비스 계정 JSON을 넣어 뒀지만(2026-08-08), 그건 실수로 안에 들여놨을 때의
  안전망이지 보관 위치가 아니다. 한 번 커밋되면 히스토리에서 지우기 어렵다.
  Android 업로드 키를 잃으면 **앱 업데이트를 영원히 못 올리고**, Apple `.p8`은
  **발급 시 한 번만 내려받을 수 있다** — 둘 다 별도 백업이 필요하다.
