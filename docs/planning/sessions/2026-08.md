# 세션 로그 — 2026-08

> **HANDOFF.md의 `0` ~ `0-23` 섹션을 2026-08-18에 이리로 옮겼다**(추가 300).
> HANDOFF는 "지금 상태"를 답하는 문서인데 세션 로그가 68%(1,647줄)를 차지해
> 원래 역할이 묻혀 있었다.
>
> ⚠️ **번호는 그대로 둔다.** `CLAUDE.md`·`AGENTS.md`·`RESUME.md`·
> `beta-observability-plan.md`·`run-app/SKILL.md`·`backlog.md`가
> `HANDOFF "0-2"` 같은 형태로 참조한다 — 다시 매기면 전부 깨진다.
>
> 순서는 **최신이 위**다(옮기면서 정렬했다 — 옛 파일에서는 정순·역순이
> 뒤섞여 있었다). 제목의 "(가장 최신)" 표시는 뺐다. 6개 섹션에 동시에
> 붙어 있어서 아무것도 가리키지 못했다.

## 목차

| 번호 | 날짜 | 무엇을 했나 |
|---|---|---|
| `0-23` | 2026-08-15 | [지갑/과금 세션 인계 + 테스터 배포 완료](#0-23-2026-08-15--지갑과금-세션-인계--테스터-배포-완료) |
| `0-22` | 2026-08-15 | [저장소 정리 세션: 갇힌 기록 회수 + 테스터 초기화 방침](#0-22-2026-08-15--저장소-정리-세션-갇힌-기록-회수--테스터-초기화-방침) |
| `0-21` | 2026-08-15 | [PM 세션 마무리: 결정 5건·F 6건·방침 개정안](#0-21-2026-08-15--pm-세션-마무리-결정-5건f-6건방침-개정안) |
| `0-20` | 2026-08-15 | [세션 소멸 후 인수인계 정리](#0-20-2026-08-15--세션-소멸-후-인수인계-정리) |
| `0-19` | 2026-08-15 | [관리자 보안 감사 12건 전부 수정·배포 완료](#0-19-2026-08-15--관리자-보안-감사-12건-전부-수정배포-완료) |
| `0-18` | 2026-08-14 | [F-07 재생성 다양성 구현 + 64커밋 3분할 PR](#0-18-2026-08-14-저녁--f-07-재생성-다양성-구현--64커밋-3분할-pr) |
| `0-17` | 2026-08-14 | [인식 정확도 실측 전환 + 테스터 오류 12건 종결](#0-17-2026-08-14--인식-정확도-실측-전환--테스터-오류-12건-종결) |
| `0-16` | 2026-08-13 | [명함 인식 결함 연속 수정 + 일괄 스캔 도구 신설](#0-16-2026-08-13--명함-인식-결함-연속-수정--일괄-스캔-도구-신설) |
| `0-15` | 2026-08-12 | [베타 관측 체계 기획 (문서만)](#0-15-2026-08-12--베타-관측-체계-기획-문서만) |
| `0-14` | 2026-08-12 | [QA 완료·버그 3건 수정·배포 직전](#0-14-2026-08-12--qa-완료버그-3건-수정배포-직전) |
| `0-13` | 2026-08-11 | [테스터 빌드 2차 통합 완성, QA 직전 중단](#0-13-2026-08-11-밤08-12-새벽--테스터-빌드-2차-통합-완성-qa-직전-중단) |
| `0-12` | 2026-08-11 | [AI 충전 화면 UI 신설](#0-12-2026-08-11--ai-충전-화면-ui-신설) |
| `0-11` | 2026-08-11 | [심사 통과·과금 확정·충전 관리 콘솔·에셋 구조화](#0-11-2026-08-11--심사-통과과금-확정충전-관리-콘솔에셋-구조화) |
| `0-10` | 2026-08-10 | [Android/iOS 직원 테스터 등록 + iOS 빌드 6 베타 심사 제출](#0-10-2026-08-10--androidios-직원-테스터-등록--ios-빌드-6-베타-심사-제출) |
| `0-9` | 2026-08-09 | [직원 테스트 인프라(허용목록·관측) 배포 + APK 준비](#0-9-2026-08-09-오후--직원-테스트-인프라허용목록관측-배포--apk-준비) |
| `0-8` | 2026-08-09 | [Android 서명 키(P1-19) + iOS 빌드5 목적문자열 + TestFlight 외부배포 세팅](#0-8-2026-08-09--android-서명-키p1-19--ios-빌드5-목적문자열--testflight-외부배포-세팅) |
| `0-7` | 2026-08-08 | [다음에 이어받을 순서](#0-7-2026-08-08-세션-마무리--다음에-이어받을-순서) |
| `0-6` | 2026-08-08 | [App Check 도입(P0-9) + 옛 iOS 앱 참조 교정](#0-6-2026-08-08--app-check-도입p0-9--옛-ios-앱-참조-교정) |
| `0-5` | 2026-08-07 | [AI 브리핑 실전 배포 + 실기기 E2E 검증 완료](#0-5-2026-08-07--ai-브리핑-실전-배포--실기기-e2e-검증-완료) |
| `0-4` | 2026-08-05 | [아이콘 시스템 + 브랜드 블루 리브랜딩](#0-4-2026-08-05-밤--아이콘-시스템--브랜드-블루-리브랜딩) |
| `0-3` | 2026-08-05 | [AI 연동 서버 프록시 전환 + 공지사항/1:1문의/관리자콘솔 + 동시 작업 브랜치 병합](#0-3-2026-08-05--ai-연동-서버-프록시-전환--공지사항11문의관리자콘솔--동시-작업-브랜치-병합) |
| `0-2` | 2026-08-05 | [법적 고지 문서 체계 정비 + 플랫폼별 현황 재조사](#0-2-2026-08-05--법적-고지-문서-체계-정비--플랫폼별-현황-재조사) |
| `0-1` | 2026-08-04 | [iOS 배포 트러블슈팅 + 계정/데이터 안전장치 + 암호화](#0-1-2026-08-04-밤--ios-배포-트러블슈팅--계정데이터-안전장치--암호화) |
| `0` | 2026-08-03 | [출시 기준 재설계](#0-2026-08-03-출시-기준-재설계) |

---

## 0-24. 2026-08-19 — 상세 화면·개인정보 정리·OCR 84% (PR 20건)

**하루에 PR 20건을 병합했다.** 갈래가 넷이다.

### ① 명함 상세 화면 (추가 330·331·332·333)

목록에서 명함을 누르면 **곧장 편집 폼**이 떴다. 여는 이유는 대개 *"연락하려고"*
인데 **읽으려는 사람에게 입력 화면을 준 셈**이었다.

**실기기 제보 넷을 연달아 고쳤고, 넷 다 코드 리뷰로는 안 나왔다.**

| 증상 | 원인 |
|---|---|
| 닫을 방법이 없다 | 손잡이가 스크롤 안에 있었다 |
| X가 **보이는데 안 눌린다** | 바텀시트 맨 위는 **끌어 내리는 제스처** 자리 |
| 자료가 많으면 이름이 위로 붙는다 | 전 칸을 그려 **길이가 들쭉날쭉** |
| 이력을 리스트로 보고 싶다 | 메모에 한 덩어리로 쌓이고 있었다 |

📌 **둘째가 배울 게 많다.** 버튼은 렌더링됐고 위치도 맞았다 — **스크린샷으로도
안 나오고 눌러 봐야** 나온다.

📌 **셋째는 여백 조정으로 안 풀렸다.** 실제로 먼저 넓혀 봤지만 긴 명함은 그대로
밀렸다. 근본은 여백이 아니라 **길이**였고, 담는 것을 줄이니 풀렸다.

이력은 **저장 구조를 안 건드리고 읽는 쪽만** 만들었다(사용자가 "작게"를 고름).
⚠️ **칸으로 나누어 보여주지 않는다** — 빈 칸이 통째로 빠져 저장돼 조각 개수가
명함마다 달라, 되살릴 수 없다. 추측해 붙이면 **틀린 이력이 그럴듯하게** 남는다.

### ② 저장소에서 제3자 개인정보를 걷어냈다 (추가 332·337)

`backlog.md`에 **실명과 휴대폰번호가 나란히** 있었다. **내가 전날 넣었다** —
결함을 재현 가능하게 적으려던 것인데, 재현에 필요한 건 **모양**이지 값이 아니다.

훑어보니 나 혼자가 아니었다 — 여러 세션이 몇 달에 걸쳐 쌓았다.

```
휴대폰 8 · 유선·팩스 18 · 이메일 9 · 실명 8명   문서 8개
```

⚠️ **자동 검색이 두 번 틀렸고 둘 다 눈으로 잡았다** — 처음엔 너무 넓게(이미
가짜인 예시까지), 그다음 너무 좁게(`\b01…`이 `MO10-`을 놓침). **정규식을
들여다봐서가 아니라 연습 실행 결과를 전수로 읽다가** 나왔다.

⚠️ **이름은 자동으로 못 찾는다.** 번호·이메일은 모양이 있지만 이름은 없다.
8명 전부 눈으로 찾았고 **남아 있을 수 있다.**

그리고 **개인정보처리방침이 실제보다 좁았다.** 부서·사무실 전화·대화 소재·재연락
기록이 빠져 있었다. ⚠️ **부서는 이 세션이 그날 넣고 방침을 안 고친 것**이다
(CLAUDE.md 4절 위반). 사용자 질문이 아니었으면 안 걸렸다.

### ③ OCR 83% → 84% — **파서를 한 줄도 안 고쳤다** (추가 334·335·336)

**자를 고쳤더니 숫자가 올랐다.** 그만큼 원래 파서가 맞았는데 틀렸다고 세어지고
있었다.

```
card_11·13·14 = 한 사진에 명함 여러 장(5·8·2장)
  → 쪼갠 것(card_201~217)이 이미 있었는데 원본만 안 치워 계속 채점되고 있었다
정답지 값 7건 정정 (명함 실물을 열어 보고)
채점기가 이메일 대소문자를 구분하고 있었다 → 이메일 92% → 94%
```

⚠️ **좌표 규칙(R-05)에 착수하려다 "잴 수 없다"를 먼저 발견했다.** 앱은 좌표를
내보내는데 **하네스가 안 읽고 있었다.** 규칙을 만들어도 좋아졌는지 알 수 없는
상태였다 — **그래서 규칙 대신 자를 만들었다.** 코드 주석이 이미 같은 말을 하고
있었다(*"구현보다 이 칸이 먼저다"*).

### ④ 관리자 — 서버에만 있고 부르는 쪽이 없던 기능 (추가 338)

```
getUserUsage         서버 ✅ 앱 ✅ 화면 ✅     이미 완성돼 있었다
grantSupportCredits  서버 ✅ 앱 ❌ 화면 ❌     함수만 떠 있었다
```

⚠️ **이 갈래를 두 번 틀렸다.** `getUserUsage`를 *"화면이 없다"*고 보고했는데
**이미 다 있었다** — 서버 함수 이름으로만 grep하고 감싼 이름(`fetchForAdmin`)을
안 봤다.

> **이름 하나로 "없다"고 단정하지 않는다.** 감싸는 층이 있으면 원래 이름은
> 안 나온다.

**멱등성 키를 시트를 열 때 한 번만** 만든다 — 누를 때마다 만들면 *"끊긴 줄 알고
한 번 더"* 가 **두 번 지급**이 된다. 돈이 오가는 자리다.

### 이 세션이 틀린 것 셋 (전부 사용자 질문·지적으로 걸렸다)

```
1. 개인정보 훑기 — 너무 넓게, 그다음 너무 좁게
2. 부서 필드를 넣으며 방침을 안 고침 (CLAUDE.md 4절)
3. getUserUsage를 "없다"고 보고 — 이미 다 있었다
```

그리고 **라틴 2차 패스의 이득을 절대 건수로 말해** 작아 보이게 했다. 사용자가
*"명함이 많으면 필요할 거야"*라고 짚었고 **원칙은 그쪽이 맞았다** — 다만 실물을
여니 **내 숫자는 양쪽으로 틀렸다**(주소 3건은 근거가 없었고, 실제 사정거리는
5건이 아니라 3건이었다).

### 내일로 넘긴 것 — **안드로이드 USB 디버깅이 안 켜져 있었다**

기기는 USB에 붙어 있었고(`SAMSUNG_Android`) **ADB 인터페이스만 없었다.** 일괄
스캔 **한 번**에 좌표 자료·라틴 패스 측정·표본 79→96장이 **같이** 나온다.

측정 대기 목록은 [`HANDOFF.md` 2-0](../HANDOFF.md#2-0-사용자가-결정할-일)에 있다.
📌 **측정을 앞에 몰아 뒀다** — 실패율을 모르는 채 구조(카카오 로컬 API·좌표 서버
저장)를 바꾸지 않기 위해서다.

## 0-23. 2026-08-15 — 지갑/과금 세션 인계 + 테스터 배포 완료

지갑/과금 세션이 닫히며 남긴 인계. **그 세션은 기록을 `feat/ai-credit-wallet`
브랜치에 남겼는데, 그 브랜치는 병합하지 않는 것이 방침이라 main에서는 보이지
않는다.** 그래서 PM이 여기 옮겨 적는다 — 브랜치가 잊히면 인계도 함께 잊힌다.

### ✅ 테스터 배포 완료 (2026-08-15)

**Android 릴리스 APK를 `eda28a2` 기준으로 빌드해 사용자에게 전달했고, 사용자가
App Distribution·iOS Xcode로 배포를 마쳤다.** 오늘 들어간 F-04·F-09·F-11·F-15와
메모 가짜 문구 제거가 테스터에게 나갔다.

⚠️ **그 빌드에 없는 코드가 하나 있다** — 자동 삽입 메모 정리 도구의 사각지대
수정(`e4b21b8`, 추가 226)이 `eda28a2` **뒤에** 들어갔다. 관리자 전용 도구라
테스터 화면에는 영향이 없고, 어차피 테스터는 데이터를 초기화한다. 다음 빌드에
자연히 포함된다.

✅ **확인된 것**: 사진 개선 동의 토글을 플래그 뒤로 숨긴 수정(#174)은
`eda28a2`에 **포함돼 있다.** 테스터가 "켜면 실패하는 토글"을 보지 않는다.

### 지갑/과금 세션이 한 일

1. **마케팅** — 실행 계획·자산 6종·워드 보고서·상품성 판단·4주 리텐션 검증
   플랜. main에 있다(#171 `docs/marketing/`, `docs/planning/`).
2. **과금 확정 + 지갑 엔진** — 충전형 E2·체험 10회·리퍼럴/최초충전.
   U1·U2·U4·U5·U7 뼈대·파일럿 계측 구현. 브랜치 **`feat/ai-credit-wallet`
   (HEAD `4b64289`)에서 완성·검증**(functions 97 · rules 75 · flutter 358 ·
   analyze 0). **reset 모드에서 몰래 지급되던 버그를 고쳐** "잠든 채 배포"가
   실제로 성립하게 했다. 관리자 하드닝·F-07을 보존한 채 rebase.
   ⚠️ **병합하지 않았다**(사용자 확정 보류, CLAUDE.md 6절).
3. **테스터 빌드** — 위 참고.
4. **F-10** — 방향 확정(A+C 재연락 루프)·스펙 작성 → main(`1b87c56`, 추가 227).
   구현은 F 담당 세션(`connection-trace-ai-v2-flutter-52`) 몫.

### 지갑 쪽 남은 일 (전부 사용자/타 세션 게이트)

| 무엇 | 비고 |
|---|---|
| **U6 리퍼럴 redemption** | **미구현** — 지갑에서 유일하게 안 만든 조각 |
| U5 실기기 테스트 | 기기가드(`deviceLedger`) 실물 확인 |
| 과금 전체 테스트 | CLAUDE.md 6절 최상위 지침 — 스토어 출시 **전** 필수 |
| `firebase deploy` · 플래그(`model=wallet`) · 스토어 상품 등록 | **사용자 최종 결정** |
| iOS 403 | 사용자 Apple 계정 역할 문제 |
| 리텐션 파일럿 판정 | 테스터 피드백이 와야 시작. 마케팅 GO 여부가 여기 달렸다 |

### 다음 세션이 헷갈리지 말 것

- **지갑 코드는 main에 병합하지 않는다.** 브랜치 보존이 현행 방침이다.
- **배포·플래그 전환·스토어 등록은 사용자 최종 결정.**
- 문서 등 저위험 변경은 각 세션 자율 병합. **지갑 코드만 예외.**

### 이 시점의 세션 구성

관리자 개발팀·저장소 정리·지갑/과금 세션이 모두 닫혔다. 남은 것은
**PM 세션**과 **F 담당 세션(`…-52`)** 둘이다. 조율에 필요한 상태는
[`RESUME.md`](../RESUME.md)에 있다.

## 0-22. 2026-08-15 — 저장소 정리 세션: 갇힌 기록 회수 + 테스터 초기화 방침

세 세션이 같은 저장소에서 동시에 돌던 상황을 정리한 세션. 코드보다 **기록이
브랜치에 갇혀 다음 사람에게 안 보이는 문제**를 주로 다뤘다. 시간순 상세는
backlog **추가 222·224·226**.

### 발단 — 작업 트리가 낡은 브랜치에 있었다

본체 워크트리가 `feat/ocr-phone-labels`에 있었는데 그 브랜치는 **origin/main보다
115커밋 뒤처지고 73커밋 앞선** 상태였다. 확인해 보니 **73개 중 69개는 이미
main에 있었다** — 다른 세션이 3분할 PR로 병합했다. 실제 미반영은 문서 4건뿐인데,
그 문서가 낡은 브랜치에 갇혀 있었다.

⚠️ **모르고 그 워크트리에서 작업을 시작하면 낡은 코드를 고치게 된다.** 세션을
시작할 때 `git rev-list --left-right --count origin/main...HEAD`로 먼저 재 볼 것.

### 병합한 것 (PR 5건)

| PR | 내용 |
|---|---|
| #168 | 갇혀 있던 기록 이관(추가 222·224) + HANDOFF 첫 줄 + `.gitignore` + `-admin` 워크트리 제거 |
| #171 | 지갑 세션 문서 12건 — 지갑 **코드**는 병합하지 않고 문서만 분리 |
| #176 | 자동 삽입 메모 정리 도구의 사각지대 수정(추가 226) |
| #177 | 테스터 데이터 정리 미결 종결 — A안 |
| #178 | 초기화 안내에 사진 경고 |

### ⚠️ 테스터 초기화 — 다음 배포 때 그대로 쓸 것

사용자 결정: **테스터에게 "초기화했다"고 안내하고 처음부터 다시 등록하게 한다.**
지우는 주체는 **테스터 본인**(지갑 → 선택 → 전체선택 → 삭제, F-06). 새 코드가
없고 계정·로그인이 유지된다. 안내 문구는 backlog 추가 226에 그대로 쓸 수 있게
적어 뒀다 — **명함 사진이 함께 사라지고 복원되지 않는다는 경고를 반드시 포함**할
것(사진 서버 저장은 방침 v2.2 게시 전까지 꺼져 있다).

서버에서 일괄 처리하는 안 두 가지를 검토했다가 접었고, **접은 이유와 조사
결과를 추가 226에 남겼다.** 특히 이건 알아 둘 것:

> 동기화가 tombstone 기반이라 **서버 문서만 지우면 기기의 로컬 명함이 도로
> 올라온다.** "서버에서 지우면 끝"이라는 직관이 이 구조에서는 틀린다.

### 이 세션에서 확인한 사실 두 가지

1. **서버는 이미 명함 내용을 평문으로 받는다** — AI 브리핑이 `contactSummary`에
   `메모: …`를 담아 Cloud Function으로 보낸다(`ai_briefing_service.dart:173`).
   방침 v2.2 문안은 이 사실을 이미 담고 있어 고칠 것은 없지만, **"서버는 내용을
   못 본다"를 전제로 새 문안을 쓰면 어긋난다.**
2. **저장된 명함은 AES-256-GCM으로 암호화돼 있고**(`nonce 12B + ciphertext +
   MAC 16B`, base64) 키는 `users/{uid}.encryptionKeyB64`에 있다. 서버에서
   복호화·수정·재암호화가 가능하고, `updatedAt`을 올리면 동기화로 기기까지
   전파된다 — 나중에 정말 필요해질 때의 착수 자료(추가 226).

### 지갑은 병합하지 않는다 (재확인)

`feat/ai-credit-wallet`은 **브랜치에 보존**한다. 2026-08-15 사용자 재확정.
CLAUDE.md 6절은 **현행 지침**이며 낡은 문구가 아니다 — 그 지침을 "파킹 해제를
반영 못 한 잔재"로 보고 고치려는 시도가 실제로 있었으므로, 고치기 전에 커밋
`323de1a`(2026-08-14 22:49)의 취지를 먼저 확인할 것.

### 📦 다음 테스터 배포 — 순서대로 할 일

안내문은 [`tester-guide.md`](../tester-guide.md)에 **이번 배포 기준으로 갱신해
뒀다**(초기화 안내 + "이번 배포에서 달라진 것"). 그 문서를 그대로 보내면 된다.
맨 아래 "배포하는 사람이 확인할 것" 절만 테스터에게 보내지 않는다.

**⚠️ 배포 게이트 두 가지가 먼저다. 둘 다 사용자 승인 사항.**

| # | 게이트 | 왜 |
|---|---|---|
| G1 | **실기기 전체 테스트** | 명함 필드추가로 **백업 구조(`toBackupJson`)가 바뀌었다.** 다기기 동기화·복원이 깨지면 테스터 데이터가 유실된다. 절차는 0-2 참고 |
| G2 | **방침 게시를 코드 배포와 동시에** | 직통·팩스·웹사이트를 새로 저장하므로 `privacy-policy.html` v2.1이 함께 나가야 한다. 게시 시 **헤더 버전·시행일·고지 기간 확정** 필요(초안 상태). v2.2(명함 사진·법정 보존)도 같은 게시 건이다 |

게이트를 통과하면 배포 순서:

1. **`tester-guide.md`의 앱 버전 예시 값**(`b295e63`)을 이번 빌드 해시로 교체.
   제보에서 어느 빌드인지 가르는 유일한 값이다.
2. **`tool/build_app.sh apk release`** — `flutter build`를 직접 쓰지 말 것
   (커밋 해시가 안 박힌다). ⚠️ **debug 빌드로 배포하면 로그인 화면에
   "로그인 건너뛰기"가 그대로 보인다.**
3. Firebase App Distribution으로 배포(무료).
4. **관리자 콘솔에 테스터 로그인 이메일 등록**
   (https://connection-sense-admin.web.app → 테스터 관리). 빠뜨리면 App Check에
   막혀 **AI가 전부 실패**한다. **초대 메일 주소와 실제 로그인 주소가 다를 수
   있다.**
5. 안내문 발송.

**다음 배포 때 `tester-guide.md`에서 지울 것**(안 지우면 사고가 난다):

- **"먼저 해 주실 일"(초기화) 절** — 이번 배포에만 해당한다. 남겨 두면 테스터가
  매번 명함을 지운다.
- **"이번 배포에서 달라진 것"** — 갈아 끼운다. 뽑는 명령은 그 문서 갱신 규칙에
  적어 뒀다.

### 남은 것

- **테스터 기기의 옛 메모**는 정리하지 않았다 — 위 초기화로 함께 사라진다.
  서버측 일괄 처리 안을 두 가지 검토했다가 접었고, 조사 결과와 접은 이유는
  backlog 추가 226에 있다(나중에 정말 필요해지면 착수 자료).
- **F-15 묶음 머리글은 실기기 미확인** — 지금 데이터는 주소가 전부 달라 묶일
  짝이 없다. 확인하려고 가짜 명함을 만들지 않았다(CLAUDE.md 4절). 테스터
  재등록 후 같은 건물 명함 두 장이 들어오면 그때 보인다.
- **테스트가 끝난 뒤 되돌릴 것**은 0-9의 "⚠️ 테스트 종료 후 되돌릴 것"과
  `tester-guide.md` 맨 아래에 있다 — `DAILY_LIMIT` 20→10(P0-11),
  `enforceAppCheck: true` 복원, `config/testers` 비우기. **허용목록은 App Check
  우회 권한이라 열어 둔 채 잊으면 안 된다.**

### 📌 확인이 필요한 낡은 기록 하나

CLAUDE.md 6절에 **"⚠️ Android 릴리스가 아직 debug 키스토어로 서명돼 있다
(P1-19)"**가 남아 있는데, 이 기기에는 `android/key.properties`가 있고
(2026-08-09 생성, 0-8의 서명 키 작업 시점과 맞는다) `build.gradle.kts`는 그
파일이 있으면 업로드 키로 서명한다. **즉 이 기기에서 만든 릴리스는 debug 키가
아니다.**

다만 `key.properties`는 저장소 밖 파일이라(`.gitignore`) **다른 기기에서는
없을 수 있고, 그러면 debug 키로 폴백한다.** CLAUDE.md 문구를 지울지, "빌드
기기에 key.properties가 있어야 한다"로 고칠지는 **사용자 판단이라 손대지
않았다.** 스토어 업로드 전에 정리할 것.

## 0-21. 2026-08-15 — PM 세션 마무리: 결정 5건·F 6건·방침 개정안

이 세션은 "진행 상황 점검"으로 시작해 **막혀 있던 결정을 풀고, 그 결정이 요구한
코드와 문서를 만든 뒤 전부 병합**하는 데까지 갔다. 시간순 상세는 backlog
**추가 218·220·221·223·225**.

### 확정된 사용자 결정 (전부 문서에 반영됨)

| 결정 | 내용 |
|---|---|
| 명함 사진 서버 저장 | **저장한다.** 목적은 계약 이행(저장·복원·**본인 명함 보정**), 인식 알고리즘 개선만 별도 동의 |
| 탈퇴자 CS 보존 | **3층 설계 전부 채택** — 결제 원장 5년 / 문의 3년 분리 보관 / AI 로그는 파기 + uid 없는 집계 |
| 지갑형 무료분 이월 | 예외 없이 전원 이월(2026-08-12 결정 재확인) |
| 베타 관측 4건 | 권장안 그대로 |
| P0-11 (AI 한도 20→10) | "테스트 종료 후 필수" → **"최종 테스터 테스트 후 결정"** |

### 만든 것

- **개인정보처리방침 v2.2 초안**(+ 약관·계정삭제 안내·앱 권한 안내). ⚠️ **게시 전.**
- **법무 검토 요청서** — [`docs/legal/legal-review-request-2026-08-15.md`](../legal-review-request-2026-08-15.md).
  질문 5개(고지 기간, IAP 보존 주체, 제3자 이미지 처리 근거, 잔여 이용권,
  기기 해시 보존)에 각각 *사실관계·우리 판단·확신이 없는 이유·답에 따라
  달라지는 것*을 붙였다.
- **CS 보존 설계 스펙** — [`cs-retention-spec-2026-08-15.md`](../cs-retention-spec-2026-08-15.md). 구현 미착수.
- **명함 사진 서버 저장** — 암호문을 그대로 올린다. ⚠️ 플래그로 꺼 둠.
- **F 6건**: F-04·F-09·F-11·F-15 구현, F-05·F-07 확인 종결.
- **관리자 정리 도구 1개** — 메모에 박혀 있던 자동 문구 정리.

### ⚠️ 지금 "꺼져 있는" 것들 — 켜는 순서가 정해져 있다

```
법무 회신 → 방침 v2.2 게시 + 고지 기간 경과
   ↓
firestore.rules·storage.rules 배포
   ↓
functions 배포 (firebase deploy --only functions)   ✅ 2026-08-15 완료
   ↓
kCardPhotoBackupEnabled = true
   ↓
사진 업로드·복원 동작 + 설정에 동의 토글 나타남
```

이 순서를 건너뛰면 **고지 없는 수집**이 된다. `test/card_photo_backup_gate_test.dart`가
플래그를 고정하고 있어, 켜려면 그 테스트를 함께 고쳐야 한다(실수 방지 장치).

⚠️ **functions 배포 단계가 왜 새로 끼었나**(2026-08-15): 탈퇴해도 서버의 명함
사진이 안 지워지는 결함이 있었다. 앱이 스스로 지우려는 호출이 **계정을 지운
뒤에** 일어나 `request.auth`가 null이 되고 `storage.rules`가 거부한다. 그래서
`onUserDeletedCleanup`이 Admin SDK로 지우도록 고쳤는데, **그건 배포돼야
동작한다.** 배포 없이 플래그부터 켜면 그 사이에 탈퇴한 사람의 사진이 영영
남는다 — 방침이 약속한 파기가 안 되는 상태다. 코드는 준비됐고 배포만 남았다
(`functions/src/cardPhotoCleanup.ts`).

### ⚠️ 워크트리 배정 (2026-08-16) — 세션마다 디렉터리를 나눈다

여러 세션이 **한 작업 디렉터리를 공유하면 브랜치가 통째로 바뀐다.** 파일
단위 조정만으로는 못 막는다.

| 워크트리 | 담당 |
|---|---|
| `…-flutter` (본체) | **지도 세션** (`feat/map-anchor-gesture`) |
| `…-flutter-feature` | **손익/원가 세션** (`docs/storage-cost-pnl`) |
| `…-flutter-docs` | **PM** |
| `…-flutter-wallet` | 지갑 브랜치 보존 — **아무도 건드리지 않는다** |
| `sharp-chebyshev-…` | 캐시 평문 정리 세션 |

**왜 생겼나**: 2026-08-16, 손익 세션이 본체에서 작업하는 동안 지도 세션이 같은
디렉터리에서 `feat/map-anchor-gesture`로 갈아탔다. **손익 세션 발밑에서 브랜치가
바뀌었다.** CLAUDE.md 4절이 경고한 그대로다(*"QA가 검증하던 커밋이 발밑에서
바뀐다"*).

⚠️ **커밋은 푸시하기 전까지 안전하지 않다.** 그날 손익 세션의 커밋 5개가 산
이유는 워크트리 분리가 아니라 **푸시해 뒀기 때문**이다. 분리가 되기 전까지는
그게 유일한 안전망이고, 분리한 뒤에도 비용이 거의 없다.

📌 이 배정을 CLAUDE.md 4-2절에 규칙으로 넣는 것은 **사용자 승인 대기 중**이다.

### 남은 일 — 누가 무엇을 하나

| 무엇 | 누구 | 상태 |
|---|---|---|
| ~~법무 검토 의뢰~~ | — | ✅ **완료**(2026-08-15). 회신·대조표 15건까지 받았다 |
| ~~방침 v2.2 게시~~ | — | ✅ **게시 완료(2026-08-16)** — 공고 8/16 / **시행 8/23**. 방침 2.2 · 약관 1.1. 실물 4페이지 확인 |
| **rules 배포** | **사용자 승인** | 🔴 **다음 차례.** 사진 동의 필드·암호문 경로 미배포. `--only firestore:rules,storage` |
| **사진 백업 플래그 켜기** | **사용자 승인** | ⏳ **시행일 2026-08-23 이후**, 목표 **8월 말**. ✅ 축소 구현 완료 · ✅ 한도 확정(**무료 200장 / 충전 2,000장**) · ✅ Storage(서울)+규칙 배포. **남은 것: 한도 구현 · 개별 삭제 재시도 · 수명 주기 규칙**. `card_photo_backup_gate_test.dart`도 함께 고쳐야 한다 |
| 테스터 고지 | **사용자 판단** | 게시만으로 고지는 성립하나, 실사용 중이라 앱 공지·메일 병행을 권한다 |
| ~~CS 보존 3층 구현~~ | — | ⬜ **보류.** 층2(문의 이관)는 유료 결제 전에는 불필요로 확정(2026-08-15). **결제 도입 시 재개** |
| **F 나머지 1건** | 미정 | ✅ F-13·F-14 종결(추가 241) · ✅ F-01 종결(추가 244) — **미착수 0건**. 남은 것은 **F-03(손으로 자르기)뿐이고 조건부 보류**다: *자동 크롭 실패율을 재 본 뒤* 필요 여부를 판단하기로 했고 **아직 아무도 재고 있지 않다**. 제스처·빠른 동작 규칙은 [`gesture-quick-action-rules.md`](../gesture-quick-action-rules.md) |
| ~~테스터 APK 빌드~~ | — | ✅ 배포 완료(추가 229). **그 세션은 없다** |
| 관리자 보안 실물 확인 | **사용자 / 전체 테스트** | 🟡 **브라우저 5건 완료**(2026-08-15, 011에서 결함 발견·수정). 나머지 3건(007·002·F-07)은 **배포 직전 전체 테스트**로 |
| **동기화 지원 로그 구현** | 미정 | 스펙·법무 검토 완료([spec](../sync-support-log-spec-2026-08-15.md)). ⚠️ **기기 식별자가 지갑 브랜치에만 있어** 그 병합 시점에 묶인다 |

### 이 세션에서 반복해서 확인된 것

1. **"의도한 실패"도 사용자 눈에는 결함이다.** rules 미배포 상태에서 동의
   토글이 보이면 테스터에게는 고장난 기능이다 — 게이트를 닫아 둔 기능은
   화면에서도 닫아야 한다.
2. **회귀 방지 테스트는 검사 지점이 하나면 부족하다.** E-08 뒤에 만든 검사가
   생성자 기본값만 보고 있어, 같은 유형의 결함(메모 자동 문구)이 대입 경로로
   그대로 들어왔다.
3. **눈으로 못 보는 결함은 테스트로만 잡힌다.** F-15 머리글의 스크린리더 중복
   읽기는 화면상 완전히 정상이었다.
4. **문서 사이 상태가 어긋나면 이미 답한 질문을 다시 묻게 된다.** 지갑 무료분
   이월은 스펙에 확정으로 적혀 있었는데 HANDOFF가 낡아 미결로 보였다.

## 0-20. 2026-08-15 — 세션 소멸 후 인수인계 정리

관리자 개발팀 세션이 작업 도중 사라졌다. **그 세션이 남긴 것과 다른 워크트리
상태를 전수 점검한 결과**를 여기 적는다 — 세션 간 구두 전달로만 오가던 내용이
사라지지 않도록 문서로 고정한다.

### 점검 결과 — 유실된 작업은 없다. 단 한 곳에 미커밋 작업이 있다

| 워크트리 | 브랜치 | 상태 |
|---|---|---|
| `…-flutter` (주) | `feat/ocr-phone-labels` | 깨끗. ⚠️ **main보다 한참 뒤처진 옛 브랜치** — 여기서 새 작업 시작 금지 |
| `…-flutter-admin` | `docs/fix-handoff-0-18-dup` | 깨끗, 병합 완료. **관리자팀 작업은 전부 커밋·병합됐다** |
| `…-flutter-docs` | `feat/card-photo-backup` | 깨끗 |
| **`…-flutter-wallet`** | `feat/ai-credit-wallet` | ⚠️ **미커밋 작업 있음 — 아래 참고** |
| `.claude/worktrees/f02-address-optional` | 문서용 | 깨끗 |

### ⚠️ 지갑 워크트리의 미커밋 작업 — 백업해 뒀다

`…-flutter-wallet`에 **커밋되지 않은 변경 331줄 + 추적되지 않는 신규 파일 6개**가
있었다. 이 저장소는 워크트리 정리 중 미커밋 파일이 사라질 뻔한 전례가 있다
(추가 176). 그래서 **손대지 않고 저장소 밖에 복사해 뒀다.**

**커밋하지 않은 것이 맞았다** — 확인해 보니 **지갑/과금 세션이 살아 있고 그
작업이 진행 중**(U5 기기가드 `deviceLedger`)이었다. 다른 세션이 작업하는 파일을
건드리면 검증하던 코드가 발밑에서 바뀐다(CLAUDE.md 4절).

✅ **그 뒤 지갑 세션이 전부 커밋했다**(2026-08-15 확인). 백업했던 6개 파일이
`origin/feat/ai-credit-wallet`에 모두 들어간 것을 확인하고,
`~/Claude/_wip-backup-2026-08-15/`는 **삭제했다** — 원본이
안전해진 뒤에도 사본을 남겨 두면 어느 쪽이 최신인지 헷갈린다.

📌 **다음에 같은 상황이 오면**: 다른 세션의 미커밋 작업을 발견하면 **손대지 말고
저장소 밖에 복사**한 뒤, 그 세션이 커밋한 것을 확인하고 사본을 지운다. 이
저장소는 워크트리 정리 중 미커밋 파일이 사라질 뻔한 전례가 있다(추가 176).

`origin/feat/ai-credit-wallet`은 **커밋 8개가 미반영**이다(U1·U2·U4·U7 등).
푸시는 돼 있으니 브랜치 자체는 안전하다. 이 브랜치는 **main 최후 병합** 예정이며,
`deploy`와 플래그 전환은 **사용자가 직접 한다**(과금 관문, CLAUDE.md 최상위 지침).

### ⚠️ `onUserDeletedCleanup` — 세 갈래가 한 함수에서 만난다

이 함수 하나에 서로 다른 세 요구가 걸려 있다. **따로 넣으면 마지막에 넣는 쪽이
앞의 것을 덮어쓴다.**

| 출처 | 요구 |
|---|---|
| 추가 220 (사진) | 탈퇴 시 **Storage의 명함 사진 삭제** |
| CS 보존 층 2 | 문의를 삭제하지 말고 `archivedInquiries`로 **이관**(3년 보존) |
| 지갑 U5 | **`deviceLedger`(기기 해시)는 지우면 안 된다** — 탈퇴·재가입으로 무료 지급 가드가 뚫린다 |

**병합 순서(2026-08-15 재조율 확정)**

1. **지갑 먼저** — `deviceLedger` 보존만 들어간다. 사용자가 테스터 배포를
   우선해서 순서를 바꿨다(당초 계획은 지갑이 마지막이었다).
2. **PR #164(사진)** — ⚠️ **#164는 `functions/`를 건드리지 않는다.** diff로
   확인했다(바뀌는 것은 rules 2개·앱 코드·테스트뿐). 탈퇴 시 Storage 사진
   삭제는 **아직 구현하지 않은 후속 과제**다. 즉 **이 단계에서 이 함수의
   충돌은 없다.**
3. **Storage 사진 삭제 + CS 문의 이관** — 나중에 이 함수에 넣을 때
   **지갑의 `deviceLedger` 보존 위에 얹는다. 덮어쓰지 말 것.**

`firestore.rules`에서만 실제로 겹친다 — 지갑은 `creditGrants`·`deviceLedger`
**새 match 블록**, #164는 `clientWritableUserFields()`에 **2줄 추가**라
구역이 달라 공존한다. 지갑이 먼저 병합되면 **#164를 새 main 위로 올릴 때
지갑 rules를 보존**한다.

⚠️ **지갑 U5는 법무 검토 대상이기도 하다** — 기기 식별 해시를 탈퇴 후에도
보관하는 것이라, 어뷰징 방지 목적의 보존이 적법한지·방침에 고지가 필요한지
판단이 필요하다. [`legal-review-request-2026-08-15.md`](../legal-review-request-2026-08-15.md)
질문 5번.

### 남은 일 — 관리자팀이 넘긴 것

**(1) 배포 후 실물 확인 8건 → 브라우저 5건 2026-08-15 완료.** 나머지 3건은
실기기·실서버가 필요해 **배포 직전 전체 테스트로 넘긴다.**

| 항목 | 무엇을 확인 | 결과 (2026-08-15) |
|---|---|---|
| 011 | CSP 하 관리자 로그인·Firestore·Functions 정상 동작 | ⚠️→✅ **결함 발견·수정·배포 후 통과.** 아래 참고 |
| 010 | 관리자 조작 후 감사 기록 남는지 | ✅ **통과** — `adminAuditLogs`를 서버에서 직접 조회해 `billing.save` 기록 확인 |
| 012 | 로그아웃 후 재접속 시 자동복원 없음 | ✅ **통과** — 로그아웃 후 재접속 시 로그인 화면 |
| 004 | 충전 설정 저장 시 XSS 미실행 | ⚠️ **확인 불가.** 입력칸이 `type="number"`라 화면에서 스크립트를 넣을 방법이 없다. 실제 위험은 저장된 값을 렌더할 때인데 UI로는 재현되지 않는다 |
| 006 | 문의 목록 페이지네이션 | ⚠️ **확인 불가.** 문의가 **7건**이라 `INQUIRY_PAGE_SIZE = 50`을 못 넘어 "더 보기"가 나오지 않는다. **문의가 50건을 넘으면 자연히 확인된다** |
| 007 | (실기기) 탈퇴 → 문의 0건 | ⬜ 전체 테스트로 |
| 002 | (실서버) 크레딧 지급 멱등성 | ⬜ 전체 테스트로 |
| F-07 | (실기기) 재생성마다 다른 대화 포인트 | ⬜ 전체 테스트로 |

⚠️ **004·006을 "확인함"으로 적지 않는다.** 못 본 것을 봤다고 적으면 배포
직전에 한꺼번에 터진다. 006은 가짜 문의를 넣어 억지로 만들 수도 있었지만,
**테스터가 쓰는 실서버**라 하지 않았다(사용자 판단, 2026-08-15).

### 011에서 나온 결함 — 관리자 콘솔이 잠겨 있었다

`ADMIN-VULN-011`로 건 CSP의 `script-src`가 `'self'`와 `gstatic.com`뿐이라,
Firebase Auth 팝업 로그인이 부모 문서에서 불러오는
`https://apis.google.com/js/api.js`가 **차단**됐다. 그러면 로그인 결과를 받지
못하고 `auth/internal-error`가 난다.

**관리자 계정 두 개가 모두 비밀번호 없는 구글 전용 계정**이라
(`admin.js` 주석), 로그아웃하면 **다시 들어갈 방법이 없었다.** 실제로 그
상태가 됐다가 CSP를 고쳐 풀었다(`#197`, `--only hosting:admin` 배포).

**이메일/비밀번호 로그인은 그 스크립트를 쓰지 않아 멀쩡했다.** 그래서 헤더를
배포한 뒤에도 드러나지 않았다 — **보안 설정은 조이는 순간이 아니라 조인 뒤
실제로 써 봐야 안다.**

⚠️ **`globe2030@icloud.com`은 관리자가 아니다.** 애플 ID라 구글 로그인이
불가능하고, `firestore.rules`·`adminEmails.ts` 어디에도 등록된 적이 없다.
로그인 화면을 지나가도 데이터를 못 읽는다.

### ✅ 재발 방지 — 관리자 콘솔 출입구를 둘로 만들었다 (2026-08-15)

**`connectionsense@creamhouse.net`에 비밀번호를 붙였다**(Firebase 콘솔 →
Authentication → 비밀번호 재설정 → 메일 링크에서 설정, 사용자가 직접 수행).

| 출입구 | 상태 |
|---|---|
| 구글 계정 로그인 | 기존 그대로 |
| **이메일·비밀번호 로그인** | **신설** — 구글 로그인이 막혀도 들어갈 수 있다 |

**코드·규칙 변경 없음.** 그 주소는 이미 `ADMIN_EMAILS`와 `firestore.rules`에
있고 이메일 인증도 끝나 있어, 계정에 비밀번호 자격증명만 추가한 것이다.

**왜 필요했나**: 관리자 계정 둘 다 구글 전용이라 **출입구가 하나뿐**이었다.
그 하나가 막히자 아무도 못 들어갔다. 출입구가 둘이면 같은 일이 나도 잠기지
않는다.

⚠️ **비밀번호 경로가 생긴 만큼 위험도 늘었다.** 구글 계정은 2단계 인증 등이
붙지만 비밀번호는 그렇지 않다. **이 계정의 비밀번호는 저장소·문서·세션 어디에도
적지 않는다.**

**(2) `functions/src/index.ts`의 `onUserDeletedCleanup`에 후속 둘.** 같은 함수라
**함께 넣는 것이 맞다.**
- 탈퇴 시 **Storage의 명함 사진** 삭제(추가 220). 지금은 클라이언트가 지우는
  구조라 탈퇴 도중 앱이 죽으면 서버에 남는다.
- **CS 보존 층 2** — 문의를 삭제하는 대신 `archivedInquiries`로 이관
  ([`cs-retention-spec-2026-08-15.md`](../cs-retention-spec-2026-08-15.md)).

**(3) 배포 대기 중인 규칙 변경 2건**(PR #164에 포함, 아직 미배포)
- `firestore.rules` — `clientWritableUserFields()`에
  `photoImprovementConsent`·`photoImprovementConsentAt` 추가.
  ⚠️ **배포 전이라 지금 앱에서 사진 개선 동의 토글을 켜면 거부된다.** 실기기로
  확인했고 **의도한 동작**이다(추가 220).
- `storage.rules` — 암호문 경로 `users/{uid}/cards/{file}` 추가.

### ⚠️ 배포할 때 반드시 지킬 것

**`firebase deploy --only hosting`처럼 타겟 없이 배포하면 `legal`과 `admin`이
둘 다 나간다.** `docs/legal`에는 **아직 게시하면 안 되는 개인정보처리방침 v2.2
초안**이 들어갈 예정이므로, 관리자 콘솔만 배포할 때는 반드시
**`--only hosting:admin`** 으로 좁힌다.

### 열린 PR 2개 (둘 다 게시·배포 전)

| PR | 내용 | 게이트 |
|---|---|---|
| #162 | 개인정보처리방침 v2.2 초안 + 약관·계정삭제 안내·앱 권한 안내 | **법무 검토 → 고지 기간 → 게시** |
| #164 | 명함 사진 서버 저장 + 인식 개선 동의 토글(추가 220) | 방침 게시 후 플래그 켜기 |

## 0-19. 2026-08-15 — 관리자 보안 감사 12건 전부 수정·배포 완료

2026-08-13 관리자 전용 감사(취약점 12건)를 **전 항목 수정·병합·배포**했다.
시간순은 backlog 추가 216(즉시급 4)·217(중간 A 4)·218(배포 기록)·219(중간 B 4
+ 낮음 012 + 배포 완료). 상세는
[`admin-security-hardening-2026-08-14.md`](../admin-security-hardening-2026-08-14.md).

- **12건 분류**: 즉시급 4(001 관리자 판별 단일화·002 크레딧 상한/멱등/감사·
  003 강제업데이트 URL allowlist·법적문서 편집기 반영단절 차단) + 중간 8(004
  billing XSS·005 문의 답변 위조·006 문의 DoS·007 탈퇴 시 문의 삭제·008 내부
  보고서 비공개화·009 OCR 통계 오염·010 관리자 변경 감사·011 CSP/보안헤더) +
  낮음 1(012 로그아웃 세션·DOM 잔존). **전부 main 반영.**
- **배포 완료**(main `e6ef7aa` 기준): `firestore:rules` + `functions`(5개) +
  `hosting:admin`. curl 확인 — 손익보고서 URL 404, 관리자 콘솔 CSP/X-Frame-
  Options 등 헤더 적용, 012 세션 변경 반영, 콘솔 200.
- **과금 관문 준수**: 지갑/billing 모델은 파킹 상태라 이번 배포에 없다. AI
  사용량·크레딧 활성화를 건드리지 않았다.
- **인터림·후속으로 남긴 것**: 001은 진짜 단일원본(`config/admins`+Rules
  `get()`) 대신 드리프트 감지(`tool/check_admin_sync.py`, 3소스). 010은
  `adminAuditLogs` append-only이나 대상 쓰기와 원자적이지 않음(완전 Callable
  통합은 후속). 006 진짜 rate limit은 App Check/Callable 필요.
- **아직 실물 확인 필요(부분 테스트, 배포됨)**: 011 CSP 하 관리자 로그인·조회,
  004 XSS 미실행, 006 문의 페이지네이션, 010 콘솔 조작 감사 기록, 012 재접속
  자동복원 없음 → 모두 https://connection-sense-admin.web.app 에서. 007 탈퇴→
  문의 0건은 실기기. 001 해제→거부·002 크레딧 멱등성은 서버 시나리오.
- ⚠️ **007이 라이브라 탈퇴 시 1:1 문의가 영구 삭제된다.** 테스터가 탈퇴·재가입을
  반복 중이라 중요한 제보는 탈퇴 전에 처리하도록 안내하기로 함(사용자 결정).
  삭제→보관 이관(archivedInquiries)은 방침·법무 게이트로 후속(`cs-retention-spec`).

## 0-18. 2026-08-14 저녁 — F-07 재생성 다양성 구현 + 64커밋 3분할 PR

상세 시간순은 backlog **추가 214·215**. 이 세션은 코드 하나(F-07)와 정리 작업
(PR 3분할·테스터 피드백 워드 문서)을 했다. **여러 세션이 동시에** 같은 저장소를
worktree로 나눠 작업했고, PM 세션이 병합 순서를 조율했다.

### 한 일

1. **F-07 대화 포인트 재생성 다양성 — 구현·병합 완료(배포 대기).** "새로 생성"을
   눌러도 매번 비슷한 문제. 원인은 서버(`generateBriefing`)가 동일 페이로드에
   동일 프롬프트를 만들고 `generationConfig`에 temperature/topP가 없던 것.
   해결 두 축: **previousPoints 제외**(클라가 직전 포인트를 넘기면 서버가 피해서
   생성) + **temperature=1.0/topP=0.95 + 회차 시드**. PR #147 병합(관리자 #149
   위에 rebase 후). 검증: tsc·analyze·test 통과. 상세는 추가 214.
2. **`feat/ocr-phone-labels` 64커밋을 기능영역별 3PR로 분할** — #143(SNS문서)·
   #144(관리자 문의)·#145(OCR·촬영·테스터 조치), 셋 다 병합됨. #145는 isAdmin
   의존으로 #144에 스택. 합집합=원본 트리 검증. #142·#141 close, #137 유지.
   명함인식팀 OCR 3커밋(89→94%)을 #145에 cherry-pick. 상세는 추가 215.
3. **테스터 피드백(통합본 38건) 조치결과 워드 문서**(조치=빨간 글씨) 사용자 전달.

### 남은 일 + 처리방안

| 남은 일 | 처리방안 | 게이트 |
|---|---|---|
| **F-07 서버 배포** | 관리자·지갑팀의 `functions` 변경과 묶어 `firebase deploy --only functions`. 배포돼야 다양성이 실제 동작 | **사용자**(Blaze 배포) |
| **F-07 배포 후 눈확인** | 배포 후 "재생성마다 다른 포인트가 나오는지" 부분 테스트 | 배포 선행 |
| **F-07 방침 확인** | previousPoints는 기존 전송 데이터의 파생물이라 신규 수집 아님 → 방침 개정 불요로 판단. 사용자 최종 확인만 | 사용자 |
| **지갑팀 rebase** | F-07이 main에 들어갔으니 지갑팀은 갱신 main을 받으면 됨(스택 해소) | 지갑팀 |
| **`functions/index.ts` 동시 편집** | 관리자→F-07(완료)→지갑 순서로 PM이 조율 중. 새 트랙은 이 구역 겹침 정리 후 배정 | PM 조율 |

그 외 남은 큰 덩어리(변동 없음): 과금 IAP 구현(P1-2·4·5), OCR 정답지 축적(R-07),
기능개선 F 나머지 14건, P0-11(테스트 종료 후 AI 한도 20→10 복원).

## 0-17. 2026-08-14 — 인식 정확도 실측 전환 + 테스터 오류 12건 종결

**브랜치**: `feat/ocr-phone-labels` (푸시 완료, **PR 미생성 — 커밋 35개**).
자동 테스트 **328건** 통과, `flutter analyze` info 19건·error/warning 0.

### 이 세션에서 한 일

**1. 명함 인식 — "읽고도 잃는" 경로를 막았다 (추가 199~202)**

사용자 진단이 출발점이었다: *"OCR 추출은 정확히 하고 있는데 분리하는 과정에서
잘못되는구나."* 맞았다 — **이름 미검출 17장 중 16장은 원문에 이름이 있었다.**

| 칸 | 전 | 후 |
|---|---|---|
| 이름 | 82 | **100** |
| 주소 | 76 | **85** |
| 휴대폰 | 97 | **100** |
| 사무실 | 78 | **79** |

원인은 하나의 모양이었다 — **자리를 뺏기면 끝**. 이름 후보를 거르는 검사가 줄
순회가 끝난 뒤에만 돌아서, 슬로건 조각이 먼저 자리를 차지했다가 지워지면 진짜
이름을 다시 볼 기회가 없었다. **"넣고 → 빈자리 재검증"** 구조로 바꿨다.

**2. 측정 방식을 바꿨다 — 이게 더 중요하다**

- 일괄 스캔 TSV에 **원문(rawLines) 칸**을 넣어, 기기 없이 맥에서 파서에 다시
  먹이는 재생 하네스를 만들었다. **721개 값 중 710개가 기기와 동일**해 충실도를
  확인했다. 한 바퀴가 20분 → **몇 초**가 됐다.
- **검수 도구 신설**(`tool/ocr_review/index.html`) — 실물과 대조해 정답지를
  만든다. 로컬 전용(네트워크 없음), 원문 낱말을 눌러 넣어 타이핑을 줄인다.

**3. ⚠️ 정정 — "오분류 0장"은 정확도가 아니었다 (추가 198)**

추가 183·195에 적은 그 수치는 **값의 모양만** 보는 자동 판정이었다. **빈 값을
"오분류 아님"으로 셌다.** 사용자 지적("설마 테스트를 이 상태 자료로 한거야?")으로
드러났다. 앞으로 **채움률과 정확도를 다른 이름으로 부른다.**

**4. 테스터 피드백 오류 14건 중 12건 종결 (추가 203~210)**

| 완료 | 무엇이었나 |
|---|---|
| E-08 | 태그 기본값 `'AI, IT'`가 코드에 박혀 있었다 — 등록 14건 중 **13건**이 이 값이었다 |
| E-09 | 카카오톡 스킴에 경로가 없어(`kakaotalk://`) 해석 실패 → `kakaotalk://launch` |
| E-07 | `FileImage` 캐시 키가 **파일 경로**라 같은 이름으로 덮어쓰면 옛 사진이 보였다 |
| E-10 | 멀티라인 입력칸엔 키보드에 완료 키가 없다 → 끌어서 닫기 |
| E-01·E-06 | 카메라 흰 화면(배경색), 크롭 여백, 자동 촬영 임계값 |
| E-02·E-13 | **고칠 것이 없었다** — 이미 고쳐져 있었고 기록만 없었다 |

### ⚠️ 다음 사람이 꼭 알아야 할 것

**같은 실수를 오늘 네 번 했다: 재지 않고 숫자를 정했다.**

1. "오분류 0장" — 형식 검사를 정확도로 착각
2. card_66 "맨 앞 0이 떨어졌다" — 실물은 `+82 10(...)`이었다
3. 자동 촬영 대비 임계값 10.0 — **진짜 명함을 막았다**
4. 톤 비율 0.65 — 또 막았다. 재보니 명함이 0.653이었다

**임계값은 반드시 실기기에서 재고 정한다.** 재는 도구를 코드에 남겨 뒀다 —
촬영 화면의 값 표시와 `[CARDGATE]` 로그(`adb logcat`으로 수집).
⚠️ 로그가 0줄이면 **촬영이 끝나 스트림이 멈춘 것**이다. "다시 찍기" 직후에 잡는다.

**측정으로 확정한 값**(추가 210):

| 장면 | 대비 | 톤 |
|---|---|---|
| 명함 | 26~39 | 0.653 |
| 평범한 책상 | 20.4 | 0.611 |

**밝기 지표로는 명함과 평범한 장면을 못 가른다.** 각이 진 빈 곳을 거르려면
**문서 경계 검출**(통합본 R-02)이 필요하다 — 가벼운 근사는 여기까지다.

### ⚠️ 저장소 **밖**에 있는 자료 — 어디 있는지 여기 적어 둔다

셋 다 **제3자 개인정보**(테스터 실명, 명함 주인의 이름·전화·주소)라 저장소에
넣지 않는다. 대신 **위치와 성격을 기록**해 다음 사람이 찾을 수 있게 한다 —
지금까지는 이 기록이 없어서 원문을 찾을 수 없었다.

| 자료 | 위치 | 내용 |
|---|---|---|
| **테스터 피드백 통합본** | `/Volumes/Work/커넥션센스_앱_빌드6_7_테스트_피드백_통합_정리.docx` | 빌드6·7을 합쳐 다시 번호를 매긴 판. 오류 14·기능개선 15·OCR권고 7·확인 2 |
| 명함 표본 **103장** | 기기 `app_flutter/card_samples/` (`adb ... run-as`로 접근) + 맥 `~/Downloads/명함검수_이미지/` | 인식 측정용 원본. 앱의 일괄 스캔이 읽는 곳도 기기 쪽 경로다 |
| 일괄 스캔 결과 | `~/Downloads/scan_result.tsv` | **원문(rawLines) 칸 포함** — 이게 있어야 맥에서 재생 측정이 된다 |

내용 자체는 저장소에 옮겨 적혀 있다 — 통합본은 **추가 203·211**, 명함별 결함
목록은 `ocr-defect-report-2026-08-14.md`. 원본은 대조가 필요할 때만 열면 된다.

📌 빌드6·빌드7 **개별** 원본(`…이예은.docx`, `…처리계획_2026-08-12.docx`)은 현재
위 경로에 없다. 통합본으로 대체됐으나, 추가 188이 그 둘을 인용하고 있으므로
필요하면 사용자에게 다시 받아야 한다.

### 다음에 이어서 할 일

**A. 검수 도구로 정답지를 만든다.** 도구는 완성돼 있다. 정답지가 생기면
**채움률이 아니라 정확도**로 잴 수 있고, 추가 198에서 정정한 문제가 구조적으로
없어진다. `tool/ocr_review/README.md` 참고.

**B. 남은 오류 2건**
- **E-12** 현재 위치 정확도 — 재현 조건부터 좁혀야 한다
- **E-04** OCR 글자 오독(`길`→`킬`) — ⚠️ **파싱 규칙으로는 못 고친다.**
  ML Kit 인식 단계라 R-03(다중 OCR 패스)이 필요하다

**C. R-02 촬영 품질 게이트** — E-01·E-06의 잔여분(각진 빈 곳)과 한 덩어리다.

**D. 기능 개선(F) 15건** — 통합본 기준. 번호가 추가 188과 다르니
**반드시 추가 203의 대조표를 먼저 볼 것.**

**E. PR 정리** — 이 브랜치에 커밋 35개가 쌓였다. 성격별로 나눌지 한 번에 올릴지
결정이 필요하다.

**F. 미착수로 남은 것**(0-16에서 이어짐)
- 직통·팩스·웹사이트 필드 추가
- `supportProfiles` 설계 — ⚠️ `users/{uid}`에 넣으면 안 된다(복호화 키가 같은
  문서에 있어 관리자에게 열 수 없다)
- 생일 축하·생일 무료 충전(어뷰징 방어 3종 필수)

## 0-16. 2026-08-13 — 명함 인식 결함 연속 수정 + 일괄 스캔 도구 신설

**브랜치**: `audit/code-privacy-review` (푸시 완료, PR 미생성). 자동 테스트
231건 통과, `flutter analyze` info 19건·error/warning 0.

### 이 세션에서 한 일

1. **다른 세션(Codex)이 만든 커밋 9개를 검토**했다. 원격에 푸시되지 않은 채
   로컬에만 있었고, `backlog`·`HANDOFF` 갱신도 없었다. 검토에서 결함 4건을
   찾아 전부 고쳤다(추가 178).
   - OCR 키워드가 **부분 문자열**로 걸려 회사명·이름이 틀어지던 회귀
     (`PO` ⊂ `SPORTS`) — 자동 테스트 4건이 깨져 있었다
   - 터치 퀵 매핑 UI가 `rawLines` 미할당으로 **화면에 아예 안 뜸**
   - 관리자 메뉴가 **모든 사용자에게 노출**(`AuthRepository.isAdmin` 신설로 차단)
   - 관리자 화면의 고객 AI 사용량이 **항상 0** — 이미 있던 서버 함수
     `getUserUsage`를 안 쓰고 클라이언트에서 `users/{uid}`를 직접 읽고 있었다
2. **내 프로필에 생일(월·일) 입력칸 추가**(추가 179). 기능은 나중에 붙이되
   데이터는 지금부터 모은다. 방침도 함께 개정.
3. **명함 인식 오분류를 네 차례 수정**(추가 180) — 한글 키워드 부분 문자열
   충돌, 회사 로고가 이름 자리 차지, 슬로건 조각, 낱말 "기업". 고칠 때마다
   다른 변종이 나왔다.
4. **관리자 전용 명함 일괄 스캔 도구 신설**(추가 181)과 **67장 실측**.

### 다음 세션에서 이어서 할 일 (아침 재개용)

**A. 명함 인식 개선 — 67장 실측 기준으로 우선순위가 정해져 있다.**
실측 수치와 오분류 목록, 도구 사용법은 **backlog 추가 181**에 있다. 순서:
1. 전화·팩스 라벨 찌꺼기(`TEL. FAX.`)가 이름/회사명으로 흘러가는 것
2. 주소 줄이 회사명 자리를 뺏는 것(`대구 공장| 대구광역시 …`)
3. 이메일·영문 주소가 이름이 되는 것
4. 슬로건이 이름에서 밀려나 **회사명**으로 옮겨간 것(추가 180의 부작용)

고칠 때마다 **일괄 스캔으로 67장을 다시 돌려** 숫자가 좋아졌는지 확인한다.
이게 이 도구를 만든 이유다.

**B. 사용자가 가진 다른 명함도 함께 분석한다(2026-08-13 사용자 요청).**
지금 표본은 `~/Downloads/명함샘플` 67장뿐이다. 사용자에게 추가 명함 폴더
경로를 받아 같은 방식으로 넣고 돌린다. 표본이 늘면 지금 안 보이는 실패 축이
드러난다. ⚠️ 이미지는 제3자 개인정보라 **저장소 안에 넣지 않는다.**

**C. 미착수로 남은 것**
- 직통·팩스·웹사이트 필드 추가(모델·입력·표시·백업·**방침 개정**까지)
- `supportProfiles` 설계 — SNS 4종 로그인 확장과 고객응대 데이터.
  범위·저장 위치는 논의만 끝났고 구현은 시작 전. 요지는
  **`users/{uid}`에 넣으면 안 된다**(명함 복호화 키가 같은 문서에 있어
  관리자에게 열 수 없다) — 별도 컬렉션으로 분리한다.
- 생일 축하·생일 무료 충전(어뷰징 방어 3종 필수)

## 0-15. 2026-08-12 — 베타 관측 체계 기획 (문서만)

상세는 backlog 추가 170. Android·iOS 테스터 배포(1.0.0+7, `13a45a0`) 직후,
**무엇을 보고 어떻게 모으고 언제 판단할지**를 정리한 문서
`docs/planning/beta-observability-plan.md`를 신설했다. 코드·관리자 콘솔은
건드리지 않았다(`docs/admin/`는 다른 세션 영역이라 `git worktree`로 격리해
작업).

### 핵심

- **비용 실측 → 충전 티어 확정 절차**(가장 공들인 부분): `aiAuditLogs`
  토큰으로 원가(KRW) 계산하는 공식을 세우고, 표본 요건을 "성공 호출
  150건↑ · 참여 uid 5명↑ · 영업일 7일↑ 전부 충족"으로 못 박았다(근거:
  기존 실측에서 카드별 39%, 사고 단계별 4.5배 차이가 이미 드러나
  있었음 — 추가 100). 계산은 PM이, **최종 가격/회차 확정은 사용자 승인**.
- **GA(Firebase Analytics) 도입은 정식 출시 준비 단계로 미루는 걸 권장**
  — 표본이 10명대라 퍼널 통계가 무의미, 지금은 크레딧 지갑·IAP·버전
  게이트가 더 급함. 인구통계(성별/연령/지역)는 목적이 없는 한 지금은
  수집하지 않는 걸 권장(직접 입력이든 GA 추론이든).
- **테스트 종료 시 처리 목록 9개**를 표로 정리 — P0-11 되돌리기(⚠️
  `chore/p0-11-daily-limit-10`은 병합 금지, main 기준으로 상수만 새로
  고칠 것), App Check 재강제, `config/testers` 비우기 등. 크레딧 지갑
  전환(`ai-credit-wallet-spec.md`) Phase 1과 상당 부분 겹치되, 지갑
  전환은 IAP 연동이 먼저 끝나야 한다는 선후 관계를 명시.
- **`latestBuild`는 지금 올리지 않는다** — 다른 세션이 아직 빌드 6으로
  활성 테스트 중이라 배너가 뜨면 방해가 됨. 앞으로 빌드 낼 때마다 지킬
  운영 규칙 6개(전파 지연 기다리기, 플랫폼별 분리 필드 활용, 강제
  업데이트는 베타 기간 내내 0 유지 등) 등록.
- **관리자 콘솔 요구사항 3건**(사용량·로그 탭 기간 집계 카드, 감사 로그
  CSV, 티어 실측/잠정 배지)만 문서로 남기고 구현은 안 함 — 급하지 않고
  스크립트로 우선 대체 가능.

### 사용자가 확인할 것 (2-0에도 등록)

문서 8절에 권장안과 함께 정리: ① GA 도입 시점(권장: 정식 출시 준비 단계)
② 인구통계 수집 방식(권장: 지금은 안 함) ③ `latestBuild` 갱신 시점(권장:
다른 세션 종료 확인 후) ④ 베타 종료 최소 기간(권장: 날짜보다 표본 요건
충족 여부).

### 다음에 할 일

- `tool/analyze_ai_cost.py`(가칭) 개발 착수 — `flutter-developer`에게
  위임(문서 2-4절 스펙).
- 매주 표본 요건(150건/5명/7일) 도달 여부 체크.
- 테스터 설문 1차 발송(1주차 말) — 문항은 문서 3절에 제안돼 있음, 발송
  자체는 사용자(테스터 연락 수단 보유).

## 0-14. 2026-08-12 — QA 완료·버그 3건 수정·배포 직전

상세는 backlog 추가 169. **PR #121·#123·#124·#125·#126 전부 병합, Cloud
Functions 2회 배포 완료.** 코드·서버는 배포 준비가 끝났고, 스토어 업로드만
남았다.

### 지금 상태
- **main = 50ac201**, 버전 `1.0.0+7`. 테스터 빌드 2차 내용이 전부 들어 있다.
- **functions 배포됨**: 날씨 프롬프트(단정 표현 금지) + 일/월 리셋 시간대 수정.
  → 이제 리셋이 **한국시간 자정**에 일어난다(전엔 UTC 자정=KST 09시).
- **빌드 산출물**(워크트리 `scratchpad/deploy-main`, 세션 종료 시 사라짐 —
  필요하면 다시 빌드할 것):
  - Android APK `build/app/outputs/flutter-apk/app-release.apk`
  - iOS 아카이브 `build/ios/archive/Runner.xcarchive`
- **실기기 검증 완료**: Android 전체 테스트 + iOS 계정삭제·경합 방어 확인.

### 남은 배포 절차 (사용자 진행)
1. **iOS**: Xcode Organizer로 아카이브 업로드 → 처리 대기 → **⚠️ 수출 규정
   답변(빌드마다 필요)** → TestFlight 그룹(내부/외부 수동)에 빌드 7 추가.
   빌드 6이 심사를 통과했으므로 재심사 없음.
2. **Android**: 다른 세션에서 웹 콘솔(App Distribution)로 진행 중.
3. **관리자 "앱 업데이트" 탭**: iOS/Android `latestBuild = 7`.
   ⚠️ **최소 지원 빌드는 반드시 0** — 스토어 미공개 상태에서 강제하면
   사용자를 빈 페이지로 보내고 앱만 막는다.

### 이번에 고친 버그 3건 (전부 병합됨)
- 계정 삭제 후 **스피너 무한 회전**(#124) — `context.mounted` 가드가 정작
  닫아야 할 때 닫기를 막았다. 루트 Navigator/Messenger를 미리 잡아 해결.
- **로그아웃 중 계정 삭제 경합**(#126) — `signOut()`이 서버 응답을 기다리는
  동안 `isSignedIn`이 true라 둘이 겹칠 수 있었다. 상호 배제 플래그로 차단.
- **리셋이 KST 09시**(#125) — Node 런타임 TZ 기본이 UTC. **리전(서울)과
  런타임 TZ는 별개**. KST 오프셋 명시 계산 + 경계값 테스트 8건.

### ⚠️ 검증 방식이 바뀌었다 (CLAUDE.md 4장, PR #121)
수정할 때마다 전체 검증을 돌리다 하루치 개발 시간을 잃은 뒤 정한 규칙.
**전체 테스트**(전 항목 실기기, 배포 직전 한 번) / **부분 테스트**(수정한
곳 + 영향 범위) / **자동 테스트**(flutter test·analyze)로 나누고,
**검증 시작 전 사용자에게 표를 보여주고 고르게 한다.** "단위 테스트"라는
말은 쓰지 않는다(flutter test와 뜻이 겹침).

### ⚠️ 같은 저장소 동시 작업 주의 (이번에 세 번 사고)
QA 중 같은 브랜치에 커밋해 검증 대상이 바뀜 / 다른 세션이 main으로 체크아웃해
둔 디렉터리에 브랜치 확인 없이 커밋 / 임시 브랜치에서 hosting:admin을 배포해
**다른 세션의 관리자 재디자인을 라이브에서 덮어씀**(복구 완료).
→ **QA·배포는 반드시 고정 워크트리에서. 관리자 콘솔(`docs/admin/`)은 디자인
담당 세션 영역이라 건드리지 말 것.**

### 다음에 할 일
- **크레딧 지갑 전환**(설계 완료, `docs/planning/ai-credit-wallet-spec.md`)
  — 선행조건: IAP 구현(P1-2/P1-4) + **스토어 상품ID 등록(사용자만 가능)**.
  결제가 안 되는 채로 무료를 끊으면 막다른 앱이 되므로 순서를 지킬 것.
- **환불 규정 확정**(P1-5 연계) — 충전 붙이기 전 필요, 약관 반영·법무 검토.
- A2/A3(보너스·저잔량 표시) 실측 — 배포 후 자연 확인.
- iOS 수출 규정 자동화(`ITSAppUsesNonExemptEncryption`)는 **법적 신고라
  임의로 넣지 말 것** — 프랑스 관련 법무 검토 후.

## 0-13. 2026-08-11 밤~08-12 새벽 — 테스터 빌드 2차 통합 완성, QA 직전 중단

상세는 backlog 추가 165. **기능 4종 + 수정 9건이 통합 브랜치
`feat/tester-build-20260811`(HEAD `3bbbb95`)에 전부 커밋·머지 완료.**
analyze 19 info(기준선) / test 182건 전부 통과. 원격 push 완료.
**실기기 QA 도중 사용자 지시로 중단** — 아침에 QA부터 재개한다.

### 통합 브랜치에 담긴 것
- **AI 사용량 표시 개편**: 칩 "잔여 N회", 시트 "오늘 사용/잔여" 2타일,
  bonusCredits 반영(보너스 보유자의 전송이 막히던 실버그 수정), 저잔량
  "충전 준비 중" 안내(가짜 버튼 없음).
- **AI 충전 화면**(0-12) + **2열 압축 그리드 리디자인**(사용자: "화면을
  너무 많이 쓴다") + 충전 안내 3문구. 환불 규정은 미정이라 주석 자리만.
- **날씨 B안**: "현재 N°C (오늘 최고 M°·최저 L°)" + 프롬프트 단정표현
  금지. ⚠️ functions **미배포** — 배포해야 프롬프트 변경이 라이브 반영.
- **대화 포인트→소통 기록 저장**(방식① 복귀 시 확인). 채널 버튼이
  **통화/문자/카톡/이메일 4개로 확정**(더보기·일반공유 제거 — 사용자 결정
  "소통 기록을 저장할 수 있는 채널만"). 통화 취소가 남긴 묵은 다이얼로그
  수정(iOS 공유시트 resumed 함정), 이메일 칩 동적 표시.
- **탈퇴 시 로컬 정리** 머지(구 미머지 브랜치, backlog 149→**164** 정정).
- **명함 UX 4건**: 중복 시 "기존 유지"→등록 화면까지 닫기 / 촬영 시 대표
  이미지 기본 켜짐 / 지갑 스캔 버튼 아이콘을 주변 addCard로 통일 / 수정
  화면 이미지 재연결.
- **명함 이미지 경로 재연결**: 원인 — cardImagePath는 백업 JSON 미포함이라
  서버 복원이 로컬을 덮어쓰면 경로 유실(파일은 기기에 잔존). 수정 화면
  단건 + **저장소 로드 시점 일괄**(4개 로드 지점, 디렉터리 1회 조회) 둘 다
  구현. 병합 중 함수 중복 정의 1건 정리.
- 관리자 **충전 7단계**(admin.js TIER_PRICES) 배포 완료, config/billing에
  7단계(10/30/50/100/300/500/1000회)+무료10 저장·서버 검증 완료.

### 이번에 겪은 함정 (재발 방지)
- "대구 32도인데 앱은 24도" → 둘 다 맞는 값(낮 최고 vs 저녁 현재).
  증상은 정확도 문제처럼 보였지만 본질은 **표시 범위**였다.
- "공유했는데 통화 저장 다이얼로그가 뜸" → 범인은 공유가 아니라 **취소된
  통화가 남긴 의도** + iOS는 공유시트만 닫아도 resumed가 온다.
- 두 브랜치가 **같은 함수를 서로 다른 위치에 추가** → git이 충돌 없이 둘 다
  수용 → duplicate_definition 컴파일 에러. 머지 후 analyze 필수.
- 밤 Android QA가 **세션 사용량 한도로 사망, 결과 유실** — 장시간 QA는
  한도 리셋 직후 시작하는 게 안전.
- QA가 접힌 폴드 겉화면(실사용 문자 앱)을 캡처하기 직전에 스스로 멈춤 —
  개인정보 화면 캡처 금지 원칙이 실제로 작동했다.

### 아침 재개 순서
1. **Android 종합 QA**(갤럭시 펼쳐 화면 켠 상태): 사용량/충전 그리드/날씨/
   소통기록 4채널(통화취소 회귀 포함)/탈퇴 정리 adb 실물/대표 기본켜짐/
   중복 닫기/복원 시 이미지 자동 재연결/회귀.
2. **iOS QA**: 빌드·설치·실행은 자동, 화면은 사용자 육안(신뢰 페어링을
   한 번 승인해 두면 다음부터 스크린샷 자동화 가능).
3. 통과 → **PR 생성·main 머지** → **functions 배포**(사용자 `!` 실행) →
   **Android App Distribution + iOS 빌드7 TestFlight 업로드**(버전 1.0.0+7
   유지) → 관리자 "앱 업데이트" 탭 latestBuild=7.
4. 별도: 크레딧 원장 스펙 리뷰(Downloads 스펙 문서, 결정 4건), 환불 규정
   검토(작업 등록됨), billing_config 관련 문서.
- 인계 원문: `~/Downloads/커넥션센스_작업인계_2026-08-12.md`

## 0-12. 2026-08-11 — AI 충전 화면 UI 신설

상세는 backlog 추가 163. **결제(IAP) 없이 화면만** 만들었다 — P1-2(IAP
클라이언트 연동)의 UI 셸.

- 새 파일: `lib/data/models/billing_config_model.dart`,
  `lib/data/repositories/billing_config_repository.dart`(Firestore
  `config/billing` 읽기, `AppUpdateService`와 같은 주입 가능 패턴),
  `lib/presentation/features/settings/views/ai_charge_view.dart`(로딩/오류/
  빈상태/정상 4상태, 구매 버튼은 `onPressed: null` 고정 "충전 준비 중"),
  `test/billing_config_repository_test.dart`(7건).
- 진입점: 설정 → "지원" 섹션 → "AI 충전"(`settings_view.dart` 수정, "AI
  잔여 횟수" 행은 안 건드림).
- `flutter-ui-designer`(화면) → `flutter-developer`(테스트+진입점) 순서로
  위임, 둘 다 이 시점 동시 편집 중이던 다른 브랜치 파일들(`ai_usage_*`,
  `weather_service.dart`, `functions/`, `briefing_overlay_view.dart`,
  `contact_model.dart`)은 건드리지 않았다.
- **동시 작업 격리**: 1단계 종료 시점에 본체 저장소가 이미 다른 세션의
  `fix/weather-summary-b`로 체크아웃돼 미커밋 변경분이 있는 상태였다 — 신규
  파일이 실수로 거기 섞여 들어가 있던 걸 발견해 제거하고, 이후 전부
  `git worktree`로 격리된 `feat/charge-screen-ui` 브랜치에서만 작업시켰다.
  ⚠️ **아래 "커밋되지 않은 워크트리에 남아 있다"는 서술은 낡았다**(2026-08-18
  실측). 그 워크트리(`connection-trace-ai-v2-flutter-charge-screen/`)도 브랜치
  `feat/charge-screen-ui`도 **로컬·origin 어디에도 없고**, 이 작업은 `main`에
  들어가 있다 — `lib/presentation/features/settings/views/ai_charge_view.dart`
  (커밋 `357ad1c` → `4088e01` → `a593604`). 당시 기록은 다음과 같았다:
  *"이 브랜치는 커밋되지 않은 채 별도 워크트리에 남아 있다"*(origin/main 최신
  `464d8b5` 기준).
- 검증: `flutter analyze`(0 error/warning, info 19건 그대로) +
  `flutter test`(147건 전부 통과) 둘 다 PM이 직접 그 워크트리에서 재실행해
  확인함. 실기기 QA는 의도적으로 생략(다른 기능과 묶어 배치 QA 예정).
- **범위 밖(그대로 남음)**: `in_app_purchase` 연동·영수증 검증(P1-4), 결제
  동의/고지 UI(P1-3), `config/billing` 실제 값(티어 회수) 확정.

## 0-11. 2026-08-11 — 심사 통과·과금 확정·충전 관리 콘솔·에셋 구조화

상세는 backlog 추가 153·154·160. 하루에 결이 다른 작업 4묶음이 끝났다.

### ① iOS 베타 심사 통과 + TestFlight 그룹 재구성 (추가 160)

- 빌드 `1.0.0 (6)` **심사 통과("테스트 중")** — 하루 미만. 이후 빌드는
  대부분 재심사 없음.
- 기존 내부 그룹이 "자동 - Xcode 빌드" 배포 모드로 잠겨 있어 **수동 배포
  그룹 2개를 새로 만들었다**: 내부테스터(수동)·외부테스터(수동), 둘 다
  빌드 6 연결됨. ⚠️ 옛 공개 링크(`…/hkKVAUB2`)는 무효 — 새 그룹 링크 사용.
- **잔여**: 각 테스터 폰에서 빌드 6 설치 확인(전파 지연 중이었음). 안 되면
  테스터 삭제→재추가로 재초대. Android의 "1.0.0 (5)" 표기는 정상(같은 코드).

### ② 과금 모델 확정 — 충전형 (추가 153, 2-0 결정 1번 닫힘)

- 무료 10회(변경 가능) → 충전. 가격 4단계 1,000/3,000/5,000/10,000원.
  **티어별 회수는 테스트 기간 `aiAuditLogs` 실측 후 손익 분석으로 확정.**
- 구독형은 폐기 아님 — 출시 후 사용 통계 기반 별도 상품으로 추가 예정.
- P1-1~P1-5를 소모성 기준으로 재작성. 출시 전 **스토어 수수료 15% 신청
  2건 필수**(`admin-manual.md` 3-4 — Apple은 신청 안 하면 30%).

### ③ 관리자 콘솔 "충전 관리" 탭 신설·배포 (추가 154)

- 상품 설정(`config/billing` — 티어 회수·판매 여부·무료 횟수, 코드 배포
  없이 조정), CS 충전 내역 조회(`purchases` 이메일 검색), 정산 요약(월별
  집계 + CSV). **운영 배포 완료.** `purchases`는 서버만 쓰기(위조 방지).
- IAP 구현 전이라 기록이 비어 있는 게 정상.

### ④ 이미지 에셋 도메인 구조화 (PR #100)

- `assets/images/{brand|common|nearby|wallet|scan|comm|ai|settings|profile}/`
  구조로 재편, 파일명 12자 이내. **카탈로그**: `docs/design/image-catalog.md`
  (미리보기·의미·사용처). **안전망**: `test/asset_files_test.dart`(전 에셋
  로드 검증). 레거시(icons3d 잔여 등)는 번들 제외.
- 디자이너용 Figma 편집 파일도 재구성됨(레이어·컴포넌트·색상 변수,
  fileKey `6rLFYNRqHW36U9GUU0DPJt`) — 디자이너 수정분은 Figma MCP로 읽어
  코드 반영하는 흐름.

### 다음 세션이 이어받을 것

1. 테스터 폰 빌드 6 설치 확인 (위 ① 잔여)
2. 테스터 피드백 수집 → 다음 빌드(1.0.0+7, Android/iOS 동시)에 PR #91·#93
   수정 포함 + 실기기 확인 2건(전화 걸기·명함 대표 이미지)
3. 테스트 종료 시: P0-11 되돌리기 + `aiAuditLogs` 실측으로 티어 회수 손익 분석

## 0-10. 2026-08-10 — Android/iOS 직원 테스터 등록 + iOS 빌드 6 베타 심사 제출

상세는 backlog 추가 149.

### 완료

- **Android 테스터 6명**을 관리자 콘솔 "테스터 관리"(`config/testers`)에
  등록. 전부 로그인 계정 이메일 확인함. 기존 3명(아이폰 테스터용)은 그대로 둠
  — 총 10건.
- **iOS 빌드 `1.0.0 (6)` 업로드 + TestFlight 외부 베타 심사 제출 완료
  (심사 대기중)**. 빌드 5(8/9 업로드) 이후 main에 166개 커밋이 쌓여 있었는데
  외부 그룹을 아직 안 만든 상태라, 그룹 생성 전에 새로 빌드해 최신 상태를
  반영했다(PR #88, `pubspec.yaml` → `1.0.0+6`). 기존 외부 그룹
  "커넥션센스_테스터(외부)"(아이폰 테스터 3명)에 빌드 연결 후 제출.
- **심사용 계정을 신규 Gmail로 생성**해서 사용 — `connectionsense@creamhouse.net`
  (관리자 콘솔 전권 계정)을 쓰려던 것을 막고 전환함(아래 "다음 확인 필요"
  참고할 것 없음, 이미 처리됨).

### 저녁 추가 작업(추가 150)

- **P1-20 완료(PR #91)** — 매니페스트 `<queries>`에 tel 인텐트 추가.
- **P1-16 닫힘(PR #92)** — 이미 2026-08-05에 구현돼 있었음(문서 지연).
- **명함 대표 이미지 선택 수정(PR #93, 사용자 제보)** — 앞/뒷면 스캔 시
  대표가 무조건 마지막 스캔(뒷면)이 되던 것을, 이름 읽힌 면 자동 선택 +
  썸네일 선택 UI로 수정.

### 다음

- **심사 결과 확인** — 보통 1~2일. 반려되면 `store-review-notes.md` 7절
  "반려 위험이 있는 지점" 참고.
- 심사 통과 후 아이폰 테스터 3명에게 TestFlight 초대 확인 안내.
- **다음 테스터 빌드(Android 재배포 + iOS 빌드 7)에 PR #91·#93 수정을 합쳐서
  배포**(사용자 결정, 추가 150). 그때 실기기 확인 잔여 2건도 함께:
  ① 전화 걸기 버튼이 전화 앱을 여는지 ② 명함 앞/뒷면 스캔 시 대표 이미지가
  이름 있는 면으로 잡히고 썸네일로 바꿀 수 있는지.
- Android/iOS 테스트 종료 시점에 P0-11 되돌리기 체크리스트(0-9 "⚠️ 테스트
  종료 후 되돌릴 것") 함께 처리.

## 0-9. 2026-08-09 오후 — 직원 테스트 인프라(허용목록·관측) 배포 + APK 준비

상세는 backlog 추가 111·112. 목적: 직원들에게 앱을 배포해 **AI 사용성 + 실제
비용(손익분기 검증) + 로직 헛점**을 실사용으로 확인. 그 인프라를 만들고 **실제로
배포(라이브)**했다.

### 배포 완료(라이브)

- **테스터 허용목록**(추가 111) — 서버 App Check 강제 때문에 스토어 밖 빌드는
  AI가 막혔다. `appcheck-debug`는 기기별 디버그 토큰이 logcat에만 떠서 직원
  배포에 부적합 → 대신 **관리자 콘솔에서 이메일을 등록**하면 App Check 없이도
  AI를 허용하는 방식. `generateBriefing`의 `enforceAppCheck`를 false로 내리고
  본문에서 수동 검사(유효 토큰 OR 허용목록 이메일). 일일 한도 유지.
- **운영 관측 2종**(추가 112) — ① AI 호출 감사 로그(`aiAuditLogs`: uid·이메일·
  성공/실패·오류코드·토큰(thoughts 포함)·지연), ② 관리자 전용 `getUserUsage`
  callable(이메일→사용량 카운터, 암호화 키·프로필은 안 줌). 관리자 콘솔에
  "테스터 관리" · "사용량·로그" 탭 추가.
- **탈퇴 시 로그 삭제** — v2 Firestore 트리거 `onUserDeletedCleanup`
  (`users/{uid}` 삭제 시 그 uid의 aiAuditLogs batch 삭제). v1 auth onDelete는
  `@firebase/app` 미설치로 배포가 깨져서 v2로 감. Eventarc 최초 설정 지연으로
  1회 재시도 후 생성 성공.
- **개인정보 방침 반영** — 2-4항에 "AI 기능 이용 운영 로그(계정 식별자·시각·
  성공여부·토큰, 명함/대화 내용 제외, 탈퇴 시 파기)" 고지 추가, 보유기간 표에
  행 추가, 시행 전 2.0 변경이력에 반영. 앱은 이 방침을 hosted URL로 WebView
  표시하므로 배포로 앱에도 반영됨.
- **배포 명령**: `firebase deploy --only functions,firestore:rules,hosting:admin,hosting:legal`.

### Android 테스터 APK 준비 완료

- `tool/build_app.sh apk release`로 **업로드 키 서명** release APK 빌드(커밋
  `ae0cd2d`). appcheck-debug 아님 — 허용목록이 AI를 열어줌.
- 위치: `build/app/outputs/flutter-apk/app-release.apk`.
- **내일**: `firebase appdistribution:distribute <APK> --app
  1:79345379389:android:24e13cbaadaf82ac182254 --testers "이메일들"` + 직원
  이메일을 관리자 콘솔 "테스터 관리"에 등록.

### PR 정리

이 세션의 PR **#36~#40 전부 병합 완료**(열린 PR 0개). backlog 최상단 순서 충돌
(추가 109·110 vs 111·112)은 최신순으로 정렬해 해소.

### ⚠️ 테스트 종료 후 되돌릴 것 (중요)

- **`enforceAppCheck: true` 복원** + `config/testers` 비우기(허용목록은 App Check
  우회 권한이라 테스트 끝나면 닫아야 함).
- **P0-11**: `DAILY_LIMIT` 20 → 10 — 🚧 **직원 테스트 기간엔 20 유지**(추가 148).
  ⚠️ **2026-08-15 사용자 지시로 "테스트 종료 후 필수"에서 "최종 테스터 테스트가
  끝난 뒤 복원 여부를 그때 결정"으로 바뀌었다**(추가 218). 즉 20→10은 자동으로
  되돌리는 항목이 아니라 **사용자가 판단할 항목**이다 — 실측 사용량과 티어 회수
  결정(2-0의 8번)을 함께 보고 정한다.

### 미결정 / 다음 확인 필요

- **프랑스 수출 신고**(정식 출시 전 법무) — 베타는 무관.
- **Firestore `legalDocs`** 사본이 앱에서 실제로 안 쓰이는 것으로 보임(앱은
  hosted URL을 WebView로 봄). 관리자 콘솔 "법적 문서" 편집의 실효성/정합성
  정리 필요(별도).
- **정산/통계/기능토글**(관리 콘솔 2단계) — 유료화 모델 결정 후.

## 0-8. 2026-08-09 — Android 서명 키(P1-19) + iOS 빌드5 목적문자열 + TestFlight 외부배포 세팅

상세는 backlog 추가 109·110. 이 세션은 "0-7 내일 순서"의 1·5번을 끝내고,
2·3·4번(외부 그룹·심사용 계정·베타 심사)을 세팅 단계까지 진행했다.

### 완료

- **P1-19 Android 릴리스 서명(추가 109)** — 업로드 키스토어 생성
  (`~/keys/connectionsense-upload.jks`), `android/key.properties`(gitignore),
  `build.gradle.kts` 서명 연결. APK·AAB 둘 다 업로드 키 서명 검증 완료.
  키스토어·비밀번호는 저장소 밖 2곳 백업(iCloud "커넥션센스(중요키)", 맥북
  "커넥션센스 메모"). **PR #36.**
- **iOS 빌드 `1.0.0 (5)` 업로드 완료** — "0-7 내일 순서" 1번(빌드4 업로드)은
  Xcode 재로그인 없이 그냥 통과(어제 실패는 일시 장애였음)했고, 이어서
  **경고 90683**(`NSLocationAlwaysAndWhenInUseUsageDescription` 누락)을 고친
  빌드 5를 새로 올렸다. geolocator가 '항상 허용' 위치 API를 참조해 뜬 경고로,
  Info.plist에 목적 문자열을 추가해 해결. 빌드 5는 App Store Connect에서
  **경고 삼각형 없이** 처리 완료. **PR #37**(pubspec `1.0.0+5` + Info.plist).
- **빌드 5 수출 규정 답변 완료** — "표준 암호화 알고리즘"(직접 구현한
  AES-256-GCM) 선택, 프랑스 후속 질문은 "아니오". ⚠️ 회사 명의 신고이고
  프랑스 부분은 정식 출시 전 법무 검토 대상(베타는 무관).
- **TestFlight "테스트 정보" 입력·저장** — 베타 앱 설명, 피드백 이메일
  (`connectionsense@creamhouse.net`), 개인정보처리방침 URL, 베타 앱 심사
  정보(연락처 + 심사용 추가 정보/심사 노트). 문안 출처는 `store-review-notes.md`.

### 다음(내일) — 외부 베타 배포 마무리

1. **직원 중 아이폰 사용자 확인 → 테스터 이메일 수집** (사용자가 오늘
   "내일 직원 확인 후 그룹 생성"으로 보류). 외부 그룹은 테스터 이메일이
   있어야 초대·심사 제출이 되므로 이메일 수집이 선행.
2. **외부 그룹 생성** — TestFlight 상단 파란 배너의 **"그룹 생성"** 링크로
   만든다(사이드바 ➕는 "내부 그룹"이라 다름 — 오늘 실제로 헷갈렸다).
   외부 그룹이 하나도 없으면 사이드바에 "외부 테스팅" 섹션이 안 보인다.
3. **그룹에 빌드 5 추가 + "테스트할 내용" 입력**(문안은 store-review-notes.md 2절).
4. **심사용 계정 생성**(새 Gmail, 2단계 인증 끄기, 테스트 명함 3~5건) →
   "테스트 정보 → 베타 앱 심사 정보 → 로그인 필요"에 계정/비번 입력 +
   심사 노트의 "(심사용 계정 입력)" 자리 채우기. ⚠️ 기존 관리자 계정 재사용 금지.
5. **베타 심사 제출**(1~2일).

### 열린 PR

- **#36** Android 서명 키(P1-19) — docs(추가 109·110·0-8 포함)도 이 브랜치에 있음
- **#37** iOS 목적 문자열(경고 90683) + 빌드번호 `+5`

두 PR은 서로 독립적이라 병합 순서 무관(문서는 #36에만 있어 충돌 없음).

## 0-7. 2026-08-08 세션 마무리 — 다음에 이어받을 순서

하루 종일 진행한 세션의 최종 상태. 상세는 backlog 추가 99~107.

### 지금 각 채널에 무엇이 올라가 있나

| 채널 | 빌드 | 담긴 것 |
|---|---|---|
| TestFlight | `1.0.0 (3)` | App Check 무한 로딩 수정까지 |
| **아카이브(미업로드)** | **`1.0.0 (4)`** | 위 + **UX 3종 + AI 실패 안내 개선** |
| 아이폰(개발 설치) | `f26643b` | 전부 |
| 갤럭시(개발 설치) | `1.0.0 (3)` 상당 | 전부 |

`1.0.0 (4)` 아카이브는 여기 있다 — Xcode Organizer에서 바로 업로드 가능:
`~/Library/Developer/Xcode/Archives/2026-08-08/커넥션센스 1.0.0 (4).xcarchive`

**업로드가 오늘 안 된 이유**: Xcode가 `Unable to authenticate with App Store
Connect`로 실패했다. 계정 권한은 정상(Admin, Certificates·Identifiers·Profiles
✅)이므로 **세션 만료 또는 22시 정기 점검 시간대와 겹친 일시 장애**로 보인다.
다음에 Xcode에서 재로그인(2단계 인증)하면 될 가능성이 높다.

### 내일 순서 (권장)

1. **Xcode 재로그인 → `1.0.0 (4)` 업로드**
2. **각 빌드의 수출 규정 답변** — ⚠️ **빌드마다 답해야 한다**(오늘 확인).
   빌드 2·3이 지금 "수출 규정 관련 문서 누락" 상태다. 없으면 TestFlight
   배포가 진행되지 않는다. 문안은 `store-review-notes.md` 4-1절
3. **심사용 계정 생성** — 새 Gmail, 2단계 인증 끄기, 테스트 명함 3~5건.
   ⚠️ 기존 관리자 계정(`connectionsense@`, `apps@`) 재사용 금지
4. **외부 테스트 그룹 생성 → 베타 심사 제출**(1~2일). 20명이면 내부 테스트로는
   불가(내부는 App Store Connect 팀 사용자만)
5. ~~**Android 업로드 키스토어 생성**(P1-19)~~ → ✅ **완료(2026-08-09, 추가 109)**.
   `~/keys/connectionsense-upload.jks` 생성, `key.properties` 작성, `build.gradle.kts`
   서명 연결, APK·AAB 서명 검증까지 완료. 키스토어·비밀번호는 저장소 밖 2곳 백업 완료.
   상세는 위 P1-19 표 행 참고.

### ⏳ 다음 테스터 배포 때 함께 머지·검증할 미머지 브랜치 (추가 150)

사용자 결정: 실기기 검증이 필요해 코드만으로 안 머지하고 넘겼다. 다음 배포
빌드에 합쳐 확인 후 머지한다. 둘 다 analyze 19건·test 103건 통과.

- `fix/account-deletion-local-cleanup` — 회원 탈퇴 시 기기에 남던 명함
  이미지·프로필 사진·암호화 키 정리(보안 보고서 1-2). ⬜ `adb run-as`/재가입
  으로 파일·Keychain 삭제 확인. ⚠️ 이 브랜치 backlog 엔트리가 "추가 149"인데
  main에 이미 다른 추가 149가 있음 — **머지 시 번호 정정**.
- `feat/card-no-address-badge` — 명함 "주소 없음" 배지 + 명함첩 위치 진단
  배너(B) + 지오코딩 실패 형태 로깅(C, 주소 원문 미기록).

### 테스터 배포 전에 처리를 권하는 것

- **P0-11** `DAILY_LIMIT` 20 → 10 — 🚧 **직원 테스트 기간엔 20 유지**(추가
  148). 테스트가 끝나면 두 파일(`functions/src/index.ts`,
  `lib/core/services/ai_briefing_service.dart`)을 함께 고치고
  `firebase deploy --only functions`까지 돌릴 것.
- **P1-40** 전화번호 중복 검사 — 테스터가 늘면 바로 겪는데 작업량이 작다
- **P1-13** Crashlytics — 없으면 테스터 피드백이 "가끔 죽어요"에서 멈춘다

### 오늘 발생한 사고 (반복 방지)

- **앱 무한 로딩** — `await AppCheckService.activate()`가 시작을 막았다.
  예외가 아니라 "응답 없음"이라 `try/catch`로도 안 잡혔다. 수정 완료
- **명함 2건 복구 불가** — 계정 삭제 다이얼로그가 삭제 대상을 표시하지 않아
  의도와 다른 계정이 삭제됐다. 계정 삭제는 암호화 키까지 지우므로 **다른
  기기의 로컬 사본도 복구 불가**가 된다. 다이얼로그 수정 완료

### 오늘 반복된 패턴

증상이 가리키는 곳과 원인이 있는 곳이 세 번 달랐다.

| 증상 | 실제 원인 |
|---|---|
| "App Store Connect 403" | Xcode 계정 미등록(로컬) |
| "npm audit 8건" | 상류 패키지(우리가 못 고침) |
| "무한 로딩" | App Check가 앱 시작을 막음 |

그리고 **문서가 실제 상태보다 뒤처진 것을 여섯 번 고쳤다**(P0-1·P0-5·P0-6·
P1-26·P1-27·P0-2·P1-7). 완료 처리를 그때그때 하지 않으면 다음 세션이 이미
끝난 일을 다시 검토한다 — 실제로 오늘 P0로 잡고 시작한 일 중 둘이 이미
끝나 있었다.

## 0-6. 2026-08-08 — App Check 도입(P0-9) + 옛 iOS 앱 참조 교정

상세는 [`backlog.md`](../backlog.md) 추가 99, 운영 절차는
[`admin-manual.md`](../admin-manual.md) 2절.

**배경**: 어제(추가 96~98) AI 프록시를 실제로 켰는데, P0-9(App Check)는
"프록시 배포 **전에** 선행"으로 등록돼 있던 항목이었다. 순서가 뒤집힌 채
하루가 지나 있었고, 그동안 **Google 로그인만 통과하면 우리 앱이 아닌
스크립트도 회사 명의 유료 Gemini 키를 쓸 수 있는 상태**였다.

**한 일**: `firebase_app_check` 도입 + `AppCheckService`, 서버
`enforceAppCheck: true` 전환·배포, `maxInstances` 코드 명시,
`tool/build_app.sh`에 `appcheck-debug` 인자 추가, `firebase.json`의 옛 iOS
앱 ID 교정.

**검증**: `flutter analyze` 18건(신규 0) / `flutter test` 89/89 /
**아이폰 실기기에서 강제 상태로 호출 성공**(`{"app":"VALID","auth":"VALID"}`).

### ⚠️ 다음 사람이 반드시 알아야 할 것

**정식 무결성 검증기는 스토어 배포를 전제로 한다.** 이번에 실기기로 확인했다.

| 검증기 | 통과 조건 | 지금 |
|---|---|---|
| Play Integrity | Google Play가 아는 앱(내부 테스트 포함) | ❌ Play 미배포 + debug 키 서명(P1-19) |
| App Attest | TestFlight / App Store 빌드 | ❌ 개발 서명은 `403 App attestation failed` |

그래서 **스토어를 거치지 않는 빌드는 반드시**
`tool/build_app.sh <타겟> release appcheck-debug`로 빌드하고 그 기기의
디버그 토큰을 Firebase에 등록해야 한다. 안 그러면 AI 브리핑이 막힌다.
**반대로 스토어 업로드 빌드에는 이 인자를 붙이면 안 된다** — 붙이면 디버그
토큰만 있으면 누구나 우리 앱인 척할 수 있어 App Check가 무의미해진다.

즉 **P0-9의 남은 절반은 P0-1(App Store Connect)과 P1-19(Android 서명 키)에
묶여 있다.**

### 이번에 드러난 함정들 (전부 코드로는 안 보이던 것)

- **Firebase App Check API가 꺼져 있었다.** 이 상태에선 등록을 정확히 해도
  기기에서 403이 나는데, 앱 로그는 "콘솔에 등록하라"는 엉뚱한 안내만 준다.
- **Firebase iOS 앱에 Apple 팀 ID가 비어 있었다** → `77L7BH2M2W` 등록.
- **iOS 앱이 두 개다.** 옛 번들 ID(`…ios:711add…`)와 현재(`…ios:534c87…`).
  `firebase.json`만 옛 앱을 가리키고 있었다 — `flutterfire configure`를
  돌리면 iOS 로그인·백업이 통째로 깨지는 지뢰였고 이번에 교정했다.
  **옛 앱을 Firebase에서 삭제할지는 아직 결정 안 됨**(참조를 끊어 위험은 제거).
- **App Check REST API는 등록 여부와 무관하게 기본 config를 돌려준다** —
  조회 결과만 보고 "등록돼 있다"고 판단하면 안 된다.
- **`maxInstanceCount`가 코드 지정 없이 이미 3이었다.** 처음에 10으로
  잡았다가 발견하고 되돌렸다 — 상한을 거는 줄 알았는데 푸는 셈이었다.
- **예산 알림은 차단 장치가 아니다.** 실제 방어는 `maxInstances`, uid별 한도,
  Gemini 월 지출 한도 셋뿐.
- **Node.js 20 런타임이 2026-10-30에 폐기된다** — 그 뒤엔 런타임을 올리기
  전까지 Cloud Functions 배포 자체가 막힌다(신규 항목으로 등록).

## 0-5. 2026-08-07 — AI 브리핑 실전 배포 + 실기기 E2E 검증 완료

상세 시간순 기록은 [`backlog.md`](../backlog.md) 추가 94~98. 이 세션에서
P0-3(AI 서버 프록시 검증)이 드디어 완료됐다 — Blaze 전환부터 실제 대화
포인트가 화면에 뜨는 것까지 전부 확인.

**한 일**:
1. **Firebase Blaze 전환 + Gemini 유료 키 발급 + Cloud Functions 배포**
   (추가 94) — `docs/planning/admin-manual.md`에 절차와 콘솔 URL을
   문서화해 다음에 같은 작업을 반복 안 해도 되게 했다.
2. **주소 버그 2건 실기기 검증 완료**(추가 95) — 도로명 없이 시/도만
   덮어쓰던 버그, OCR에서 건물번호가 상세주소로 밀리던 버그.
3. **AI 브리핑 E2E 실기기 검증**(추가 96) — 겹겹이 쌓인 문제를 하나씩
   걷어냈다: Gemini 선불 크레딧 고갈 → 월 지출 한도 → 서버 하루 호출
   한도(`DAILY_LIMIT=10`) 소진 → **`gemini-3.6-flash`가 사고 과정/초안을
   먼저 쓰다 출력 토큰(400)을 다 써서 진짜 답변 전에 잘리던 파싱 버그**
   (`MAX_OUTPUT_TOKENS`를 3000으로 올리고 한글 없는 줄을 걸러내는 방어
   로직 추가로 해결). 두 연락처(이든/한병임)에서 실제 Gemini 호출로
   정상 한국어 대화 포인트 3개 생성 확인.
4. **버그 2건 추가 발견·수정**(추가 97) — 최초 설정 후 "내 프로필"을 다시
   수정할 진입점이 앱에 아예 없던 버그(설정 화면에 상시 진입점 추가),
   그 편집 시트가 iOS에서 화면 맨 위까지 붙어 보이던 버그(`AddCardModalView.
   show()`와 같은 높이 제한 패턴 적용).
5. **AI 브리핑 사용성 피드백 3건 반영**(추가 98) — Gmail 매번 재연결
   (조용한 재로그인 우선 시도로 완화), 통화/문자/카톡 미연동 보완용
   자유 텍스트 메모 칸, 통화 버튼 누르면 대화 포인트가 사라지는 문제
   (클립보드 자동 복사로 완화). Gmail 403도 원인(Gmail API 비활성화)을
   찾아 해결.

**검증**: `flutter analyze`(18건, 기존 19건보다 개선) / `flutter test`
(89/89 통과) / Android+iOS 실기기 재빌드 확인.

**빌드 환경 팁(재사용)**: iOS `flutter run`이 "Timed out waiting for
CONFIGURATION_BUILD_DIR"로 실패하면 **Xcode.app GUI가 동시에 열려 있는지
먼저 확인할 것** — 완전히 종료하면 바로 해결된다(이 세션에서 반복 재현·
해결 확인).

**다음에 할 것**:
- 수동 소통 기록 입력 UX 개선(사용자가 "불편하다"고만 함, 구체적 지점
  추가 청취 필요).
- Gmail 외 이메일(네이버/아웃룩/iCloud) 연동 — 국내 이메일 점유율 조사
  결과 Gmail이 압도적(MAU 약 1600만 vs 네이버 약 223만, 다음 약 128만).
  제공사마다 인증 방식이 달라 우선순위 재검토 필요.
- 안드로이드 실기기에서 반복 재현되는 "명함 복호화 실패(3건)" 로그 —
  오늘 변경사항과 무관해 보이나 다음에 `tool/verify_device_local.py`로
  실물 확인 필요.
- P0-1(App Store Connect 403), P0-2(Apple 로그인 Firebase 설정),
  P0-9(App Check) 등 기존 P0 항목은 그대로 남아 있음.

## 0-4. 2026-08-05 밤 — 아이콘 시스템 + 브랜드 블루 리브랜딩

**배경**: 사용자가 Claude Design(claude.ai/design)에서 만든 아이콘 핸드오프
번들(`~/Downloads/untitled 3/`)을 전달하며 "내일 06시까지 아이폰·안드로이드
모두 적용하고 검수까지 해놔, 묻지 말고, 내가 물어보면 정리해서 보고해"라고
지시 — 이 세션은 사용자가 잠든 동안 자율적으로 진행한 야간 작업이다.

**핸드오프 내용**: `커넥트센스 아이콘 시스템.dc.html` 안에 24×24 격자·선
굵기 1.25px·기본색 `currentColor`+강조색 `#2563EB`(브랜드 블루) 규칙으로 총
38종 아이콘이 SVG 프리미티브(path/circle/line/rect/text) 데이터로 정의돼
있었고, 함께 온 참고 이미지(`ChatGPT Image ...png`)에는 위치핀 안에 인물
실루엣 + 명함이 겹쳐진 브랜드 마크(앱 아이콘 시안)가 담겨 있었다.

**한 일**:
1. **브랜드 컬러 전환** — `lib/core/theme/app_colors.dart`의
   `accent`/`accentText`/`accentSoft`를 기존 퍼플(`#6C5CE7`, 2026-08-04
   확정 리디자인 때 정한 색)에서 브랜드 블루(`#2563EB`/`#1D4ED8`/
   `#E8F0FE`)로 교체. 이 세 토큰이 테마 전체(버튼/선택 상태/입력창 포커스
   등)의 유일한 소스라 다른 파일은 손댈 필요가 없었다.
2. **아이콘 SVG 38종 생성** — dc.html의 `ICONS` 배열과 렌더 함수(`attrs`/
   `strPart`)를 Python으로 그대로 재현해 `assets/icons/*.svg` 생성. 기본
   요소는 `currentColor`, 강조 요소는 리터럴 `#2563EB`로 박아 넣어 다크
   배경에서 기본색만 바꿔도 강조색이 유지되게 했다(스펙 규칙 그대로).
3. **`AppIcon` 위젯 시스템** — `flutter_svg` 추가, `lib/core/icons/
   app_icons.dart`에 `AppIconId` enum(38종) + `AppIcon` 위젯. `flutter_svg`
   2.x의 `SvgTheme(currentColor: ...)`로 강조색은 건드리지 않고 기본색만
   치환한다 — 처음에 `colorFilter`(BlendMode.srcIn)로 시도했다가 강조색까지
   덮어써서 잘못됐다는 걸 알아채고 `SvgTheme`으로 바꿨다.
4. **앱 런처 아이콘/스플래시 재생성** — 참고 이미지 속 브랜드 마크를 SVG로
   직접 재현(사진을 그대로 잘라 쓰면 1024px로 업스케일할 때 흐려져서,
   같은 구도를 벡터로 다시 그려 `cairosvg`로 1024×1024 래스터화)하고
   `assets/icons3d/pin_card_blue_full_bleed.png`/`_splash.png`로 저장,
   `flutter_launcher_icons`+`flutter_native_splash`로 iOS/Android 양쪽
   재생성. 기존 "라벤더 레이더" 아이콘(2026-08-04, task #19/21/22)을
   대체한다.
5. **화면 적용** — 하단 탭바, 설정 화면 전 메뉴 행, 레이더/명함지갑의
   주요 액션 버튼, 브리핑 오버레이(추천 대화 포인트·최근 소통 기록 4채널·
   메모·안부 전화), 소통 기록 추가/수동 입력 시트, 내 명함 화면, 알림
   센터, 위치 동의 시트 등 스펙과 제목이 정확히 일치하는 화면에 전부
   적용. `_SettingsRow`/`_SettingsSwitchRow`/`ActionCircleButton`은
   `IconData` 대신 `Widget`(또는 `AppIconId?`)을 받도록 바꿔서 Material
   Icons와 신규 아이콘을 같은 자리에서 섞어 쓸 수 있게 했다. 스펙에 없는
   범용 UI 아이콘(닫기·체브런·에러·새로고침 등, 총 117개 남음)은 그대로
   Material Icons 유지 — 이건 아이콘 시스템이 커버하는 범위 밖이라 일부러
   안 건드렸다.
6. **검증** — `flutter analyze`(에러 0, 기존 info 19건 외 신규 없음),
   `flutter test`(39/39 통과). 실기기 육안 확인까지 완료(아래 참고).

**실기기 검증 완료 (2026-08-06 아침, 사용자가 폰을 펼치고 아이폰을 무선
연결해줘서 마무리)**:
- **Android**: 실기기(SM F966N, Galaxy Z Fold)를 펼친 뒤 탭바·명함지갑·
  설정·주변 화면을 스크린샷으로 육안 확인 — 브랜드 블루 액센트, 커스텀
  아이콘(명함 스캔·통화·로그아웃·계정삭제·주변인맥감지 등) 전부 스펙대로
  정상 렌더링, 크래시 없음.
- **iOS**: 아이폰(16 Pro)이 무선(네트워크)으로 연결돼 `flutter build ios
  --release`(코드사이닝 팀 77L7BH2M2W로 자동 서명)를 새로 빌드하고
  devicectl로 설치+실행. 프로세스가 정상 구동 확인됐다(크래시 없음).
  단, 무선 연결 환경이라 `idevicescreenshot`(libimobiledevice)이 기기를
  못 찾아 실제 화면 스크린샷은 못 남겼다 — Flutter는 Android/iOS 양쪽에서
  동일한 Dart 렌더링 엔진(Skia/Impeller)으로 UI를 그리므로 Android에서
  확인된 렌더링과 동일할 것으로 판단한다. iOS 26.5 시뮬레이터에서
  `google_mlkit` pod의 arm64 슬라이스 미지원으로 시뮬레이터 실행이 막히는
  건 여전한 기존 제약(이번 작업과 무관)이라 그대로 남겨둔다.

**작업 브랜치**: `feature/icon-color-rebrand` (worktree
`connection-trace-ai-v2-flutter-work/`, `main`에서 분기). [[connection-trace-ai
auto-commit]] 규칙대로 완료 단위마다 커밋. `main` 병합/원격 push는 사용자
확인 후 진행.

## 0-3. 2026-08-05 — AI 연동 서버 프록시 전환 + 공지사항/1:1문의/관리자콘솔 + 동시 작업 브랜치 병합

이 세션은 다른 세션("0-2" 작성자, 좌표 C안·법적 문서·Apple 로그인 담당)과
**같은 저장소를 동시에** 작업했다. 처음엔 같은 작업 디렉토리를 공유해서
파일이 실시간으로 섞이는 사고가 날 뻔했고, 그걸 계기로 `git worktree`로
격리해 작업하는 방식으로 전환했다 — 아래 "동시 작업 충돌과 해결" 참고.

### AI 연동 화면 — BYOK → 서버 프록시 전환 (커밋 `ed6b4b7`)

P1-8 항목 실제 구현 완료. `ai_provider.dart`·`ai_credentials_repository.dart`
삭제, `ai_briefing_service.dart`는 `cloud_functions`로 `generateBriefing`
하나만 호출하도록 재작성. `kAiServiceDeployed = false` 플래그로 서버
미배포 상태를 안내 문구("서비스 준비 중")로 정직하게 표시하고, Cloud
Functions 배포 후(P1-7 선행) 이 플래그만 `true`로 바꾸면 된다.
`ai_connection_modal_view.dart`는 API 키 입력 폼 → 기능 소개 + 사용량
한도(하루 10회/월 100회) 안내 화면으로 전면 재작성. Android(SM F966N
실기기) + iPhone(릴리스 빌드) 양쪽 확인 완료.

**iOS 디버그 빌드 크래시 원인 발견(중요, 재사용 가능한 지식)**: Flutter
디버그 빌드는 `flutter run`/Xcode 같은 툴링이 실제로 붙어있지 않으면
홈 화면(또는 `devicectl device process launch`)에서 실행하는 즉시
`signal 11`로 크래시한다("Cannot create a FlutterEngine instance in debug
mode without Flutter tooling or Xcode"). 실기기 확인을 위해 devicectl로
직접 install/launch할 때는 **반드시 release 빌드**(`flutter build ios
--release`)를 써야 한다. 이번에 이 문제로 몇 차례 "화면이 이상하게
나온다"는 오진이 있었는데, 사실은 훨씬 이전 세션에서 Xcode로 제대로
띄워둔 옛날 프로세스가 백그라운드에 남아있던 걸 보고 있었던 것 — 앱을
완전히 지우고 나서야 진짜 크래시가 드러났다.

### 주변 인맥 화면 소소한 개선

`radar_view.dart`의 대표 카드 버튼 라벨을 "상세보기" → **"AI 대화
가이드"**로 변경(실제로는 항상 `openBriefing`으로 연결되는데 라벨이
목적을 안 드러냈음). "가까운 인맥" 리스트 항목은 원래부터 동일하게
연결돼 있어 추가 수정 불필요했음.

### Gmail 가져오기 — 계정 전환 버그 수정 (커밋 `c15fe09`)

`EmailSyncService._currentAccount`가 한 번 로그인하면 계속 유지돼서,
사용자가 다른 Google 계정으로 다시 로그인하고 싶어도 계정 선택 화면이
다시 뜨지 않는 버그 발견. "다른 계정으로 로그인" 버튼 추가 — `signOut()`
후 재인증하는, `google_sign_in` 패키지 공식 문서가 권장하는 패턴 그대로.

### 공지사항 · 1:1 문의 · 관리자 콘솔 신설

사용자 요청("관리 페이지도 만들어서 개인정보나 각종 법적고지부분을
수정 갱신하도록 하고, 1:1 문의와 공지사항도 만들어서 관리 될 수 있게
해야해")에 따라 신규 구축.

- **Firestore 스키마**: `notices/{id}`(공지, published 필터), `inquiries/
  {id}`(+ `replies` 서브컬렉션), `legalDocs/{slug}`(법적 문서 본문).
  관리자 판별은 Blaze 미전환 상태라 커스텀 클레임을 못 심어주는 대신
  `admins/{uid}` 화이트리스트 문서(클라이언트 직접 접근 불가, Firebase
  콘솔에서만 추가 가능)로 처리 — `notices`를 published 필터 없이
  조회해보는 쿼리가 성공하는지로 간접 판별한다.
- **앱**: 설정 → "지원" 섹션에 공지사항(목록/상세, `flutter_markdown_plus`
  로 렌더링)·1:1 문의(작성 + 답변 스레드) 화면 추가.
- **관리자 콘솔**(`docs/admin/`): 빌드 도구 없는 순수 HTML/JS +
  Firebase JS SDK(CDN 모듈). 로그인 + 공지사항 CRUD + 문의 답변 + 법적
  문서 편집. 브라우저에서 로그인 폼·에러 처리까지 확인했으나, **이
  프로젝트에 Firebase 웹 앱이 등록된 적이 없어**(Android/iOS만 있었음)
  `admin.js`의 `firebaseConfig`가 아직 `"REPLACE_ME"` placeholder다.
  배포 전 필요한 일(관리자 계정 생성, `admins/{uid}` 수동 추가 포함)은
  `docs/admin/README.md`에 정리해 뒀다.
- **의도적으로 안 한 것**: 기존 법적 문서 5종의 실제 문안을 Firestore로
  옮기는 작업(P1-15의 연장)은 보류했다. `privacy-policy.html`을 다른
  세션이 바로 그 시점에 좌표 저장 정책 관련해서 실시간으로 고치고
  있었기 때문 — 지금 옮기면 곧 틀리게 될 문안을 그대로 박제하게 된다.
  그 작업(아래 "0-2" 좌표 C안)이 이미 병합됐으니, **다음 세션에서
  진행 가능**.

### 타겟 시장 문서 보강

`docs/planning/business/pnl-analysis-freemium.html`의 타겟 시장 섹션에
"관계 유지가 곧 매출"인 대표 영업직군으로 보험설계사(71.2만 명,
2025년 말 생명보험협회 집계)·자동차 영업사원(약 3만 명, 업계 추정)을
실제 통계와 함께 추가. 기존 577만 명(경영·회계 사무직+매장판매직)과는
KSCO 분류상 별도 직군이라 실제 핵심 타겟은 더 크다는 점을 명시.

### 동시 작업 충돌과 해결(재사용 가능한 패턴)

**무슨 일이 있었나**: 세션 중반, 같은 작업 디렉토리(`connection-trace-
ai-v2-flutter/`)를 다른 세션이 `feat/geo-privacy-hardening` 브랜치로
체크아웃해 실시간으로 편집하고 있다는 걸 뒤늦게 발견했다. `login_view.
dart`(+53줄), `settings_view.dart`(+95줄)에 내가 안 건드린 변경이
섞여 있었고, 심지어 내가 "오픈소스 라이선스 화면을 한글 안내문으로
감싸겠다"며 수정한 코드가 알고 보니 main에는 없는, **그 세션이 아직
커밋 안 한 기능**(`legal_document_view.dart`)을 수정한 것이었다.

**해결**: `git worktree add`로 별도 디렉토리(`connection-trace-ai-v2-
flutter-work/`)에 `main` 기준 새 브랜치(`feature/admin-console`)를 파서
격리했다. 이후 모든 작업을 그 워크트리에서만 진행해 공유 디렉토리를
전혀 건드리지 않았다. 두 세션 다 작업을 마친 뒤, 두 브랜치가 건드린
파일 목록을 `git diff --name-only`로 비교해 **겹치는 파일 3개**
(`pnl-analysis-freemium.html`, `open_source_notice_view.dart`,
`settings_view.dart`)를 찾고, 각각 내용이 동일한지(→ 자동 병합 확실)
혹은 다른 위치에 추가된 것인지(→ 자동 병합 가능성 높음) 미리 확인한
뒤 병합했다. 실제로 `git merge`가 셋 다 충돌 없이 자동 병합했고,
`flutter analyze`(에러 0) + `flutter test`(39건 전부 통과)로 확인 후
`origin/main`에 push했다.

**교훈**: 같은 저장소를 여러 세션이 동시에 작업할 가능성이 있으면,
시작하자마자 `git worktree`로 격리하는 게 안전하다 — 브랜치만 나누고
디렉토리를 공유하면 `git checkout`이 서로의 작업 트리를 덮어써
사고가 난다.

## 0-2. 2026-08-05 — 법적 고지 문서 체계 정비 + 플랫폼별 현황 재조사

상세 경위는 [`backlog.md`](../backlog.md) 추가 75에 있음. 이번 세션은 **문서
작업만** 했고 앱 코드는 건드리지 않았다.

### 문서 작업 — `docs/legal/`이 1개 → 6개로

리멤버(주식회사 리멤버앤컴퍼니)의 공개 문서 체계를 벤치마크로 조사한 뒤,
우리 앱에 필요한 문서를 확정해 작성했다.

- **`privacy-policy.html` v1.0 → v2.0 전부개정** (14항목 → 19항목).
  가장 중요한 건 **BYOK 서술 제거**다 — 커밋 `ed6b4b7`에서 BYOK가 삭제되고
  서버 프록시로 전환됐는데 방침은 여전히 "이용자가 직접 발급한 API 키는
  회사 서버를 거치지 않습니다", "향후 서버 경유로 전환할 계획"이라고 적혀
  있었다. **개인정보 전송 경로를 사실과 다르게 고지한 상태**였고, Apple
  심사 5.1.1 / Play 사용자 데이터 정책 위반 소지였다. 그 밖에 국외 이전·
  자동수집장치·행태정보·개인위치정보·가명정보·권익침해 구제 6개 항목을
  신설하고, 위탁 표를 Firebase 1행 → 4행(Gemini / OS 지오코딩 / 카카오
  우편번호 추가)으로 확대했다.
- **`terms-of-service.html` 신규**(19개조). 결제가 없어도 필요한 이유는
  결제가 아니라 **이용자가 제3자 명함 정보를 입력하는 구조**(제8조)다.
- **`app-permissions.html` 신규**. 정보통신망법 §22-2. 위치·카메라·사진
  3종을 전부 "선택" 권한으로 고지.
- **`account-deletion.html` 신규 — 조사 중 발견한 누락 블로커.** Google
  Play는 앱 내 삭제 기능과 **별개로 계정 삭제 안내 웹 URL**을 요구하는데
  지금껏 어느 문서에도 없었다. 없으면 Play "앱 콘텐츠" 제출이 막힌다.
- **`index.html`, `legal.css` 신규**. 인덱스 + 사업자 정보 푸터, 공통 스타일.
- **`firebase.json`에 `hosting` 섹션 추가**. `public: "docs/legal"`로 지정한
  이유는 `public: "docs"`로 하면 `docs/planning/**` 기획 문서가 전부 웹에
  노출되기 때문. Hosting은 **Spark(무료) 요금제로 가능**해서 Blaze(P1-7)
  대기 없이 배포할 수 있다.

**⚠️ 게시 순서 제약**: 방침은 아래 C안 기준으로 "좌표를 서버에 보관하지
않는다"고 쓰여 있다. **코드는 2026-08-05에 반영 완료**(추가 76)됐지만
**실기기에서 마이그레이션이 실제로 도는지는 확인되지 않았다** — 게시 전에
Firestore 문서를 직접 열어 좌표가 남아 있지 않은지 확인할 것(각 HTML 상단에
화면 미표시 주석으로 박아 둠). 같은 이유로
"AI 모델 학습에 사용되지 않습니다"류 문장은 넣지 않았다 — Gemini 무료 등급은
입력을 서비스 개선에 활용하므로 유료 등급 결제(P1-7) 전에는 못 쓴다.

### 사용자 결정 4건

1. **좌표 C안 확정** — 명함 주소를 변환한 좌표를 서버 백업에서 제외한다.
2. **Firebase Hosting**에 배포.
3. **보호책임자 = 최우진(대표이사) + `connectionsense@creamhouse.net`**.
4. **위치기반서비스 이용약관은 만들지 않는다** — 방침의 "개인위치정보의
   처리" 항목으로 커버.

### 좌표 C안 — 구현 완료 (추가 76, 2026-08-05)

**핵심 착안점: 좌표는 독립적인 데이터가 아니라 주소에서 파생되는 값이다.**
따라서 백업 대상이 아니라 **필요할 때 다시 계산하면 되는 값**이다. 재계산
경로는 명함을 처음 등록할 때 쓰는 것과 동일하다
(`AddressGeocodingService.validateAndConvert`).

기기 변경·계정 재연결 시 흐름: 서버에서 복원 → 주소는 있고 좌표만 빈 상태 →
`GeoBackfillService`가 주소를 지오코딩해 채움 → **기기에만** 저장(암호화된
`saved_contacts_v2`). 기기당 1회.

- `ContactModel.toJson()`(기기용, 좌표 포함) / **`toBackupJson()`(서버용,
  좌표 제외)** 분리. 서버 백업에는 반드시 후자를 쓸 것.
- 이미 서버에 올라간 문서는 좌표가 **암호문 안에** 있어 필드만 지울 수 없다 →
  로그인 시 `rebackupAllContacts()`로 통째로 덮어써 걷어낸다(계정당 1회,
  플래그 `geo_stripped_from_backup_v1_<uid>`).
- `GeoBackfillService`가 막는 실패 모드: 지오코더 몰아치기(순차+400ms), 대량
  처리(1회 30건 상한), 오프라인에서 타임아웃 낭비(연속 3건 실패 시 중단),
  못 찾는 주소 무한 재시도(3회 후 포기, **주소가 바뀌면 초기화**), 시도 기록에
  주소 평문 저장(SHA-256 해시 12자만).
- 복원 직후 화면이 "주변에 인맥 없음"으로 보이지 않도록 레이더에 진행 안내
  카드를 추가했다.

**검증**: 단위 테스트(`test/geo_backfill_test.dart` 10건,
`test/fresh_install_test.dart` 6건), `flutter analyze` 새 이슈 0건,
`flutter test` 37건 통과. **양쪽 실기기 검증 완료** —
- **iPhone 16 Pro**(추가 77·78): 마이그레이션, 복원, 기기 변경 시나리오(키를
  Firestore에서 내려받기)까지 화면으로 확인.
- **갤럭시 Z 폴드**(추가 79): `adb run-as`로 기기 저장소를 열어 암호문을
  복호화, **같은 명함이 기기에는 좌표가 있고 서버에는 없다**는 것을 양쪽
  저장소에서 직접 확인. 이 과정에서 **재시도가 사실상 죽어 있던 결함**을
  발견·수정했다(복원 경로에서만 호출 → 로그인할 때마다 이어서 처리).

**서버 문서를 자동으로 검증하는 법**(재사용): Firebase CLI가 로그인돼 있으면
`~/.config/configstore/firebase-tools.json`의 refresh token을 액세스 토큰으로
교환해 Firestore REST를 관리자 권한으로 읽을 수 있다. 문서는
`users/{uid}.encryptionKeyB64`로 AES-256-GCM 복호화한다(base64(nonce 12B +
암호문 + MAC 16B)). 개인정보 값은 출력하지 말고 `lat`/`lng` 키 존재 여부만
볼 것. 상세는 추가 77.

**⚠️ 이번 검증에서 드러난 것 3가지**(전부 P1로 등록):
- 서버에 **명함 개인정보가 평문으로 남아 있었다**(5건 중 3건). 추가 72의
  암호화는 "다음에 저장될 때" 재암호화되는 구조라 한 번도 다시 저장되지 않은
  서버 문서는 계속 평문이었다. 이번 마이그레이션으로 로그인한 계정은 해소.
- **iOS Keychain은 앱을 삭제해도 안 지워진다** → 재설치 후 로그인 세션과
  암호화 키가 남았다. **2026-08-05 해결 완료**(추가 78, `FreshInstallService`).
  이 수정으로 재설치가 곧 새 기기와 같은 조건이 되어, 그동안 미검증이던
  **기기 변경 시 Firestore에서 암호화 키를 내려받는 경로까지 실기기에서
  검증됐다**(앱 삭제 → 재설치 → 로그인 화면 → 재인증 → 명함 복원 + 거리 표시).
- **Apple 로그인은 별도 uid를 만든다**(P1-24).

### 좌표 문제 — 추가 40의 경고를 정정

이 앱에는 성격이 다른 두 좌표가 있는데 그동안 섞여 기록돼 있었다.
**(a) 이용자 실시간 GPS** — 기기 메모리에서 거리 계산만, 저장·전송 없음.
**(b) `ContactModel.geo`** — 명함에 적힌 주소를 지오코딩한 값으로,
`contact_model.dart:112-113`의 `toJson()`에 포함돼 Firestore에 저장 중.
위치정보법 §2 1호는 "특정한 시간에 존재하거나 존재하였던 장소"를 위치정보로
정의하므로 (b)는 시점 요건을 충족하지 않아 해당하지 않을 가능성이 크다.
따라서 **C안의 가치는 "법적 의무 제거"가 아니라 설계 결정과 코드를 일치시켜
"회사는 어떤 좌표도 보유하지 않는다"를 코드로 입증 가능하게 만드는 것**이다.
(최종 판단은 법무 검토 필요.)

### 플랫폼별 개발 현황 — iOS/Android 비교

**⚠️ 먼저 알아야 할 것: Apple 로그인 구현이 전부 커밋되지 않은 작업 트리
상태다.** `sns_auth_provider.dart`, `auth_repository.dart`(124-208행에
`signInWithApple`, 287-332행에 Apple 재인증 + `reauthenticateCurrentProvider`),
`login_view.dart`, `pubspec.yaml`(`sign_in_with_apple: ^8.1.0`),
`project.pbxproj`, 신규 `ios/Runner/Runner.entitlements`. 아래 P0-2와 추가
73의 "`isAvailable`이 여전히 false" 기록은 **이미 낡았다**.

플랫폼별로 다른 것만:

| 항목 | iOS | Android |
|---|---|---|
| Apple 로그인 | ✅ 코드 구현 + entitlements 3개 config 연결 | ⛔ 의도적 미지원(버튼 미렌더) |
| 릴리스 서명 | ✅ 정식 팀 서명(`77L7BH2M2W`) | ❌ **debug 키 사용 중**(`build.gradle.kts:38-40`) |
| 전화 걸기 | ✅ 제약 없음 | ⚠️ `<queries>`에 DIAL 인텐트 누락 → Android 11+ 실패 가능 |
| adaptive icon | ✅ 22개 사이즈셋 | ❌ 레거시 PNG만 |
| 수출규정 키 | ❌ `ITSAppUsesNonExemptEncryption` 누락 | 해당 없음 |
| 스토어 업로드 | ⛔ App Store Connect 403 | ⛔ debug 키라 Play 업로드 불가 |

실기기 검증 격차(여기가 본질):

| 항목 | iOS | Android |
|---|---|---|
| Google 로그인 → 백업/복원 | ✅ | ✅ (`adb`로 저장 내용까지) |
| **Apple 로그인** | ❌ **빌드조차 안 됨** — `Podfile.lock`에 `sign_in_with_apple` pod 없음, `pod install` 미실행 | ✅ 빌드 성공 |
| 명함 등록 전체 플로우 | ❌ 로그인 화면까지만 | ✅ 전 플로우 QA |
| 퍼플 리브랜딩 UI | ❌ 미확인 | ✅ |
| 아이콘/스플래시 | ⚠️ 재빌드만, 육안 미확인 | ✅ 스크린샷 확인 |
| 암호화 실측 | ❌ 근거 없음 | ✅ `adb run-as` |
| Gmail OAuth | ✅ | ❌ 근거 없음 |

**한 줄 요약**: Android는 기능 검증이 앞서고 배포 준비(서명)가 뒤처짐.
iOS는 배포 준비가 앞서고 기능 검증이 크게 뒤처짐 — 특히 **iOS는 Apple
로그인 코드가 들어간 뒤 한 번도 빌드된 적이 없다.**

## 0-1. 2026-08-04 밤 — iOS 배포 트러블슈팅 + 계정/데이터 안전장치 + 암호화

이 세션(위 "0"번 섹션 이후 이어진 대화)에서 실제로 끝낸 것. 상세 에러 원인·
해결 과정은 전부 [`error-notes.md`](../error-notes.md)의 2026-08-04 항목들에
있음 — 같은 증상 재발 시 거기부터 볼 것.

- **iOS Bundle ID 충돌 해결**: `com.connectiontrace.connectionTraceAiFlutter`가
  예전 개인 Apple ID(무료 Personal Team)에 선점돼 있어 회사 계정(CreamHouse
  Co., Apple Developer Program 유료 가입 완료)으로 서명이 안 되던 문제.
  `com.creamhouse.connectionsense`로 변경, Firebase에 새 iOS 앱 재등록,
  `GoogleService-Info.plist`/`firebase_options.dart`/`Info.plist`의
  `GIDClientID`·URL 스킴 전부 동기화. Android는 영향 없음(패키지명 그대로).
- **Firebase iOS 15.0 배포타겟 충돌 해결(Flutter 툴체인 자체 버그)**:
  `FlutterGeneratedPluginSwiftPackage`가 iOS 13.0으로 하드코딩 생성돼
  Firebase 요구사항(15.0+)과 항상 충돌 — 프로젝트 설정으론 못 고치는 SDK
  버그(Flutter 3.44.8). `pubspec.yaml`에 `flutter.config.enable-swift-
  package-manager: false`로 이 프로젝트만 SPM을 끄고 기존 CocoaPods
  경로로 전환해서 해결.
- **iOS/Android 실기기 전체 파이프라인 재검증 완료**: Google 로그인 → Firebase
  Auth → Firestore 백업 → 로그아웃/재로그인 → 서버 복원까지 양쪽 플랫폼
  실기기(iPhone, 갤럭시 Z 폴드)에서 직접 확인. Android는 `adb`로 로컬
  shared_preferences 실제 내용까지 직접 열어서 검증(문정순/더자안 테스트
  명함으로 확인).
- **계정 삭제 기능 구현(추가 71)**: 설정 화면에 위험 표시된 "계정 삭제" 항목.
  Firestore 데이터 먼저 삭제 → Firebase Auth 계정 삭제 → 로컬 초기화 순서.
  "최근 로그인 필요" 에러 시 Google 재인증 후 자동 재시도까지 구현. 에러를
  조용히 삼키지 않고 사용자에게 명확히 표시(다른 백업 로직과 다른 부분).
- **다중 계정 전환 안전장치 구현(추가 71)**: 마지막 로그인 uid를 기기에 저장,
  다른 계정으로 로그인했는데 기존 로컬 데이터가 있으면 "유지 vs 교체" 다이얼로그로
  명시적 선택 요구(강제 선택, 바깥 탭으로 안 닫힘). **주의(2026-08-04 재감사에서
  확인)**: 이건 uid별 저장소 키 격리(원래 `server-setup-plan.md` 설계)가 아니라
  다이얼로그 확인 절차로 완화하는 방식이다 — `contacts_repository.dart`의
  로컬 저장 키(`saved_contacts_v2`)는 여전히 전역 키. 기능은 동작하지만 원
  설계보다 가벼운 안전장치이므로 "3. 해야 할 일"에 QA 스트레스 테스트 항목으로
  남겨둠.
- **개인정보처리방침 작성·게시(추가 72 이전)**: 코드 전수 조사 기반으로 작성
  (`docs/legal/privacy-policy.html`) — 통화/SMS/카카오톡은 자동 수집 안 함
  (수동 입력만), Gmail은 제목+미리보기까지 수집, 명함 사진은 서버 미전송 등
  실제 구현과 정확히 일치하도록 작성. 사업자등록증 기준 회사 정보(등록번호
  220-86-89511, 대표 최우진, 서울 영등포구 양평로21가길 19) 반영 완료.
  **아직 임시**: 개인정보 보호책임자 담당자·전용 문의메일(현재 대표자/
  creamhouseapp@gmail.com로 임시 지정, 문서 안에 명시돼 있음).
- **명함/프로필 로컬+서버 AES-256-GCM 암호화 적용(추가 72)**: 실기기에서
  `adb run-as`로 명함 데이터가 평문으로 그대로 읽히는 걸 발견(문정순 님
  전화번호·이메일·주소가 그대로 노출) → 로컬(shared_preferences)과
  서버(Firestore) 양쪽 다 암호화하도록 전환. 키는 계정(uid)당 1개, 기기
  Keystore/Keychain(`flutter_secure_storage`)에 캐시 + Firestore
  `users/{uid}.encryptionKeyB64`에도 보관(기기 변경 시 복원 흐름 유지 위함).
  **한계(의도된 설계, 과장 금지)**: 키가 Firestore 안에 데이터와 함께 있어서
  완전한 제로-지식 암호화는 아님 — 기기 분실/로컬 유출/백업 파일 유출/DB
  일부 유출은 방어되지만, Firestore 프로젝트 전체 접근 권한이 있는 사람은
  이론상 키+암호문 둘 다 볼 수 있음. 진짜 키 분리(Cloud Functions/KMS)는
  Blaze 인프라(아래 "해야 할 일" 참고)가 갖춰진 뒤 가능. 레거시 평문 데이터는
  로그인 시 자동으로 감지돼 투명하게 재암호화됨(데이터 유실 없음, 기존
  "문정순" 테스트 명함으로 마이그레이션 확인 완료). 명함 원본 사진(JPG)
  암호화는 이번 범위 밖(별도 백로그 항목).
- **Android 실기기 정식 테스트 완료(추가 71)**: 갤럭시 Z 폴드(SM F966N,
  Android 16)에 debug APK 빌드·설치·실행, Flutter/Dart 크래시 없음 확인.
  (참고: 이 기기는 폴더블이라 `adb input tap`으로 화면 내 버튼 자동 클릭이
  좌표계 어긋남 때문에 잘 안 됨 — 화면별 상세 동작 확인은 실제로 폰을 쥐고
  하는 게 정확함.)
- **미해결 — 다음 세션에서 이어갈 것**: **App Store Connect 빌드 업로드
  403 권한 에러**. Xcode에 로그인된 계정은 `apps@creamhouse.net`(choi
  woojin, CreamHouse Co. 팀, Developer Portal에서는 Admin 역할 확인됨).
  앱 레코드 생성/조회는 성공하는데 실제 빌드 업로드(`CREATE BUILD` API)만
  `FORBIDDEN_ERROR.ROLE_NOT_VALID`로 거부됨 — Developer Portal 권한과
  App Store Connect 권한은 별개 시스템이라, App Store Connect 웹사이트의
  "사용자 및 접근"에서 이 이메일 계정 자체의 역할을 확인해야 함(목록에
  없으면 신규 초대 필요). 아래 "해야 할 일" P0-1 참고.

## 0. 2026-08-03 출시 기준 재설계

- 확정 UI: 흰 배경, 블루 주색, 라운드 카드, Material 3 하단 3탭
  `주변 / 명함 / 설정`. 가짜 인물·가짜 알림·미동작 토글은 출시
  화면에서 제거했다.
- 위치 상태 구조는 `LocationConsentStore`(앱 동의) →
  `LocationGateway`(OS 권한/GPS) → `RadarViewModel`(상태) → 화면으로
  분리했다. 앱 동의 전에는 OS 권한 팝업을 요청하지 않는다.
- 강남역 fallback은 사용자 위치와 명함 주소 모두에서 제거했다.
  실제 좌표가 없으면 거리를 계산하지 않는다.
- 좌표는 서버로 전송·저장하지 않고 사용 중 메모리에서만 거리
  계산에 쓴다. 이 단말 내 처리 구조를 유지하는 한 위치정보지원센터
  안내상 LBS 사업 신고 제외. 서버 위치 전송을 추가하면 재판단해야 한다.
- 출시 Android 명단에서 통화기록·문자·전화 상태·마이크·전체
  저장소 권한을 제거했다. `call_log`/`flutter_sms_inbox`/
  `permission_handler` 의존성도 제거했고, 기존 실험 서비스는 항상
  비활성인 안전 스텁이며 생산 UI 진입도 차단했다.
- AI 브리핑은 화면 진입 시 자동 호출하지 않는다. 사용자가 전송할
  기본정보/메모/소통기록을 보고 기록별로 선택한 뒤, 이번 요청 전송에
  명시적으로 동의해야만 Claude/OpenAI/Gemini 호출 경계로 넘어간다.
- 소통자료 입력은 Gmail 공식 OAuth 가져오기, 통화 후 메모, 문자 붙여넣기,
  카카오톡 붙여넣기로 구성. Gmail도 조회 즉시 저장하지 않고 사용자가
  선택한 메일만 기기에 저장한다.
- 검증: 위치 회귀 테스트 5건 통과, Android APK 빌드/권한 감사
  통과, iOS 실기기 빌드·설치·실행 성공. Android 실기기는 현재
  ADB에 보이지 않아 설치 QA가 보류된 상태다.
