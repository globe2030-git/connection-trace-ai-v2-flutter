# 이어서 하기 — 복귀하면 이 문서부터

마지막 갱신: **2026-09-05** — 09-05 병합분 27건까지 반영. 이전 갱신은 2026-09-04였다.
기준 커밋: `main` `339c5950` (#851) — 이 문서를 열기 전에 아래 "0. 먼저 3분"을 하면 최신인지 알 수 있다.

> 🚨 **09-05 에 `origin/main` 이 27번 움직였다.** 그런데
> `HANDOFF.md` 는 **한 줄도 안 바뀌었다**([추가 668] 이 경고한 자리의 두 번째 재현).
> 📌 **그래서 이 문서와 HANDOFF 를 같은 커밋에서 함께 고쳤다** — 한쪽만 고치면
> 다음 사람이 읽는 쪽이 낡은 쪽일 수 있다.
>
> ⚠️ **세는 법에서 미끄러질 뻔한 것을 함께 적어 둔다.**
>
> ```
> git log --since=2026-09-05          →   0 건   ← 날짜만 주면 빈 결과가 나온다
> git log --since='2026-09-05 00:00'  →  27 건   ← 잰 시각 22:30. 22:10 에는 24였다
> ```
>
> **0은 「없다」처럼 보이지 「못 봤다」처럼 보이지 않는다.** 두 가지 이상으로 세라.
>
> ⭐ **이 문단을 쓰면서 한 번 더 미끄러졌다 — 적어 둔다.** 처음에 *"이 저장소가
> 하루에 제일 많이 움직인 날"* 이라고 썼다. **재보니 아니었다** — 08-26 이 124건,
> 08-23 이 110건이다(`git log --pretty=%ad --date=short origin/main | sort | uniq -c`).
> 📌 **최상급은 재지 않으면 쓰지 마라.** 24 라는 **잰 숫자** 옆에 붙어 있어서
> 그것까지 잰 것처럼 보였다 — **숫자 하나를 재면 그 문장 전체를 잰 것 같아진다.**

> 🚨 **2026-09-04에 이 문서가 여드레 낡아 있었다 — 무엇이 틀렸었는지 남긴다.**
>
> ```
> §2 「꺼져 있는 것들」   명함 사진 백업이 「꺼짐」으로 적혀 있었다 → 08-26에 켜졌다
>                        방침 v2.2가 「미게시」로 적혀 있었다      → 08-26에 게시됐다
> §3 「켜는 순서」        ①~④가 할 일로 서 있었다                 → 전부 끝났다
> §4 「다음 backlog 번호」 388 이라고 적혀 있었다                   → 지금은 673
> §4 세션 분담           2026-08-21 기준이라 지금과 다르다
> ```
>
> ⚠️ **더 나빴던 것은 「같은 문서 안에서 어긋난 것」이다** — §0-0은 *"08-26에
> 사진 백업이 켜졌다"* 라고 옳게 적고 있었는데 **§2는 「꺼짐」이었다.** 복귀
> 직후 §2만 본 사람은 **켜진 것을 꺼진 줄 안다.** [추가 668] 이 HANDOFF 에서
> 겪은 것과 **같은 모양**이다(87행은 고쳐졌는데 121행은 안 고쳐졌다).
>
> 📌 **그래서 이 문서를 고칠 때는 「한 곳만」 고치지 마라** — 같은 사실이
> 몇 군데에 적혀 있는지 `grep` 으로 먼저 세고 시작한다.

> **왜 이 문서가 따로 있나**: [`HANDOFF.md`](./HANDOFF.md)는 1,000줄이 넘어
> "지금 무엇부터"가 안 보인다. 이 문서는 **복귀 직후 15분** 동안 볼 것만 담는다.
> 배경·경위가 궁금하면 그때 HANDOFF와 [`backlog.md`](./backlog.md)로 간다.

---

## 0. 먼저 3분 — 상태 확인

```bash
cd /Volumes/Work/Claude/connection-trace-ai-v2-flutter
git fetch origin
git log --oneline origin/main -1          # main이 얼마나 나갔나
git status -sb                            # 내가 어느 브랜치인가
git rev-list --count HEAD..origin/main    # 내 작업 트리가 몇 커밋 뒤처졌나
gh pr list --state open --limit 900        # 남의 세션이 올려둔 것이 있나
                                           # 🚨 --limit 없으면 기본 30만 본다(추가 667)
```

⚠️ **뒤처짐을 반드시 재라.** 2026-08-15 아침에 본체 워크트리가 main보다
**103커밋 뒤**에 있었고, 모르고 그 위에서 작업할 뻔했다. 낡은 브랜치에서
시작하면 이미 고쳐진 것을 다시 고치게 된다.

---

## 0-0. 🚨 세션이 사라졌다면 — **여기부터 15줄만**

```
지금 무엇부터   HANDOFF.md 「2026-09-04에 할 일」  ← 빌드 하나로 여덟 + 671 재확인
어제 무슨 일     backlog 추가 670·671 (실기기에서 결함 둘)
재는 법          tool/README.md — gsutil · dumpsys · uiautomator
```

**2026-09-05 22:10 실측 (저장소만 — 아래 ⚠️ 를 함께 볼 것):**

```
main         339c5950 (#851) · 열린 PR **3건** (#845 #850 #852) — 22:10 에는 4건이었다
             셈법: gh pr list --state open --limit 900 --json number,title,statusCheckRollup
             🚨 --limit 없으면 기본 30만 본다([추가 667])
방침 파일     저장소 헤더는 버전 2.6 · 공고 2026-08-31 · 시행 2026-09-02  (그대로)
v2.7 초안     docs/legal-drafts/privacy-policy-v2.7-draft.html (1,206줄)
             📌 파일 머리에 **게시 절차 5단계**가 적혀 있다 — 게시 전에 그것부터 읽을 것
사진 백업     kCardPhotoBackupEnabled = true  (card_photo_backup_service.dart:60)
관리자 스위치  adminSessionRequired() { return false; }  (firestore.rules:27)
다음 backlog  692   ⚠️ 688~691 은 이미 배정됐다 — §4 끝의 표를 볼 것
```

> 📌 **옛 값(2026-09-04): `main e5c9adf` · 열린 PR 0건 · 다음 backlog 673.**
> 하루 만에 `main` 이 **24커밋** 나갔고 번호는 **19개** 늘었다.
> ⚠️ **이 블록은 매일 낡는다 — 여기 적힌 값은 언제나 「그 시각의 것」이다.**

🚨 **아래 셋은 이 자리에서 못 쟀다 — 옛 값을 지웠지 새 값을 넣은 것이 아니다.**

```
기기          폴드 연결 여부 · 누가 잡고 있는지 · 어느 빌드가 깔렸는지
서버          사진 건수 · 명함 수 · config/billing.model 유무
세션 분담      지금 누가 무엇을 잡고 있는지
```

📌 **옛 값(*"폴드 연결됨 · 서버 사진 102건"*)은 2026-08-27 것이라 지웠다.**
⚠️ **낡은 실측값은 「없는 것」보다 나쁘다** — 없으면 재지만, 있으면 그대로 믿는다.
**필요하면 그 자리에서 다시 재고, 잰 날짜와 함께 여기 적는다.**

### 🚨 08-26에 켜진 것 — 이 전제로 일해야 한다

```
🔵 명함 사진 서버 백업     켜짐. 기존 102장 소급 업로드 완료
🔵 명함 그룹 기능          켜짐 (기본값 true)
🔵 사진 한도               무료/충전 구분 없이 **2,000장 하나**
🔵 방침                    게시됨. 공고 8/26 · **시행 2026-09-02** (v2.1~v2.4)
🔵 배포                    firestore.rules · hosting:legal/site · storage.rules
🔒 지갑(충전형)            여전히 꺼짐 — config/billing.model 필드 없음
```

⚠️ **낡은 문서에 「사진 백업 꺼짐」·「시행 9/15」가 보이면 그쪽이 낡은 것이다.**
시행일의 유일한 출처는 `docs/legal/privacy-policy.html` 헤더다.

> **2026-09-04 갱신 — 위 목록에 그 뒤로 더해진 것**
>
> ```
> 🔵 방침 파일    저장소 헤더가 v2.6 (공고 8/31 · 시행 9/2) 로 올라갔다
> 🔵 규칙 배포    firestore:rules 09-03 09:11 (필수 동의 3필드 화이트리스트)
> 🔵 P1-17       필수 동의가 서버에 남는다 — 09-03 실기기 확인 완료
> 🔒 번호 확인    여전히 꺼짐 — config/phoneVerification 문서 자체가 없다
> 🔒 지갑         여전히 꺼짐 — config/billing.model 필드 없음 (그대로)
> ```
>
> 🚨 **꺼진 것 둘의 성질이 같다** — 값을 바꾸는 게 아니라 **없는 것을 만드는 것**이
> 스위치다. **둘 다 globe2030님 결정**이고, 번호 확인은 잘못 만들면
> **테스터 전원이 인증 화면에 갇힌다**([추가 645]).

> **2026-09-05 갱신 — 그 뒤로 더해진 것**
>
> ```
> 🔵 규칙 배포    firestore:rules 09-05 21:5x 경 — cardSources(#833) 때문에 나갔고
>                #835·#836 이 함께 나갔다 (§3-9 에 무엇이 함께 나갔는지 적어 뒀다)
> 🔵 Xcode Cloud  Archive - iOS 가 처음 통과했다 (09-05 11:00, 9cc3f612)
>                원인은 cmake 였다 — [추가 677]
> 🔵 주석 정정    사진 백업이 「꺼져 있다」던 코드 주석 둘이 닫혔다 (#844, [추가 687])
> 🔒 관리자 2차 인증  코드는 2단계까지 끝났고 **스위치는 꺼져 있다**
>                firestore.rules:27 `adminSessionRequired() { return false; }`
> 🔒 번호 확인    여전히 꺼짐 — config/phoneVerification 문서 자체가 없다 (그대로)
> 🔒 지갑         여전히 꺼짐 — config/billing.model 필드 없음 (그대로)
> ```
>
> 🚨 **꺼진 것이 셋으로 늘었다.** 관리자 2차 인증의 스위치는 앞의 둘과 성질이
> 다르다 — **없는 것을 만드는 것이 아니라 코드 한 줄(`false` → `true`)** 이고,
> **설계 4단계(콘솔 인증 화면) 배포와 같은 배포**여야 한다. 먼저 켜면
> **인증할 화면이 없어 관리자가 아무것도 못 한다.**

✅ **코드 주석 둘이 「꺼짐」이라 말하던 것 — 09-05 에 둘 다 닫혔다** (#844, [추가 687])

```
실물   card_photo_backup_service.dart:60   kCardPhotoBackupEnabled = true
✅ 닫힘  auth_gate.dart                     #822 에서 과거형 경위로 고쳐졌다
✅ 닫힘  contact_image_service.dart         #844 에서 사실로 고쳐졌다 (09-05)
✅ 닫힘  card_photo_backup_service.dart     같은 파일이 자기 자신과 모순돼 있었다
                                            (28~30행 "기본은 꺼져 있다" vs 60행 = true)
```

📌 **한 파일이 자기 자신과 모순돼 있던 것이 제일 잘 속였다** — 위를 읽고 아래를
안 읽는다([추가 668] 의 HANDOFF 87행·121행과 같은 모양이다).

🚨 **그런데 그 주석을 고치다 결함이 하나 나왔다 — 아래 §5 「확인이 필요한 낡은 기록」①.**

⭐ **`auth_gate` 쪽이 값나가는 자리였다.** 그 문장은 **계정 전환 시 옛 사진을
안 지우는 판단의 근거**였고, 백업이 켜지면서 **근거가 거짓이 됐다.** 주석
자신이 *"켤 때 이 판단을 다시 봐야 한다"* 고 예고해 뒀는데 아흐레 동안
아무도 안 봤다. **09-04 에 그 판단을 다시 봐서 나온 것이 30일 뒤 삭제(#822)다** —
주석만 고친 것이 아니라 **판단을 먼저 다시 본** 것이 맞는 순서였다.

📌 **`contact_image_service` 쪽은 결이 달랐다** — 판단의 근거가 아니라 방침 서술이라
급하지는 않았지만, **읽는 사람에게 「사진이 서버에 안 올라간다」로 읽혔다.**
개인정보가 걸린 자리에서 가장 나쁜 종류라 09-05 에 함께 닫았다.

⭐ **고칠 때 결론만 바꾸지 않았다** — *"왜 꺼 뒀었나"* 와 *"다시 끌 때의 순서"* 를
남겼다. **켜졌다고 그 이유가 없어지는 것이 아니다.**

### 🚨 08-26에 비싸게 배운 것 셋 — 안 읽으면 같은 자리에서 넘어진다

**① 하나를 고쳐야 다음 것이 보인다** (하루에 세 번 났다)

```
517 서버 삭제가 규칙에 막혀 있었다  → 고치니  518 장부가 안 줄어드는 것이 보였다
511 테스트가 바인딩을 안 켰다       → 고치니  510 로드 경합이 보였다
①   광고 동의 저장 실패             → 고치니  ②   성공 경로가 비어 있던 것이 보였다
```

**앞의 것이 뒤의 것을 가리고 있었다.** → 하나를 고친 뒤에는 **반드시 다시 재라.**

**② 배포가 방어막을 걷는다** (추가 514)

`firestore.rules`를 배포하자 그 전까지 저장을 막아 주던 조건이 사라졌는데
아무도 계산에 안 넣었고, 같은 날 자동 탭 사고가 두 번째로 났다.
→ **배포할 때 「무엇이 더 이상 막히지 않게 되나」를 물어라.**

**③ 본 사람이 본 대로 적어야 정확하다**

세션 둘이 같은 것을 기록할 때, **자기가 본 것만 적고 남이 본 것은 안 옮긴
쪽**이 결과적으로 더 정확했다 — 옮겨 적으면 로그 원문과 *"이건 안 쟀다"*가
빠지고 출처만 흐려진다. 📌 **쪽지로 들은 것을 자기 관찰처럼 적지 마라.**

🚨 **옮기고 싶어지는 때가 언제인지 알아 둬라 — 남이 본 것이 값져 보일 때다.**
그 관찰이 중요해 보일수록 *"사라지기 전에 내가 대신 적어 둘까"* 가 된다.
**값져 보일수록 옮기고 싶어지고, 옮길수록 많이 빠진다.** 대신 적지 말고
**본 사람에게 적으라고 하라.** 그 사람이 사라질 것 같으면 **그것부터 시켜라**
— 실제로 2026-08-27에 한 세션이 그렇게 적어 두고 몇 시간 뒤 사라졌고,
남긴 것은 온전했다.

### ⚠️ 실기기 함정 (08-26 실측)

```
명함 상세의 전화 아이콘   시트 없이 **바로 건다** (목록·주변 화면 것만 시트)
설정의 백업 숫자          앱 시작 시점 값이다 — 재시작해야 맞는다 (추가 513)
빈 gsutil 출력            "없다"와 "권한이 없어 안 보인다"는 화면이 같다
개수 판정                 개수는 같은데 내용이 다를 수 있다 — 목록(해시)으로 봐라
gh run rerun              옛 병합 지점을 그대로 다시 쓴다 — main을 고쳐도 안 바뀐다
android/key.properties    본체에만 있다. 워크트리에서 릴리스를 빌드하면
                          **경고 없이 debug 서명**이 나오고 덮어 설치가 거부된다
```

🚨 **되돌릴 수 없는 조작(삭제·탈퇴·결제·동의)은 사용자에게 부탁한다.**
그리고 **자동 탭 전에 화면을 먼저 확인한다**(CLAUDE.md 4-2, 08-26 신설).

---

## 0-1. 자리를 비운 사이 바뀐 것 (2026-08-21 하루 결산 — 밤 정리)

**병합된 것** (전부 CI 통과, 경위는 backlog 추가 369~387):

```
#386  카카오·네이버 로그인 (커스텀 토큰·웹뷰, 차단급 리뷰 3건 반영)
#389  공식 로그인 버튼 + 설정 이메일 가리기
#396  버튼 크기 교정 + B안(소셜 로그인 테스터의 AI 차단 해소)
#399  377 계정 전환 서버 유출 차단 (선택 전 서버 쓰기 금지)
#401  행안부 주소 판정(규칙 A·82.2%) + 주변 화면 지역 구획(63명 되살림)
#405  ⑤ 명함 상세 시트 (전화 띠 제거·줄별 연락 아이콘·닫기/편집 통일)
#402  backlog 7건 기록 · 문서 #390~#404 (법무 질문 6~9 회신·매트릭스·브리프 등)
대기   #406 ⑦-지갑 상단 고정 · #407 383 촬영 가이드 플랫폼 분기 — CI 통과 시 병합
```

**서버 실물로 확인된 것**: B안 배포 후 카카오·네이버 계정의 AI 호출 통과(로그
실측), 계정 전환으로 생긴 **이중 사본 190건 정리 완료**(겹침 0, 주 계정 무변화).

**수치 정정**: 행안부 매칭 기준선 67.7%는 대조 규칙의 성능이었다 — 올바른
측정은 **82.2%**(기기 32.3% 대비 2.5배). 재채점·372 스펙은 이 기준으로 다시 잰다.

**확정된 설계**(목업 캔버스 "P2 기능개선 화면설계"): ⑤ 상세 시트(병합됨) ·
⑥-C 홈 지도 카드(그림+실데이터, A안 미채택) · ⑦ 목록 상단 고정(지갑 구현
완료 #406, 주변은 다음). **P2 4건 목업은 사용자 확정 대기.**

**법무**: 질문 6~9 회신 완료(계정 식별·계정 전환), `legal-researcher`
서브에이전트 신설(`/legal-researcher`), account-deletion.html 계정 기준 정정
(게시 대기).

---

## 1. ⭐ 사용자가 직접 해야 할 것 — 이것 하나가 여러 개를 막고 있다

### 법무 검토 의뢰 — ✅ 1~3차 전부 회신 완료

> 🚨 **2026-09-04 갱신 — 아래 절은 08-21 기준이고, 그 안의 표는 더 낡았다.**
> 표에 *"⬜ 아직 안 보냄 (2026-08-15 기준)"* 이라고 적혀 있는데 **바로 위
> 문단이 「발송·회신까지 끝났다」고 말한다** — 한 절 안에서 서로 어긋난다.
> **표는 「질문 5개가 무엇이었나」 참고용으로만 읽어라.**
>
> ```
> 1차 (질문 1~5)   ✅ 2026-08-15 발송·회신
> 2차·3차 (6~9)    ✅ 2026-08-21 회신
> 그 뒤            ✅ 08-25 · 08-31 회신 (legal-review-reply-2026-08-25/31.md)
> 남은 것          ⬜ **변호사 확인** — 법무 검토는 변호사 의견서가 아니다
> ```

### (아래는 08-21 기준 원문) 1차는 발송·회신까지 끝났다 (추가 373)

의뢰서 진행 상태 표 실물 기준: 1차(질문 1~5)는 2026-08-15에 발송·회신 완료
([회신](./legal-review-reply-2026-08-15.md)), **2차(질문 6~8)·3차(질문 9)도
2026-08-21에 회신 완료**([2차·3차 회신](./legal-review-reply-2026-08-21.md)).
**남은 단계는 1차 회신의 문안 반영(굵은 5건, 방침 게시 전 필수)뿐**이다.
아래 원문은 1차 발송 전 기준 — 질문 내용 참고용으로만 볼 것.

| | |
|---|---|
| **무엇** | [`docs/legal/legal-review-request-2026-08-15.md`](./legal-review-request-2026-08-15.md) 를 법무에 전달 |
| **상태** | ⬜ **아직 안 보냄** (2026-08-15 기준) |
| **왜 급한가** | 이 회신이 없으면 **방침 게시 → rules 배포 → 사진 저장 플래그**가 전부 막힌다 |
| **어떻게** | 문서를 그대로 보내면 된다. 질문 5개에 *사실관계·우리 판단·확신이 없는 이유·답에 따라 달라지는 것*이 붙어 있어, 변호사가 코드를 안 봐도 답할 수 있게 썼다 |

**묻는 것 5개** (각각 답에 따라 할 일이 달라진다):

1. **고지 기간 7일 vs 30일** — 보존 기간 신설이 "이용자에게 불리한 중대한
   변경"인가. **30일이면 v2.1(명함 필드)을 먼저 분리 게시할지 정해야 한다.**
2. **IAP 구조에서 회사가 전자상거래법상 거래기록 보존 주체인가** —
   ⚠️ 결제 원장이 **지금도 탈퇴 후 남는데 방침에 항목이 없다.** 보존 의무가
   없다면 오히려 **삭제하도록 코드를 고쳐야 한다.**
3. **명함 사진(제3자 이미지)의 계약 이행 범위** — "본인 명함 정보의 확인·보정"을
   계약 이행에 넣는 것이 과한 확장인지.
4. **잔여 충전 이용권의 소멸·환불** — 정해진 바가 없어 문안을 **비워 뒀다.**
5. **기기 해시의 탈퇴 후 보존**(지갑 U5) — 가명정보인지, 고지가 필요한지,
   무기한 보존이 허용되는지.

**회신이 오면 할 일**은 아래 3번 "켜는 순서"를 그대로 따르면 된다.

---

## 2. 지금 "꺼져 있는" 것들 — 의도된 상태다

버그로 오해하지 말 것. 전부 **일부러 닫아 둔 것**이다.

🚨 **2026-09-04에 이 표를 실물과 맞췄다 — 위 다섯 줄 중 넷이 틀려 있었다.**
아래가 지금 값이다. **옛 표는 이 절 끝에 「무엇이 어떻게 틀렸었나」로 남긴다.**

| 무엇 | 지금 | 켜는 조건 | 근거 |
|---|---|---|---|
| 명함 사진 서버 업로드·복원 | ✅ **켜짐** `= true` | (끝남 — 08-26) | `card_photo_backup_service.dart:60` 실물 |
| 설정의 "사진 제공" 동의 토글 | ✅ **보임**(위 플래그에 묶임) | (끝남) | `settings_view.dart:389` |
| `firestore.rules`·`storage.rules` | ✅ **배포됨** | (끝남 — 08-26, 그리고 09-03에 필수 동의 3필드) | [추가 670] 기록 |
| 개인정보처리방침 | ✅ **게시됨** — 저장소 헤더 v2.6 (공고 8/31 · 시행 9/2) | (끝남) | `privacy-policy.html` 헤더 |
| 개인정보처리방침 **v2.7** | ⬜ **게시 안 됨** | **globe2030님 결정** · #779 앱 배포 뒤여야 문안이 참이 된다 | HANDOFF 「결정이 필요한 것」 |
| 번호 확인 게이트 | 🔒 **꺼짐** | `config/phoneVerification` **문서 신설** — globe2030님 | [추가 642·643·645] |
| 지갑/과금 | 🔒 **꺼짐** (코드는 main 병합됨, 2026-08-18 PR #299) | `config/billing.model` **필드 신설** — globe2030님 | CLAUDE.md 6절 |

⚠️ **꺼진 셋의 성질**: 뒤의 둘은 **값을 바꾸는 게 아니라 없는 것을 만드는 것**이
스위치다. **딸려 배포돼도 안 켜진다** — 그래서 배포와 활성화가 별개다.

### 📄 v2.7 초안은 여기 있다 — **`docs/legal-drafts/privacy-policy-v2.7-draft.html`**

1,206줄. [추가 653] 이 게시되는 파일에서 갈라내 옮겨 둔 것이다.
🚨 **파일 머리에 게시 절차 5단계가 적혀 있다 — 게시 전에 그것부터 읽어라.**

```
1. 앱 배포(#779, 범위 판정)가 먼저다 — 법무 6차 회신
      그 전에 게시하면 「기존 회원께는 요구하지 않는다」가 지킬 수 없는 약속이 된다
2. Firestore TTL 정책을 켜고 functions 배포
      그래야 「마지막 발송일부터 30일 파기」가 참이 된다
3. 이 파일의 공고일·시행일을 채운다
      시행일 = 공고일 + 7일 이상, 그리고 9/9 이후(v2.5가 9/9 시행)
4. docs/legal/privacy-policy.html 로 옮긴다
5. firebase deploy --only hosting:legal
```

✅ **법인명 대조는 닫혔다 (2026-09-04)** — 등록증 표기가 **「크림하우스 주식회사」**
(띄어쓰기 있음)로 확인됐고, 저장소 전체를 **등록증과 한 글자까지 같게** 맞췄다.
⚠️ **띄어쓰기가 표기의 일부다** — 붙임(「크림하우스주식회사」)이나 약칭
(「크림하우스(주)」)으로 되돌리지 마라([추가 673]).

⬜ **남은 것은 선임 변호사 확인 하나** — 법무 검토는 변호사 의견서가 아니다.

📌 **날짜가 「(게시 시 확정)」으로 비어 있는 것은 일부러다** — 법무 6차:
*"그럴듯한 날짜를 미리 적으면 게시하는 사람이 고칠 곳을 못 본다."*

> 🚨 **2026-09-04에 이 자리를 한 번 틀리게 적었다 — 경위를 남긴다.**
>
> ```
> 처음 찾은 법   docs/legal/privacy-policy.html 에 grep + docs/planning/legal-drafts/ 를 ls
> 결론           "어디에도 없다"
> 실제           docs/legal-drafts/  ← 폴더가 다르다 (planning/ 아래가 아니다)
> ```
>
> ⚠️ **찾는 말이 아니라 「찾는 곳」이 좁았다.** `legal-drafts` 라는 이름이
> 맞았는데 **경로를 짐작으로 붙였고**, 없다는 결과를 그대로 문서에 적었다.
> 📌 **CLAUDE.md 4절 「「없다」를 grep 으로 말하지 마라」의 사례다** —
> 넓혀서(`grep -rln` 저장소 전체) 다시 세니 **한 번에 나왔다.**

> 📌 **옛 표는 이렇게 틀려 있었다 (2026-08-15 기준, 여드레 방치)**
>
> ```
> 사진 백업        「꺼짐 = false」        → 실제로는 08-26부터 true
> 동의 토글        「화면에 안 보임」      → 보인다
> rules           「미배포」              → 배포됐다
> 방침 v2.2       「main에 있으나 미게시」 → 게시됐고 이미 v2.6까지 갔다
> ```
>
> ⚠️ **그리고 아래 「테스터에게 알릴 것」이 그 표에 딸려 함께 낡아 있었다** —
> *"사진은 아직 기기에만 있다"* 는 **08-26 이후로 거짓**이고, 그대로 읽으면
> **테스터에게 틀린 안내를 하게 된다.** 📌 **낡은 표 하나가 안내 문구까지
> 끌고 낡는다** — 표를 고칠 때 **그 표를 근거로 쓴 문장까지 같이 본다.**

---

## 3. ✅ 법무 회신 후 켜는 순서 — **전부 끝났다 (2026-08-26)**

⚠️ **이 절을 할 일로 읽지 마라.** ①~④가 다 지나갔고, 사진 업로드·복원은 돌고
있다. **남겨 두는 이유는 순서 자체가 다음 개정(v2.7)에서 그대로 다시 쓰이기
때문이다** — 게시 → 고지 기간 → 배포 → 플래그 순서는 바뀌지 않는다.

```
① 법무 회신 (고지 기간 7일/30일 확정)                    ✅ 08-15·08-21·08-25·08-31
      ↓
② 방침 v2.2 게시     firebase deploy --only hosting:legal      ✅ 08-26 게시
      ↓  (고지 기간 경과를 기다린다)
③ rules 배포         firebase deploy --only firestore:rules,storage  ✅ 08-26
      ↓
④ 플래그 켜기        kCardPhotoBackupEnabled = true             ✅ 08-26
      ↓
사진 업로드·복원 동작 + 설정에 동의 토글 나타남               ✅ 돌고 있다
```

🚨 **v2.7 은 이 순서를 처음부터 다시 밟는다** — 다만 ④에 해당하는 것이
플래그가 아니라 **`config/phoneVerification` 문서 신설**이고, **②보다 앞에
#779 앱 배포**가 와야 문안이 참이 된다(순서가 하나 더 끼어든다).

- ②를 건너뛰고 ④를 켜면 **고지 없이 제3자 사진을 수집**하게 된다.
- `test/card_photo_backup_gate_test.dart`가 플래그를 고정하고 있어, ④를 하려면
  그 테스트를 **함께 고쳐야 한다**(실수로 켜지는 것을 막는 장치).
- ⚠️ **`firebase deploy --only hosting`처럼 타겟 없이 배포하면 `legal`과 `admin`이
  둘 다 나간다.** 관리자 콘솔만 배포할 때는 반드시 `--only hosting:admin`.

---

## 3-9. 🟡 관리자 인증 2단계 — **코드는 끝났다. 스위치가 꺼져 있다** (2026-09-05)

> **개발A(터미널) 가 중단한 것을 개발C(터미널) 가 이어받아 마쳤다.**
> 브랜치 `feat/admin-auth-stage2`. globe2030님이 *"관리자 2단계를 마저 끝낸다"*
> 를 직접 고르셨다(아래 「착수 순서」의 물음에 대한 답).
>
> 🚨 **「끝났다」가 「켜졌다」가 아니다.** `firestore.rules` 의
> `adminSessionRequired()` 가 **`false`** 로 있고, 그것을 `true` 로 바꾸는 것이
> **설계 4단계(콘솔 인증 화면)와 같은 배포**여야 한다. 지금 켜면 관리자가
> 아무것도 못 한다 — 인증할 화면이 없기 때문이다.

### 끝난 것 — Callable 셋 (커밋 완료)

```
adminOtpRequest        등록된 번호와 같을 때만 인증번호를 보낸다
adminOtpConfirm        확인되면 adminSessions/{uid} 를 만든다
adminSessionHeartbeat  활동 중임을 알려 유휴 만료를 뒤로 민다

검증   npx tsc --noEmit 통과 · npm test 260건 통과·실패 0 (신규 9건)
```

### ⭐ 설계를 짜다 한 곳을 고쳤다 — 이어받는 사람이 먼저 알아야 한다

설계 §3-2 는 *"만료 계산은 TypeScript 한 곳에만"* 인데, 기존 관리자 조작에도
20분 만료를 걸려면 rules 도 판정해야 한다. 둘 다 하면 **상수가 두 곳에 생긴다**
(ADMIN-VULN-001 과 같은 구조).

```
adminSessionExpiresAt(session) = min(활동+20분, 인증+12시간)   ← TypeScript 가 계산
rules 는  request.time < expiresAt  한 줄로 비교만 한다        ← 숫자를 모른다
```

⭐ **한 필드가 두 만료를 다 담는다.** 20분을 30분으로 바꿔도 rules 는 안 고친다.

### ✅ `firestore.rules` — 셋 다 들어갔다 (다만 **누가·언제**가 갈린다)

```
③ isAdmin() 에 신선도    #835 · 개발C · 09-05    adminSessionRequired() 스위치 뒤 ← 꺼져 있다
① adminSessions/{uid}   #836 · 개발A · 09-05    allow read, write: if false · Admin SDK 만
② config/admins         #836 · 개발A · 09-05    🚨 읽기도 막는다
```

> 🚨 **이 절은 2026-09-05 에 한 번 틀린 채로 서 있었다 — 지우지 않고 남긴다.**
> 원래 *"셋 다 들어갔다 (개발C, 2026-09-05)"* 였는데, 그때 `main` 에 있던 것은
> **③ 뿐**이었다. ①② 는 개발A 의 작업 트리에 **미커밋으로만** 있다가 사라졌고,
> **#836(`beedc294`)에서 처음 커밋**됐다.
>
> 📌 **지금은 결론이 참이다** — 셋 다 `main` 에 있다(`firestore.rules:341` · `:356`).
> **그래서 더 위험했다**: 결론이 맞으면 아무도 출처를 안 의심한다.
>
> ⚠️ **경위는 아래 §「실물 대조」에 있다** — 같은 문서 **80줄 아래**에서
> *"firestore.rules ⬜ 이 브랜치가 한 줄도 안 건드림"* 이라고 정확히 적고 있었다.
> 🚨 **두 줄이 나란히 모순인 채로 서 있었고, 위에 있는 이 줄이 먼저 읽혔다.**
> 규약 4장이 경고한 [추가 668] 그대로다 — *"HANDOFF 87행은 정정됐는데 121행은
> 그대로였고, 내일 아침 세션이 읽는 것은 121행이었다."*

**②의 이유**: 관리자가 그 문서를 고칠 수 있으면 **다른 관리자의 2차 인증 번호를
자기 것으로 바꿀 수 있다** — 그러면 2차 인증이 아무것도 막지 못한다.

### 🚨 ③을 「주석으로 막을 수 없었다」 — 그래서 스위치를 넣었다

인수인계에는 *"③은 화면(4단계)과 함께 배포해야 한다"* 라고만 적혀 있었다.
**그것을 지킬 방법이 없다는 것이 문제였다.**

```
firebase deploy --only firestore:rules  는 이 파일을 통째로 올린다
   → 다른 세션이 cardSources 를 배포하면 ③ 도 함께 나간다
   → 🚨 관리자가 아무것도 못 하게 된다 (인증할 화면이 없으니까)
```

⭐ **「안 나가게」는 지킬 수 없으므로 「나가도 안 켜지게」로 바꿨다.**

```
function adminSessionRequired() { return false; }   ← 이 한 줄이 스위치다
```

📌 **꺼져 있는 동안은 `get()` 이 아예 안 돈다** — `||` 가 앞에서 끊긴다. 문서
조회 비용도 0이다. 켜는 것은 **4단계 화면 배포와 같은 배포**여야 한다.

### 🚨 인수인계가 틀렸던 곳 하나 — `request.time < expiresAt` 은 **안 된다**

```
문서   rules 는  request.time < expiresAt  한 줄로 비교만 한다
실물   expiresAt 은 Timestamp 가 아니라 epoch 밀리초 「숫자」다
       index.ts 의 writeAdminSession 이 adminSessionExpiresAt 의 number 를 넣는다
```

그대로 적으면 **타입이 달라 비교가 오류**가 되고, **rules 의 오류는 곧 거부**라
**관리자가 조용히 잠긴다.** `request.time.toMillis() < expiresAt` 로 썼다.

⚠️ **이것이 왜 안 잡힐 뻔했나** — 음성 대조군으로 재 봤다. 틀린 규칙으로도
**DENY 케이스 셋은 전부 통과했다**(오류 → 거부라서 기대와 결과가 같다).
**ALLOW 케이스 하나만이 이것을 잡았다.**

> **「막히는지」만 재면 「잘못 막히는 것」은 안 보인다.**

### ✅ 테스트 — 켠 상태를 **미리** 재 둔다

`test/firestore_rules/verify_rules.py` 에 11건을 더했다. **86/86 통과**(실측,
2026-09-05, Firebase 규칙 평가 API).

```
꺼진 상태 6건   잠금 ①② 넷 + 대조군(config/testers 는 열려 있다) + 「꺼진 동안은
                세션 없이도 관리자 조작이 된다」 ← 이것이 「지금 배포해도 안전」을 고정
켠  상태 5건   같은 파일의 스위치만 켠 **사본**으로 평가한다 — 파일은 안 고친다
                없음·살아있음·만료·경계(expiresAt == now)·허용목록 밖
```

🚨 **켜기 전까지 그 경로는 한 번도 안 돈다** — 사본으로 미리 재지 않으면
위의 `toMillis` 결함이 **4단계에서야** 드러났을 것이다.

### ⬜ globe2030님 결정이 필요한 것

```
config/admins 최초 등록   번호 해시를 누가 어떻게 넣나 (Firebase 콘솔 수기?)
firestore.rules 배포      ✅ **09-05 에 나갔다** — 아래 「흐르던 것이 멈췄다」 참고
                          ⬜ 다음 배포 때 「무엇이 함께 나가나」를 다시 볼 것
```

### ✅ 흐르던 것이 멈췄다 — `firestore:rules` 가 09-05 저녁에 배포됐다

> 🚨 **이 절은 09-05 낮까지 *"지금도 흐르고 있다 — cardSources 규칙이 미배포다"* 였다.**
> `#833` 이 `main` 에만 있고 서버에 없어서, **그동안 등록된 명함은 파싱 원본이
> 안 남았다.** 원본은 **저장하는 순간에만** 남길 수 있어 **그 명함들은 지금도 못
> 되살린다**(사진에서 재OCR 하는 무거운 길밖에 없다). ⚠️ **지운 것이 아니라 닫힌
> 것이므로 경위를 남긴다** — 「언제부터 언제까지 원본이 없는지」가 나중에 필요하다.

```
✅ 2026-09-05 21:5x 경(KST) 배포됨
   무엇으로 쟀나   PM 이 `npx --yes firebase-tools@15.29.0 deploy --only firestore:rules`
                  의 성공 로그를 봤다 —
                  `✔ firestore: released rules firestore.rules to cloud.firestore`
                  `✔ Deploy complete!`
⬜ 콘솔 배포 이력으로는 아직 대조하지 않았다 — globe2030님께 요청해 둔 상태다
   (globe2030님이 보여 주신 화면은 배포 **전**이었다 — 맨 위가 09-03 09:11)
```

⭐ **「명령이 성공했다」와 「서버가 그렇게 됐다」를 갈라 적는다.** 대개는 같지만
**같다고 가정하는 순간 확인할 자리가 사라진다** — 이 저장소가 09-05 하루에 계속
부딪힌 자리가 이것이다.

### 🚨 그 배포에 **함께 나간 것 셋** — [추가 514] 「배포가 방어막을 걷는다」

`--only firestore:rules` 는 **파일을 통째로 올린다.** 09-03 09:11 배포 이후
`firestore.rules` 를 만진 커밋 **전부**가 이때 나갔다(잰 법:
`git log origin/main --since='2026-09-03 00:00' -- firestore.rules` → 3건).

```
c85f1c8d  #833  cardSources 규칙            ← 이것 때문에 배포했다
98b1cd32  #835  isAdmin 신선도 + adminSessionRequired 스위치
beedc294  #836  adminSessions · config/admins 명시적 차단
```

📌 **관리자 쪽 둘은 나갔지만 켜지지 않았다** — `firestore.rules:27` 이
`return false;` 다(실측). `||` 가 앞에서 끊겨 `get()` 조차 안 돈다.
⚠️ **켜는 것은 설계 4단계(콘솔 인증 화면) 배포와 같은 배포여야 한다** — 먼저 켜면
**인증할 화면이 없어 관리자가 아무것도 못 한다.**

⚠️ **다음 `--only firestore:rules` 배포도 같은 성질이다.** 배포 전에
*"지금 무엇이 함께 나가나"* 를 한 번 보고, **PM 확인을 거친다.**

### ✅ 실물 대조 — 위 인수인계가 맞는지 다시 쟀다 (2026-09-05, 개발A 재개 시)

📌 **문서를 믿지 않고 확인했다**(4-2절 *"표를 믿지 말고 `main` 실물을 먼저 연다"*).
**넷 다 맞았다** — 이어받는 사람은 다시 안 재도 된다.

```
Callable 셋            ✅ index.ts:3000 · 3091 · 3166
adminSessionExpiresAt  ✅ adminAuth.ts:167
firestore.rules        ⬜ 이 브랜치가 한 줄도 안 건드림 (git diff origin/main...HEAD 0줄)
                          grep 1건은 「후속 과제」라고 적힌 주석뿐(:29)
자동 테스트             ✅ npm test 260건 통과 · 실패 0
```

### ✅ 착수 순서는 갈렸다 — globe2030님이 직접 고르셨다 (2026-09-05)

```
2026-09-04 검토        관리자 2단계 먼저
2026-09-05 PM 전달      C안 1단계 먼저   ← PM 이 전한 것
2026-09-05 직접 확인    **관리자 2단계를 마저 끝낸다**  ← 개발C 가 직접 여쭤 받은 답
```

📌 **PM 이 전한 것과 달랐다.** 4-2절이 *"PM 이 전한 것은 사용자 승인을 갈음하지
않는다"* 라고 적어 둔 자리가 **실제로 갈린 자리**가 됐다 — 물었으므로 갈렸다.

> ✅ **09-05 저녁 갱신 — C안 1단계는 시작됐고 끝났다.** 이 경고가 쓰인 날
> 늦게 `AccountPaths` 가 들어갔다([추가 684], #837 · `84f89ca5`) — `users/` 를
> 조립하는 자리 **11곳**을 한 파일로 모았고 **동작은 한 글자도 안 바뀐다.**
> 📌 **경고가 값을 했다** — *"누가 하고 있겠지"* 로 안 읽혔기 때문에 잡혔다.
> ⚠️ **다만 「1단계가 끝났다」이지 「C안이 끝났다」가 아니다** — 2단계(사람 레이어
> 도입)·3단계(잇기)는 그대로 남아 있고, 3단계는 [추가 681] 의 **셋을 같은 PR 로**
> 내야 한다.

### 🚨 계정 안내 갈래가 **두 갈래로 갈려 있다** — 5단계 전에 반드시 정리

```
globe2030님이 개발 세션에 직접   "그래도 다른 계정을 하겠다면 어쩔 수 없지만"
                                → 설계: 「새 계정으로 계속하기」를 막지 않음
PM 이 전한 「병」안               선택지는 「잇기」뿐. 새 계정을 원하면 다른 번호로
```

**정면으로 다르다.** 화면 하나가 아니라 **방침의 차이**다 — 「병」이면 *"이 번호로는
새 계정을 못 만든다"* 가 되고, 설계는 *"권하되 막지 않는다"* 다.
🚨 **`사람-레이어-C안-설계-2026-09-05.md` §4 는 아직 「막지 않는다」로 쓰여 있다.**

### 📮 세션 간 메시지 — 클라우드는 못 보낸다 (2026-09-05 실측, 두 곳에서 확인)

```
로컬 터미널 ↔ 로컬 터미널   ✅ 양방향
로컬 → 클라우드            ✅ 도착
클라우드 → 아무 곳          ❌ 자격이 「전달」에 안 쓰인다
```

🚨 **같은 세션도 어디서 도는지에 따라 달라진다** — 오전에 클라우드에서 막힌 것이
맥으로 옮겨 오자 그냥 됐다. **막히면 세션 종류를 먼저 본다.**

⚠️ **세션 이름은 도중에 바뀐다** — 하루에 `…-97` → `…-9a` → `…-5a` 로 세 번 바뀌었다.
**대괄호 안의 ref 는 안 바뀐다.** 이름으로 보내 반송되면 `ListAgents` 로 다시 본다.

📌 공식 문서에는 이 제약이 안 적혀 있다 — 근거는 오류 문구 자체다.

```json
{"success": false, "message": "Failed to send to bridge:session_…: auth: this cloud
 session cannot message other sessions yet — its credential is accepted for its own
 work but not for delivering to another session, so a reply from here is not
 possible; say so in your response instead of retrying"}
```

⚠️ **다음은 아직 안 갈렸다**: 계정·워크스페이스 설정으로 켤 수 있는지 · 언제 풀리는지.

---

## 4. 다른 세션이 하고 있는 일 (내가 손대지 않는다)

### 🔵 지금 붙어 있는 세션 — 2026-09-05 22:10 실측

> 📌 **잰 법은 `ListAgents` 다**(각 세션이 스스로 신고한 것이 아니라 런타임이
> 보여 주는 목록). ⚠️ **그러므로 「무엇을 하고 있나」는 PM 장부에서 온 것이고
> 「살아 있나」만 이 도구로 잰 값이다** — 둘의 출처가 다르다.

| 이름 | 워크트리 | 잡고 있는 것 | 안 건드리기로 한 것 |
|---|---|---|---|
| **PM(터미널)** `[b115f0]` | 본체 | `docs/legal-drafts/*` · `.github/workflows/ci.yml` · `tool/*` · 승인 창구 전부 | — |
| **개발A** `[18650e]` | `ct-doc-fix` | `functions/src/*` · `test/firestore_rules/*` · backlog **689**(PR #850) | — |
| **개발B** `[941cd6]` | `ct-signup-design` | `lib/core/services/*` · `lib/data/services/*` (PR #845) | `firestore.rules` · `functions/` · `pubspec.lock` |
| **개발C** `[33299e]` | `ct-conflict-fix` | 삭제·파기 경로 전수 점검 ([추가 690], #851 병합됨) | HANDOFF · RESUME |
| **개발D** `[fe2776]` | `ct-handoff-sync` | **이 문서 + `HANDOFF.md`** · backlog **691** | `lib/` · `firestore.rules` · `docs/legal*` |

🚨 **승인 창구가 09-05 에 바뀌었다 — PM 으로 모였다.**

```
PM 이 정한다        착수 · 순서 · 범위 · 겹침 · backlog 번호 · 커밋 · PR · 병합
globe2030님만       🚨 firebase deploy (서버 배포) — 이것 하나뿐이다
                    과금 스위치(config/billing.model 신설)도 여기 딸린다
```

⚠️ **그래도 「PM 이 승인했다더라」를 다른 세션이 전하는 것은 갈음하지 않는다**
(규약 4-2). **PM 에게 직접 받는다.**

### 🚨 같은 지시가 여러 세션에 동시에 간다 — 09-05 에 실제로 겹쳤다

```
「을로 진행」이 PM 과 다른 세션에 둘 다 갔다
  PM     바로 착수 → PR 을 올렸다
  세션    「준비만」 하고 착수 알림은 PR 올릴 때 하려고 미뤘다
결과     같은 파일을 둘이 고쳤다 (일괄 스캔 내보내기의 부서 열)
```

⭐ **10분 안에 「테스트가 있는 쪽」으로 정리했다.** 한쪽은 열 개수를 한 번 세어
*"16 = 16"* 으로 확인했고, 다른 쪽은 **머리글과 값의 짝을 테스트로 고정**했다.
📌 **그래서 알리는 시점을 앞당겼다 — 파일을 열기 전에 알린다**(규약 4-2).

### ⚠️ 오늘 있었던 환경 사고 하나 — 지금은 복구됐다

```
무슨 일   brew uninstall firebase-cli 가 **node 를 함께 지웠다**
결과      몇 시간 동안 functions 테스트와 배포가 막혔다
복구      globe2030님이 brew install node
지금      ✅ 정상 — node v26.8.1 · npm 11.19.0 · npm test 260건 통과
          (PM 이 09-05 저녁 실측. 이 세션이 잰 값이 아니다)
```

📌 **`firebase deploy` 가 죽으면 `npx --yes firebase-tools@<버전>` 으로 우회한다**
— 런북에 적혀 있다(PR #848, 아직 미병합).

### 📮 (지난 기록) PM 께 — 개발C(터미널) [f9ab1f] 인계 (2026-09-05)

> 🚨 **쪽지로 못 보내 여기 적는다.** 이 세션은 **받기만 되고 보내는 것이 막힌다**
> (아래 「보내는 쪽이 막히는 것은…」 참고). PM 이 이 문서를 갱신하는 쪽이므로
> 여기가 제일 확실한 자리다.

**끝난 것**: 관리자 인증 **2단계** — `firestore.rules` (§3-9 에 자세히)

> ⚠️ **2026-09-05 정정 — 이 「끝난 것」은 ③ 까지다.** 이 인계 시점에 커밋된 것은
> `isAdmin()` 신선도와 스위치뿐이고, **①②(`adminSessions`·`config/admins` 전면
> 차단)는 안 들어가 있었다.** 그 둘은 **#836 에서 개발A 가 넣어 병합됐다**
> (`beedc294`). 위 §3-9 의 정정 상자를 함께 볼 것.

```
브랜치   feat/admin-auth-stage2 · 푸시 완료 (79adb1a1)
검증     verify_rules.py 86/86 · check_admin_sync 일치
         flutter analyze info 11 (error·warning 0) · flutter test +1988 ~4
         (로컬 macOS · Flutter 3.44.8 — CI 기준선 1,944 와 잰 곳이 다르다)
보고서   docs/planning/admin-auth-stage2-pm-report-2026-09-05.md
```

⚠️ **「끝났다」가 「켜졌다」가 아니다** — `adminSessionRequired()` 가 `false` 다.
켜는 것은 **설계 4단계(콘솔 인증 화면) 배포와 같은 배포**여야 한다.

**PM 이 정해 줄 것 넷**

```
① backlog 번호   안 집었다(4-2절). 배정 대기
② PR            안 열었다 — globe2030님 「기록하고 중지」 지시
③ rules 배포     ✅ **닫혔다 — 09-05 21:5x 경 배포됐다**(위 §3-9). 그때 이 인계의
                변경(#835)도 함께 나갔고, **예상대로 켜지지는 않았다**(스위치 false)
                ⚠️ 그동안 등록된 명함의 파싱 원본은 **되돌릴 수 없다**
④ C안 1단계      ✅ **09-05 저녁에 끝났다** — [추가 684](#837). 이 경고를 쓴 뒤
                개발B 가 착수했다. ⬜ 2·3단계는 그대로 남아 있다
```

**🚨 장부 정정 하나** — 개발B [941cd6] 가 보내온 장부에는
*"개발A [18650e] · firestore.rules 단독 점유 · M firestore.rules 가 지금 떠 있습니다"*
로 돼 있다. **그 미커밋은 내 손에서 커밋됐고 지금 `git status` 는 깨끗하다.**
경위는 §3-9 「착수 순서」다 — globe2030님께 **직접** 여쭤 이어받았다.

⚠️ **개발A 가 `waiting` 으로 떠 있다.** 같은 브랜치를 다시 이어받으면 **자기
미커밋이 사라진 줄 안다.** 장부를 「커밋됨·푸시됨」으로 옮겨 주기 바란다.

**점유 반납**: 본체 워크트리 · `feat/admin-auth-stage2` — **손 뗐다.**
`functions/src` 는 한 글자도 안 건드렸다(개발A 것 그대로).
`pubspec.lock` · `lib/core/services` · `lib/data/services` 도 안 만졌다.

---

2026-08-21 확정 분담이다. 근거는 backlog 추가 370.

| 누가 | 무엇 | 상태 |
|---|---|---|
| **소셜·좌표·주소 세션** (08-21 밤 종료, 인계 완료) | 남긴 몫: 행안부 지오코딩 앱 연결(⚠️ 좌표 API 운영키 대기) · 386 검출 조건 측정(debug 슬롯) · 387 자르기 기준 전환(386 이후) · 자리채움 22건 정리(삭제 전 목록을 PM에 보냄) · 카카오 웹훅 · 진단 화면 | 인계는 이 절 + backlog 376~387 |
| **기능개선 세션** (클라우드, 살아 있음) · 주소 `f9d47e` | 계정·DB 설계(C안) · 관리자 인증 · 파싱 원본. 브랜치 `claude/feature-improvements-bug-check-jzz275` | 🚨 **이 세션은 다른 세션에 메시지를 못 보낸다** — 아래 참고 |
| **PM 세션** (살아 있음) | PR 게이트 · ⑥-C/⑦-주변 발주 · 문서 정합 · 위 자율 큐 | 이 문서를 갱신하는 쪽 |
| (소멸) 앞 PM 세션 `-0f` | 정답지 95건·필드별 채점·행안부 실패 분해 — 전부 병합됨(#385·#387·#388) | PR #386 리뷰 10건은 추가 369로 회수. 2026-08-21 소멸 확인 |
| (종료) 그 이전 세션들 | 인계: [`sessions/2026-08.md`](./sessions/2026-08.md) 0-19~0-23 | — |

### 🚨 클라우드 세션은 **메시지를 받기만 한다** (2026-09-05 실측)

```
PM → 클라우드 세션   ✅ 된다. 오늘 빌드(터미널) 세션이 실제로 보내왔다
클라우드 세션 → PM   ❌ 막힌다
```

> 🚨 **2026-09-05 추가 실측 — 클라우드만이 아니다.**
>
> ```
> 개발B(터미널) → 개발C   ✅ 도착했다
> 개발C → 개발B·PM        ❌ 보낼 수단 자체가 없다
> ```
>
> 개발C(`connection-trace-ai-v2-flutter-0b` [f9ab1f], 데스크톱 앱 Code 탭)에서
> 잰 것이다. **오류 문구가 아니라 「도구가 아예 없다」였다.**
>
> ```
> ListAgents            ✅ PM [b115f0] · 개발A [18650e] · 개발B [941cd6] 가 보인다
> SendMessage           ❌ 도구 목록에도 ToolSearch 에도 없다
> send_message(b115f0)  ❌ "Session b115f0 not found"
> list_sessions         ❌ "No other sessions found"
> ```
>
> ⚠️ **「보이는 목록」과 「보낼 수 있는 목록」이 다른 장부다.** `ListAgents` 에
> 떠 있다고 보낼 수 있는 것이 아니다.
>
> 📌 **그래서 위 절의 「로컬 터미널 ↔ 로컬 터미널 ✅ 양방향」을 그대로 믿지 마라**
> (§3-9). 그것도 **잰 세션에서만** 참이었다. 🚨 **막히면 세션 종류가 아니라
> 「이 세션에 그 도구가 있는지」부터 본다** — 종류가 같아도 다르다.
>
> ⭐ **막혔을 때의 우회로**: 이 문서(§4)에 적는다. PM 이 갱신하는 쪽이라
> 쪽지보다 오히려 안 사라진다.

#### 무엇을 불렀나 (2026-09-05)

```
SendMessage({
  to: "bridge:session_013QGj2WPa6UQ2V3DCeTXnDM",   ← 그 세션이 보내온 from 값 그대로
  summary: "679 기록 완료 + ci.yml 변경 사전 통보",
  message: "…"
})
```

#### 🚨 돌아온 것 — **전문**

⚠️ **줄여 적지 않는다.** 처음에 앞 한 줄만 적었다가 고쳤다 — 원인을 좇으려면
**뒷부분이 더 중요하다**(어디까지 되고 어디부터 막히는지가 거기 있다).

```json
{
  "success": false,
  "message": "Failed to send to bridge:session_013QGj2WPa6UQ2V3DCeTXnDM: auth: this cloud session cannot message other sessions yet — its credential is accepted for its own work but not for delivering to another session, so a reply from here is not possible; say so in your response instead of retrying"
}
```

옮기면: *"이 클라우드 세션은 아직 다른 세션에 메시지를 보낼 수 없다. **이 세션의
자격증명은 자기 작업에는 받아들여지지만 다른 세션으로의 전달에는 받아들여지지
않는다.** 그래서 여기서 답장하는 것은 불가능하다. 재시도하지 말고 그 사실을
응답에 밝혀라."*

#### 이 문구가 **실제로 말하는 것**

```
분류      auth        인증·권한 문제다. 네트워크나 주소 오류가 아니다
경계      자기 작업 ✅ / 남에게 전달 ❌     ← 자격증명 자체는 유효하다
범위      this cloud session               세션 종류에 걸린 제약으로 읽힌다
지시      재시도하지 말라
"yet"                                     나중에는 될 수 있다는 뉘앙스
```

#### ⚠️ 이 문구가 **말하지 않는 것** — 짐작으로 채우지 않는다

```
⬜ 계정·워크스페이스 설정으로 켤 수 있는 것인지
⬜ Claude Code 버전이나 환경(anthropic_cloud) 때문인지
⬜ 상대가 bridge 세션이라 그런지, 아무 세션에나 그런지
⬜ 언제 풀리는지
```

📌 **실측으로 아는 것은 하나뿐이다 — 받는 것은 된다.** 같은 날 빌드(터미널)
세션이 보낸 메시지가 실제로 도착했다. **채널은 살아 있고 방향만 막혀 있다.**

⚠️ **한 번만 시도했고 재시도하지 않았다.** 오류가 그렇게 지시했고 `auth` 라
반복해도 같을 종류다. 🚨 **다른 상대(bridge 가 아닌 세션)로는 시험하지 않았다**
— 시험하려면 남의 세션에 시험용 메시지를 넣어야 해서 그냥 하지 않았다.
**그래서 위 셋째 줄(⬜)이 아직 안 갈렸다.**

📌 **그래서 클라우드 세션의 보고는 이 문서(와 커밋)로 온다.** 안 보내는 것이
아니라 못 보내는 것이다 — **PM 이 먼저 물어야 답이 온다.**

⚠️ **이름을 안 정하면 도중에 바뀐다.** 2026-09-05에 한 대화 안에서
`…-97` → `…-9a` → `…-5a` 로 **세 번** 바뀌었다.
🚨 **바뀌지 않는 것은 대괄호 안의 ref 다** — 위 표에 그 값을 적어 둔다.
이름으로 보내 안 닿으면 ref 로 다시 시도한다.

#### ⭐ 그런데 이름은 **정할 수 있다** — `claude --name`

```bash
claude --name "PM(터미널)"                      # 새로 시작하며
claude --resume <세션ID> --name "PM(터미널)"     # 대화를 이어가며
```

`claude --help` 원문:

```
-n, --name <name>    Set a display name for this session
                     (shown in the prompt box, /resume picker, and terminal title)
```

📌 **터미널 창 제목에 그대로 뜬다.** 창이 여럿일 때 **제목만 보고 어느 세션인지**
가릴 수 있다 — 위의 「이름이 바뀌어 못 찾는」 문제가 여기서 풀린다.

⚠️ **확인한 것과 안 한 것을 가른다.**

```
✅ 확인   --name 이 표시 이름을 정한다 (claude --help 원문)
⬜ 미확인  그것이 세션 간 메시지의 「주소」까지 고정하는지
          ListAgents 가 보여 주는 주소와 표시 이름이 같은 것인지 안 재 봤다
```

🚨 **그래서 주소는 여전히 ref 를 함께 알린다.** 이름이 고정돼도 주소가 그것인지는
아직 모른다.

#### 📌 이 답을 못 찾았던 이유 — 도구가 못 본 자리였다

2026-09-05에 조사 에이전트에게 물었더니 *"공식 문서에 없다"* 고 답했다.
**맞는 답이었다 — settings.md·CLI reference·desktop.md 어디에도 없다.**
그런데 **`claude --help` 에는 있었다.**

```
찾은 곳    공식 문서 4종        → 0건
있던 곳    claude --help 한 줄  → 있음
```

⭐ **그리고 그날은 볼 수가 없었다** — 클라우드 컨테이너에는 `claude` 실행 파일이
없어서 `--help` 를 돌릴 수 없었다. **기계가 바뀌자 한 줄로 나왔다.**

⚠️ **CLAUDE.md 4장의 「세는 도구가 못 보는 것」이 여기에도 걸린다** — 문서 검색은
**문서만** 본다. `--help`·`--version`·실제 실행은 **다른 도구**다.
**「없다」를 한 도구로 말하지 마라.**

### 📮 기능개선 세션 → PM (2026-09-05, 답 대기 중)

**끝난 것**

```
#830 병합(610e8d5)   관리자 인증 1단계 + CI 에 Cloud Functions 테스트 단계 신설
                     🚨 그 전까지 functions 테스트 17개를 CI 가 한 번도 안 돌렸다
브랜치 27c68ed        cardSources(파싱 원본 보존) · architecture.md · data-schema.md
                     · 사람-레이어-C안-설계 · uid 세 안 비교 §8 에 C안 결정 기록
```

**PM 이 정해 줄 것 셋**

```
① backlog 번호      제가 잰 값은 681 (origin/main 680). ⚠️ 대조용이지 배정이 아니다
② 파일 점유         다음에 만질 것: firestore.rules · functions/src/index.ts · docs/admin/*
                    푸시된 브랜치와는 안 겹치는 것을 확인했으나
                    🚨 **미커밋 작업은 저도 못 본다** — PM 확인이 필요하다
③ 착수 순서         ✅ 갈렸다 — globe2030님이 「관리자 2단계 먼저」를 직접 고르심
                    (2026-09-05, 개발C 가 직접 여쭘). 2단계 코드는 끝났고
                    ✅ **C안 1단계는 09-05 에 끝났다** — [추가 684](#837)
```

🚨 **globe2030님께 올라간 것 (PM 이 대신 정하지 않는다)**

```
「오래」 값                    C안 3단계(잇기) 전에 필수 — 잘못 이으면 남의 명함이 열린다
~~firestore.rules 배포~~       ✅ **09-05 에 끝났다**(위 §3-9) — 아래 설명은 그때의 이유다
번호 변경 갑/을 확정           갑안(이용자 직접) 권고
조회 기록 보관 기간            법무 확인 필요
```

⚠️ **rules 배포가 왜 시간에 민감했나**(✅ 09-05 에 배포돼 닫혔다): `cardSources`
규칙이 서버에 없어 **그동안 등록된 명함은 파싱 원본이 안 남았다.** 원본은 저장하는
순간에만 남길 수 있어서, **그 사이 명함은 지금도 못 되살린다** — 배포가 소급하지
않는다. 🚨 **이유를 지우지 않는다** — 같은 모양(코드와 규칙을 갈라 올리는 것)이
또 나온다.
🚨 그리고 `firebase deploy --only firestore:rules` 는 **rules 전체를 배포**하므로
다른 세션의 미배포 변경이 함께 나간다 — **배포 전에 PM 확인이 필요하다.**

⚠️ **여러 세션이 붙을 때 오늘 겪은 것 둘**:
- `backlog.md`의 "## 작업 로그" 바로 아래에서 **세 번 충돌**했다. 번호를 나눠
  가져도(220~227) **삽입 위치가 같으면 git은 충돌로 본다.** 기록은 작업이 끝난
  뒤 한 번에 넣고, 넣기 직전에 `git fetch && git merge origin/main`을 먼저 한다.
- **같은 문서를 두 세션이 각각 쓸 뻔했다**(F-10 스펙). 쓰기 전에 이미 쓴 사람이
  있는지 묻는 편이 빠르다.

**다음 backlog 번호: 692** (2026-09-05 22:30 실측 · `origin/main`(`339c5950`) 색인 최고가
**690** — 배정은 PM 에게 요청, CLAUDE.md 4-2절)

> 🚨 **687 다음이 688 이 아니다 — 넷이 이미 배정돼 있다.** 「비어 보이는 번호」를
> 유실로 읽고 메우면 안 된다.
>
> ```
> 687   ✅ #844 병합됨
> 688   PR #845 — 미병합 (개발B)
> 689   PR #850 — 미병합 (개발A)
> 690   ✅ #851 병합됨 (개발C — 삭제·파기 경로 전수 점검)
> 691   개발D (이 갱신)
> ```
>
> 📌 **689 가 한때 비어 보였던 경위**: PM 이 `#842` 용으로 배정했는데 그 PR 에
> 안 들어간 채 병합됐고, 그 뒤 `#850` 에 다시 배정됐다. **유실이 아니다.**
> ⚠️ 규약 4-2 가 *"비어 있는 번호는 다른 세션의 PR 에 있을 수 있다 — 그때는
> 어느 PR 에 있는지 적어 둔다"* 라고 한 자리다.

> ⚠️ **이 줄이 388 로 여드레 넘게 서 있었다.** 실제로는 그 사이 285개가 더 쌓여
> 672까지 갔다. 📌 **번호는 매일 늘어난다 — 여기 적힌 값은 언제나 하한으로만
> 읽고, 쓰기 직전에 `backlog.md` 맨 위 줄을 직접 본다.**
>
> 🚨 **위 §4 표(누가 무엇을 잡고 있나)는 2026-08-21 기준이라 지금과 다르다.**
> 세션 분담은 **PM에게 물어 확인한다** — 이 문서로 알 수 있는 것이 아니다.
⚠️ 예전 기록에 **중복 번호가 실재한다**(추가 277이 셋) — backlog 전체 재정렬을
시도하지 말 것. 최근 블록만 만진다(2026-08-21 정렬 검사에서 실측 확인).

### ✅ 자율 큐 — 전부 완료 (2026-08-22, 추가 388~390)

```
① #406·#407 병합 ✅   ② ⑥-C+⑦-주변 병합(#411) ✅   ③ 372 정정 ✅
④ 운영 규칙 명문화 ✅   ⑤ I안 사실 조사 ✅ (account-phone-anchor-facts)
```

~~다음 기기 배치에서 볼 것~~ → ✅ **배치 완료(2026-08-22, 추가 391·392)**
— ⑤·⑥-C(+#413 수정)·⑦·386(가설 기각→원인 규명→#415→실기기 확인) 전부 통과.

⚠️ **다음 기기 세션 첫 순서**: 폴드에 깔린 빌드 `cb63f89`는 검증용 로컬
병합본이라 **main 히스토리에 없는 해시**다 — #415 병합 후 main에서 재빌드해
올릴 것(낡은 빌드를 버그로 오인한 전례 방지). 기능은 #415와 동일이라
테스터 배포만 아니면 위험 없음.

**사용자 손이 필요해 멈춰 있는 것**: P2 4건 목업 확정 · 계정 구조 I안 채택 ·
방침 게시(1차 회신 문안 반영 후) · Functions 배포 경로 ⓐⓑⓒ · OCR 착수 순서 ·
광고 수신 동의 여부 · 다음 기기 배치(⑤·⑦ 화면 + 386 debug 측정 — 준비물:
**테두리에 색 있는 명함 여러 장**, 폴드 반경은 "전체"로 바뀐 상태).

---

## 5. 승인·확인 대기 목록

### 2026-08-21 기준 — 새 안건

| 무엇 | 누가 | 비고 |
|---|---|---|
| Functions 배포 경로(닭과 달걀 ⓐⓑⓒ) | **사용자** | `APPLE_IAP_SHARED_SECRET`이 스토어 등록 후 발급 — HANDOFF 2-0 |
| 네이버 로그인 버튼 공식 애셋 | **사용자** | 지금 있는 것은 워드마크뿐. 검수 전 멤버 등록은 완료(08-21) |
| 네이버 앱 로고 등록 + 사전 검수 신청 | **사용자** | 검수 전에는 등록 멤버만 로그인된다 |
| App Check 강제(`config/socialAuth.requireAppCheck`) | **사용자** | 켜기 전 스토어 서명 빌드의 토큰 발급 확인 필수 |
| 방침 시행일 23→27일 정정 | **사용자** | 고지 7일 원칙 — 08-20 게시 기준이면 27일 |
| 광고 수신 동의 기능 도입 여부 | **사용자** | 기록 없는 구두 안건 — 확정 전 착수 금지 |
| ~~등록 22건 자리채움 값~~ | — | ✅ **결정 끝(08-21): 지운다**(빈 값 표기용이었음). 구현은 소셜 세션, 삭제 전 목록은 PM 교차 확인 |
| OCR 무료 개선 착수 순서 | **사용자** | 추가 329 — 정답지→라틴2차→좌표재배정 순서 결정 대기 |

### 이하 2026-08-15 기준 — 재검증 필요

| 무엇 | 누가 | 비고 |
|---|---|---|
| 법무 검토 의뢰 | **사용자** | 위 1번 |
| 방침 v2.2 게시 | 사용자 승인 | 법무 회신 후 |
| ~~rules 배포~~ | — | ✅ **08-26 · 09-03 · 09-05 세 번 나갔다.** 이 줄의 「미배포」는 2026-08-15 기준이라 낡았다 |
| ~~지갑 main 병합~~ | — | ✅ **병합됨(2026-08-18, PR #299)** — 위 2절 참고 |
| 잔여 충전 이용권 처리(소멸·환불) | 사용자 + 법무 | IAP 출시 전 필수. 방침·약관·계정삭제 안내 **세 곳**에 같은 내용을 적어야 함 |
| ~~충전 티어별 회수~~ | — | ✅ **확정(2026-08-18, 추가 303)**: 7단계 전부 — 30 / 100 / 200 / 450 / 1,500 / 2,600 / 5,500회. ⚠️ 상위 3단계 **활성화 여부**는 미결. 실측 전 확정이라 기존 방침(추가 222·P1-1)의 예외 — 근거는 구현 스펙 🚨 블록 |
| ~~P0-11 AI 한도 20→10~~ | — | ✅ **결정 끝(2026-08-18)** — 되돌리는 게 아니라 **제도 폐지**(충전형). P1-5로 흡수 |
| 지갑 U6(리퍼럴 redemption) | — | **미구현** — 지갑에서 유일하게 안 만든 조각 |
| 과금 전체 테스트 | 사용자 | 스토어 출시 **전** 필수(CLAUDE.md 6절) |
| iOS 403 | 사용자 | Apple 계정 역할 문제 |

### ⚠️ 테스트 종료 후 반드시 되돌릴 것

`enforceAppCheck: true` 복원 · `config/testers` 비우기.
**허용목록은 App Check 우회 권한**이라 열어 둔 채 잊으면 안 된다.

~~`DAILY_LIMIT` 20→10(P0-11)~~ — **이 항목은 빠졌다(2026-08-18).** 되돌릴 값이
아니라 없앨 제도다(충전형 확정). `DAILY_LIMIT`/`MONTHLY_LIMIT` 제거는 지갑
전환(P1-5)과 같은 배포로 나간다 — **먼저 지우면 그사이 아무 상한도 없다.**

### 📌 확인이 필요한 낡은 기록 (사용자 판단)

**① ✅ 사진 백업 주석 둘은 닫혔다 — 🚨 그런데 고치다 결함 셋이 나왔다** (2026-09-05)

```
✅ auth_gate.dart                #822 에서 과거형 경위로 고쳐졌다 (09-04)
✅ contact_image_service.dart    #844 에서 사실로 고쳐졌다 (09-05, [추가 687])
✅ card_photo_backup_service.dart  같은 파일 안의 모순도 함께 (09-05)
```

⭐ **닫힌 것보다 「고치다 나온 것」이 값나간다.** 주석을 고치려고 업로드·삭제 경로를
따라가다 **탈퇴·삭제가 안 지우는 자리 셋**이 드러났다. **⬜ 셋 다 globe2030님 결정
대기이고, 코드는 아직 안 고쳤다.**

| 무엇 | 무엇이 남나 | 왜 무거운가 |
|---|---|---|
| **프로필 사진 「지우기」** | 로컬 `.enc` · Cloud Storage 객체 · 백업 장부의 「백업됨」(2,000장 한도를 계속 차지) | 화면은 *"이 사진은 **이 기기에만 있어** 지우면 되돌릴 수 없어요"* 라고 말한다 — **두 쪽 다 08-26 이후로 거짓이다** |
| **탈퇴 시 `users/{uid}/cardSources`** | 파싱 원본이 남는다 | 🚨 **제3자(명함 주인) 개인정보**다 |
| **탈퇴 시 `ocrStats/{uid}`** | 통계 문서가 남는다 | 방침이 **파기를 단언한** 자리다 |

🚨 **셋이 같은 모양이다 — [추가 79]·[추가 518] 의 *"서비스는 정상인데 부르는 쪽이
없다"*.** 프로필 쪽은 `deleteCardImage` 호출이 **그 파일에 0건**이었다
(`grep -rn "deleteCardImage(" lib/` → contacts_repository 둘 · leftover_account_purge 하나).
📌 **[추가 518] 에서 명함 쪽은 정확히 이것을 고쳤는데 프로필 쪽만 그때 같이 안 봤다.**

⬜ **갈래**: 갑 지우면 서버도 지운다(⚠️ 방침의 사진 파기 사유가 *"명함 삭제 시 ·
회원 탈퇴 시"* **둘뿐**이라 「프로필 사진 삭제 시」가 없다 — **법무와 맞물린다**) ·
을 문구만 사실에 맞춘다(**권하지 않음** — 「지우기」를 눌렀는데 안 지우는 것이
방침·상식과 어긋난다) · 병 둘 다.

⚠️ **여기까지는 코드를 읽은 것이고 실기기·서버 실물로는 안 봤다** — 규약 4장이
*"삭제는 실기기와 서버 실물로 확인한다"* 고 한 자리다.

📕 **근거 문서**: [`deletion-paths-audit-2026-09-05.md`](./deletion-paths-audit-2026-09-05.md)
([추가 690], #851) — 「지운다」고 말하는 자리를 **전수로** 훑은 것이다. 프로필 건은
[추가 687](#844). ⭐ **하나가 나오자 「같은 모양이 더 있는지」를 물은 것이 값을 했다** —
전수로 보니 **둘이 더 있었다.** [추가 79]·[추가 518] 과 같은 뿌리다.

**② 아래 P1-19 건** — 종전 기록 그대로 남긴다.

CLAUDE.md 6절의 *"Android 릴리스가 아직 debug 키스토어로 서명돼 있다(P1-19)"*가
**이 기기 기준으로는 사실이 아니다** — `android/key.properties`가 있고 빌드
스크립트가 그 파일이 있으면 업로드 키로 서명한다. 다만 그 파일은 저장소 밖이라
**다른 기기에서 빌드하면 debug로 폴백**한다. 문구를 지울지 "빌드 기기에
key.properties가 있어야 한다"로 고칠지는 정책 판단이라 손대지 않았다.
**스토어 업로드 전에는 정리돼야 한다.**

### 배포 후 실물 확인 8건 (관리자 보안 — 코드는 전부 배포됨)

브라우저에서 <https://connection-sense-admin.web.app> 로그인 후 한 번에 본다.

011 CSP 하 로그인·Firestore·Functions 동작 / 004 XSS 미실행 / 006 문의
페이지네이션 / 010 관리자 조작 후 감사 기록 / 012 로그아웃 후 자동복원 없음 /
007 (실기기) 탈퇴 → 문의 0건 / 002 (실서버) 크레딧 멱등성 /
F-07 (실기기) 재생성마다 다른 포인트

---

## 6. 착수 가능한 것 (승인 없이 코드까지 만들 수 있음)

| 무엇 | 스펙 | 주의 |
|---|---|---|
| **CS 보존 3층 구현** | [`cs-retention-spec-2026-08-15.md`](./cs-retention-spec-2026-08-15.md) | ⚠️ `onUserDeletedCleanup`에 **세 갈래가 겹친다** — 사진 삭제·문의 이관·지갑 `deviceLedger` 보존. 따로 넣으면 마지막이 앞의 것을 덮어쓴다 |
| 탈퇴 시 Storage 사진 삭제 | 추가 220 | 위와 같은 함수. 함께 넣을 것 |
| F-15 묶음 머리글 눈으로 확인 | 추가 225 | 실제 데이터에 **같은 주소 짝이 없어 미확인**. 테스터 재등록 후 같은 건물 명함 두 장이 들어오면 그때 |

**배포는 못 하지만 코드는 만들 수 있다** — 게이트는 배포에 걸려 있지 구현에 걸려
있지 않다.

---

## 7. 이 프로젝트에서 반복해 확인된 것 (짧게)

1. **"테스트 통과 = 동작함"이 아니다.** 오늘 잡은 결함 넷 중 셋이 테스트·코드
   리뷰로는 안 나오는 종류였다 — 메모에 박히던 가짜 문구(AI 요청에 실려 나감),
   스크린리더 중복 읽기(화면상 정상), 켜면 실패하는 토글.
2. **"의도한 실패"도 사용자 눈에는 결함이다.** 게이트를 닫아 둔 기능은
   화면에서도 닫아야 한다.
3. **정리·마이그레이션 도구는 실제 데이터에 돌려 봐야** 판정 규칙의 사각지대가
   드러난다(추가 226).
4. **회귀 방지 테스트는 검사 지점이 하나면 부족하다**(추가 225).
5. **문서 사이 상태가 어긋나면 이미 답한 질문을 다시 묻게 된다**(추가 218).

---

*이 문서는 상태가 바뀔 때마다 갱신한다. 배경·경위는 [`HANDOFF.md`](./HANDOFF.md)
[`sessions/2026-08.md`](./sessions/2026-08.md) 0-19~0-23과 [`backlog.md`](./backlog.md) 추가 216~227에 있다.*
