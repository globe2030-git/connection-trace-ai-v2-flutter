# 규약 갱신 제안 — 2026-09-05~06에 나온 **열여덟** 중 무엇을 넣나

> PM 배정. 🚨 **이 문서는 `CLAUDE.md` 를 고치지 않는다.** 규약 파일은 globe2030님
> 승인 사항이다. **제안만** 한다.
>
> ⚠️ **모으는 일이 아니라 고르는 일이다.** 규약이 스스로 적어 두었다 —
> *"규약은 늘릴수록 안 읽힌다."*

---

## 결론 — **여덟이 더 나왔는데 새 규칙은 여전히 하나다**

```
새로 더한다        **1개**   「초록이 무엇을 뜻하는가」   ← 오늘 것 넷이 여기 합쳐졌다
이미 있는 줄을 고친다  5개   --squash · 다중집합 · 파이프 · 「없다」 · 기기 배정
안 넣는다          8개   이유를 아래에 적었다
```

⭐ **9/5에 열 가지, 9/6에 여덟 가지가 나왔는데 새 규칙은 늘지 않았다.** 오늘 것
중 넷(①②③④)이 **9/5의 A와 같은 뿌리**여서 한 절로 묶였기 때문이다.

⚠️ **9/6에 나온 여덟은 대부분 다른 세션이 겪은 것이고, 나는 그것을 전달받았다.**
아래에 **누가 쟀는지**를 함께 적는다 — 어제 그것을 안 적어 틀린 일이 있었다.

📌 **넷은 「규칙을 늘리는 것」이 아니라 「이미 있는 줄이 틀렸거나 좁은 것을 고치는
일」이다.** 그래서 규약 분량이 거의 안 는다. ⭐ **오늘 다섯 건은 「규약을 안 읽어서」가
아니라 「읽었는데 그 줄이 좁아서」 났다** — 그러니 늘리는 것보다 고치는 것이 값이 크다.

---

## A. 🆕 새로 더할 것 하나 — **「초록이 무엇을 뜻하는가」**

**어디에**: 3장 「커밋 전 확인」 끝, 또는 4장 「센 것도 틀린다」 뒤

> ⭐ **9/5에는 「대조군부터」였는데, 9/6에 넷이 더 나와 한 절로 커졌다.**
> 넷 다 *"검사가 초록인데 아무것도 안 지키고 있었다"* 는 같은 뿌리다.

### 왜

오늘 이것으로 **두 번** 물렸고, 방향이 정반대였다.

```
① 규칙 테스트    ✅ 통과하는 케이스가 **아무것도 증명하지 않았다**
                 「일반 로그인 사용자」 케이스는 config/admins 를 isAdmin() 으로
                 바꿔도 통과했다 — 비관리자는 어차피 걸리기 때문이다

② 변조 시험      🚨 **19개 전부 「잡았다」로 나왔는데 전부 무효였다**
                 워크트리에 node_modules 가 없어 tsc 가 없었고, npm test 가
                 변조와 무관하게 **항상 실패**했다
```

⚠️ **①은 「통과」가, ②는 「실패」가 거짓이었다.** 둘 다 **대조군이 없었다.**

🚨 **그리고 ①을 겪고 그 교훈을 문서에 적은 사람이 몇 시간 뒤 ②에 물렸다.**

### 제안 문안

```markdown
### 🚨 검사 결과를 읽기 전에 **대조군부터** (2026-09-05)

통과도 실패도 그 자체로는 아무것도 말하지 않는다.

    양성 대조군   **통과해야 할 것이 통과하는가**
                  → 안 하면 「전부 실패」가 검사 결과처럼 보인다
    음성 대조군   **깨뜨리면 빨개지는가**
                  → 안 하면 「전부 통과」가 안전처럼 보인다

📌 **「안 잡혔다」에는 둘이 있다. 안 가르면 멀쩡한 검사를 「구멍」으로 보고한다.**

    테스트가 약하다   지켜야 할 것을 안 지킨다        → 테스트를 고친다
    변조가 약하다     애초에 지킬 것이 없는 자리다     → 변조를 고쳐 다시 잰다

⚠️ 2026-09-05에 `tombstoneCleanup` 의 `400→401` 이 **무의미한 변조**였다
(Firestore 상한 500 아래라 여전히 안전하다). **`400→600` 으로 하니 잡았다** —
한 번 더 안 쟀으면 **없는 구멍을 적을 뻔했다.**
```

⭐ **이것이 「기존 절이 안 덮는」 것이다.** 규약은 *"세는 도구가 못 보는 것"*
(추가 664)을 적었는데, 그것은 **세는 쪽**의 이야기이고 이것은 **결과를 읽는 쪽**이다.

---

### 🆕 9/6에 넷이 더 나왔다 — **같은 절에 붙인다**

⚠️ **넷 다 다른 세션이 겪은 것이고 나는 전달받았다.** 재지 않은 것은 그렇게 적는다.

#### ① 🚨 「없다」를 보는 검사는 **값을 안 잠근다** (개발C 실측)

```
findsNothing 하나로는 **어떤 문자열로 바꿔도 통과**한다
```

📌 규약 4장에 *"`grep` 은 있는 것만 찾는다 — 「없다」를 grep 으로 말하지 마라"* 는
있는데 **「검사 쪽」 판은 없다.** 같은 함정이 **테스트 코드에서** 다시 난 것이다.

⭐ **그리고 「일어나지 않는 일은 조건으로 기다릴 수 없다」가 같은 뿌리다** —
아무리 기다려도 **「아직 안 일어난 것」과 「영영 안 일어날 것」이 구분되지 않는다.**

> **「없다」 검사는 「무엇이 없는지」도 「언제까지 없는지」도 안 본다 —
> 「일어나는 쪽을 잠그는 짝」이 있어야 뜻을 갖는다.**

#### ② 고친 뒤 초록을 보지 말고, **고치기 전에 빨개지는지를 먼저** 본다

안 빨개지면 그 검사는 **그 값을 안 잠그고 있는 것**이다.
📌 위 「음성 대조군」과 같은 말인데, **순서**를 못 박는 것이 다르다 —
고친 뒤에 하면 **이미 초록이라 확인할 수가 없다.**

#### ③ **무엇이 잠겼고 무엇이 안 잠겼는지 갈라 적는다**

*"문안을 완전 일치로 잠갔다"* 가 실은 **둘 중 하나에만** 맞는 말이었다.
⚠️ **「잠갔다」는 범위를 함께 적지 않으면 다음 사람이 전부로 읽는다.**

#### ④ **고정 시간으로 기다리는 검사는 러너에서 깨진다** (#868 실측, 경계 15ms)

⭐ **이것도 「초록의 뜻」 문제다** — 15ms 차이로 통과가 갈리면 **그 초록은 우연**이다.

```
❌ Future.delayed(고정 시간)   러너가 느리면 깨지고, 빠르면 우연히 통과한다
✅ 조건으로 기다린다
```

📌 **넉넉히 기다리는 것이 검사를 무르게 만들지 않는다** — 차단이 안 풀리면 값은
**영영 안 바뀌므로** 제한 시간을 다 쓰고 **그대로 실패한다.**

---

## B. ✏️ 2장 — `--squash` 는 **PR 본문을 안 담는다**

**어디에**: 32행 *"이 저장소의 커밋 메시지는 나중에 원인을 추적하는 1차 자료로
쓰인다"* 바로 뒤

### 왜 — **규약 자신의 주장이 무너지는 자리다**

`#835` 병합 전에 PR 본문에 정정 블록을 붙였는데 **커밋 메시지에는 안 들어갔다.**
`--squash` 는 **원 커밋들의 메시지를 이어 붙이고 제목만** 반영한다.

🚨 결과: `git log 98b1cd32` 본문에 **없던 보안 사고가 있었다고** 적힌 줄이 남았다
(*"adminSessions·config/admins 가 클라이언트에게 열려 있었고"* — 사실이 아니다).
**히스토리라 못 고친다.**

⚠️ **PM 과 개발A 둘 다 「제목을 고쳤으니 본문도 갔겠거니」 했다.** 병합 뒤
`git log` 를 연 사람만 알았다.

### 제안 문안

```markdown
🚨 **`--squash` 는 PR 본문을 커밋 메시지에 안 담는다** — **제목만** 반영하고
본문은 **원 커밋들의 메시지를 이어 붙인다.** 그러므로 병합 전에 PR 본문만
고치면 **1차 자료에는 안 들어간다.** 본문을 고치려면 **원 커밋을 고쳐 다시
푸시**해야 한다.

📌 **커밋 메시지와 PR 본문이 갈리면 PR 쪽이 새것**이다(2026-09-05, `#835`).
```

---

## C. ✏️ 4-2 — ④(다중집합)는 **줄을 고칠 때 셈법이 다르다**

**어디에**: 464행 ④ 설명, 476행 *"사라진 줄이 있으면 그 줄을 그대로 보여 준다"* 옆

### 왜 — **지금 규약대로 재면 정상을 유실로 읽는다**

규약은 **「사라진 줄 0」만 정상**으로 적어 두었다. 그런데 **줄을 고치면**:

```
줄을 더하면   사라진 0 · 더한 N        ← 규약이 상정한 경우
줄을 고치면   **사라진 1 · 더한 1**    ← 유실처럼 보인다
```

2026-09-05에 `backlog` 682 한 줄을 고쳤더니 정확히 1/1 이 나왔다.

### 제안 문안

```markdown
⚠️ **④는 「줄을 고칠 때」 셈법이 다르다.** 줄을 **더하면** 사라진 0 이지만,
줄을 **고치면 사라진 1 · 더한 1** 이 나온다 — 옛 줄이 사라지고 새 줄이
더해진 것으로 잡히기 때문이다.

📌 **그때는 유실이 아니라 수정이다.** 사라진 줄과 더한 줄의 **앞 40자를
맞대어** 같은 항목인지 보면 갈린다.
```

---

## D. ✏️ 4-2 — 파이프는 **`git checkout` 만의 문제가 아니다**

**어디에**: 535행 `❌ git checkout -q B 2>&1 | tail -2` 블록

### 왜 — **그 절을 읽은 사람이 같은 날 다시 걸렸다**

지금 그 절은 **브랜치 체크아웃 이야기로만** 적혀 있다. 그래서 **파이프 일반의
문제로 안 읽힌다.**

2026-09-05에 `python3 tool/check_admin_sync.py | tail -3 ; echo $?` 가 **항상 0**
을 냈고, 하마터면 *"검출은 하는데 CI 는 통과한다"* 는 **없는 결함**을 보고할 뻔했다.
파이프를 걷으니 **깨끗할 때 0 · 변조 시 1** 로 정상이었다.

### 제안 문안

```markdown
🚨 **이것은 `git checkout` 만의 문제가 아니다.** 파이프를 걸면 `$?` 는 **마지막
명령의 것**이 되므로, **종료 코드를 보려는 명령에는 파이프를 걸지 않는다.**
출력이 길면 **파일로 받아서 본다.**

    ❌ cmd | tail -3 ; echo $?          →  tail 의 코드
    ✅ cmd > 로그 2>&1 ; echo $?         →  cmd 의 코드

⚠️ 2026-09-05에 `check_admin_sync.py` 를 이렇게 재서 **없는 결함을 보고할
뻔했다.** 위 절을 읽은 세션이 같은 날 걸렸다 — **브랜치 이야기로만 적혀 있어
자기 상황으로 안 읽혔다.**
```

---

## E. ✏️ 4장 — 「없다」를 **기억으로도** 말하지 마라

**어디에**: 260행 *"**「없다」를 grep 으로 말하지 마라.**"*

### 왜 — globe2030님 지시(*"실물 확인"*)에서 **딱 한 칸이 비어 있다**

규약 4장은 **계산 vs 실측**, **세는 도구**, **요약이 원본 행세**, **출처를 함께
옮긴다**를 다 적었다. 그런데 **「기억」이라는 단어가 없다.**

2026-09-05에 PM 이 *"제가 보낸 것은 넷뿐입니다 — 어디에도 그 문장은 없습니다"*
라고 했는데, **자기 메시지를 기억으로 센 것**이었다(실제로는 다섯이었고 그
문장은 있었다). ⚠️ **대화가 길어져 앞부분이 접히면 요약이 원본 행세를 하는데,
그것이 「내 기억」의 얼굴로 온다.**

### 제안 문안

```markdown
**「없다」를 grep 으로 말하지 마라.** — 🚨 **기억으로도 말하지 마라.**
자기가 보낸 것·자기가 쓴 것도 마찬가지다. 대화가 길어지면 앞부분이 요약으로
접히고, **그 요약이 「내 기억」의 얼굴로 원본 행세를 한다**(2026-09-05).
```

---

## F. ✏️ 4-2 — 기기를 배정하기 전에 **「그 자리에 갈 수 있는가」**를 잰다 🆕

**어디에**: 4-2 「실기기도 워크트리처럼 배정받는다」의 *"PM이 정할 것"* 목록

### 왜 (2026-09-06 · PM 이 겪음 — 나는 전달받았다)

기기를 배정했는데 **게이트가 꺼져 있어 그 화면에 못 갔다.** 기기는 워크트리보다
무거운 자원이라, **헛되이 잡으면 다른 세션이 못 쓴다.**

지금 그 목록은 *"어느 세션이 언제까지 · 누구에게 넘기는지 · 지금 어느 빌드가
깔려 있는지"* 인데, **「그 화면에 도달할 수 있는가」가 없다.**

### 제안 문안

```markdown
🚨 **그리고 「그 화면에 갈 수 있는가」를 먼저 잰다.** 스위치가 꺼져 있거나
기능이 아직 안 켜져 있으면 **기기를 잡아도 그 자리에 못 간다.**
⚠️ 확인은 **코드가 아니라 그 스위치의 실물**로 한다(`config/…` 문서가 있는지 등).
```

---

## 안 넣는 것 여덟 — **왜 안 넣는지**

| 나온 것 | 왜 안 넣나 |
|---|---|
| `git log --since=<날짜>` 가 0건을 낸다(시각을 붙여야) | **추가 664 「세는 도구가 못 보는 것」이 이미 덮는다.** 도구별 함정을 하나씩 적기 시작하면 끝이 없다 |
| 「이미 적혀 있다」 전에 `git log -S` | **「조회한 값은 조회한 순간의 것」이 덮는다.** ⚠️ 다만 결이 하나 새롭다 — **「이미 있다」가 「오늘 내가 넣은 것」일 수 있다.** 규약을 늘리기보다 **E 의 예시로 읽히면 된다** |
| `dart format` 을 디렉터리 단위로 돌리면 analyze 가 는다 / CI 는 `dart format` 을 안 본다 | **값이 낮다.** 겉모습이고 되돌리기 쉽다. ⭐ 다만 **3장의 「CI 가 못 보는 것」 목록에 한 줄** 더할 값은 있다 — globe2030님이 원하시면 |
| **실물 확인** — 기억·요약·전언으로 말하지 않는다 (globe2030님 지시) | 🚨 **안 넣는 것이 아니라 이미 4장 전체가 그것이다.** 계산/실측 · 세는 도구 · 요약이 원본 행세 · 출처를 함께 옮긴다 — 전부 이 지시의 다른 얼굴이다. **비어 있던 한 칸이 「기억」이고 그것이 E 다** |
| `brew uninstall` 이 의존성을 물고 간다(node 가 사라졌다) | **규약이 아니라 런북 소재다.** 「새 노트북에 원래 없던 것」이 아니라 **오늘 제거로 사라진 것**이라 1-1 절과 성격이 다르다(PM 확인) |
| 🆕 `git push` 성공은 「올라갔다」가 아니다 — PR 이 없으면 아무 데도 안 간다 | **규약 2장이 이미 *"push → `gh pr create`"* 를 절차로 적어 두었다.** 지키지 않은 사례이지 새 규칙이 아니다. ⚠️ 다만 **`ls-remote` 는 「푸시됐다」만 보여 준다**는 것은 알아 둘 값이 있다 |
| 🆕 막혔을 때 **배포 대상이 아닌 경로**를 파고들지 않는다(macOS 빌드) | **상황이 너무 특수하다.** 규약으로 적으면 *"막히면 딴 길로 가지 마라"* 같은 문장이 되는데, **그것은 판단이지 규칙이 아니다** |
| 🆕 세션에 지시하기 전에 **「누가 한 것인지」** 확인한다 | **4-2 의 *"옮겨 적을 때는 「출처」까지 함께 옮긴다"* 가 덮는다.** ⚠️ 다만 결이 하나 다르다 — 그때는 **「내용」의 출처**였고 이번은 **「일한 사람」의 출처**다. 같은 문장으로 읽히면 된다 |

---

## ⬜ 이 제안이 못 본 것

```
⬜ 오늘 다른 세션에서 나온 것이 더 있을 수 있다
   PM 이 준 열 가지 + 내가 겪은 것으로만 골랐다
⬜ 「고른다」의 기준이 「이미 덮는 절이 있는가」 하나다
   빈도·무게로 다시 고르면 답이 달라질 수 있다
⬜ 제안 문안이 실제로 읽히는지는 **넣어 봐야** 안다
   🚨 규약이 길어질수록 안 읽힌다는 것 자체가 아직 안 재진 주장이다
```
