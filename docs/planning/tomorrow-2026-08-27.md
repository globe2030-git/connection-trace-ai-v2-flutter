# 다음에 할 일 — 순서 (2026-08-27 정리)

> 📌 **왜 다시 짰나**: 2026-08-26 하루에 backlog 가 **500~520 스물한 개** 늘었다.
> 그중 여럿이 서로 얽혀 있어(하나를 고쳐야 다음 것이 보이는 구조가 세 번 나왔다)
> **번호 순서로 하면 안 되는 상태**가 됐다.
>
> ⚠️ **이 문서는 제안이다.** 순서 확정은 사용자 몫이다.

## 0-A. 🖐 **사용자 손이 필요한 항목** — 먼저 알고 시작하라

세션이 혼자 못 하는 것들이다. **모르고 착수하면 그 자리에서 멈춘다** —
2026-08-26에 실제로 그렇게 멈췄다.

```
A-1        구글 로그인 버튼 누르기        세션은 계정 생성·인증을 못 한다
⑫ 광고 동의  다른 계정 로그인 + 「시작하기」  🚨 자동 탭 금지 (추가 514)
D-2        알림톡 템플릿 심사 신청
모든 배포    firebase deploy
되돌릴 수 없는 조작   삭제 · 탈퇴 · 결제 · 동의 · 촬영
```

⚠️ **위 항목이 든 일감은 사용자가 함께 있을 때만 시작하라.** 아니면
**손이 필요 없는 부분까지만** 해 두고 멈추는 것이 낫다.

---

## 0. 지금 상태 (2026-08-27 실측)

```
열린 PR      0건
main         85db617
워크트리      본체 하나 · 로컬 브랜치 2개
기기          폴드 연결됨 · 아무도 안 잡음 · adb logcat 0건
             ⚠️ 앱이 **로그인 화면** 상태다 (아래 A 때문)
서버 사진     102건 (소급 업로드 완료분)
명함          103명
```

---

## A. 🚨 먼저 — **다른 구글 계정으로 로그인이 안 된다** (추가 520)

**이용자가 앱에 못 들어가는 문제다.** 다른 어떤 것보다 앞선다.

```
증상    로그아웃 → 다른 구글 계정 로그인 → "Google 로그인에 실패했습니다"
대안    없다 — 안드로이드에서 Apple 은 안 그려지고,
        카카오·네이버는 이 빌드에 키가 안 들어가 안 보인다
모른다  재현되는지 · 원인 · (release 빌드라 앱 로그가 안 나온다)
```

**순서**

```
A-1  사용자가 다시 눌러 보고 logcat 을 실시간으로 잡는다   ← 지금 빌드로 된다
A-2  재현되면 원인 판단
     🚨 debug 빌드는 서명이 달라 지우고 깔아야 하고, 명함 103장이 사라진다
     복원 경로는 08-26에 켠 것이라 **한 번도 실물로 돌려 본 적이 없다**
     → 다른 기기·다른 계정에서 먼저 재는 길부터 찾는다
```

⚠️ **A-1 은 사용자 손이 필요하다**(로그인은 세션이 못 한다).

---

## B. 이용자에게 닿는 결함 — 고치면 바로 값이 된다

```
B-1  추가 513   설정의 사진 백업 숫자가 앱 시작 시점 값이라 갱신되지 않는다
                🚨 명함을 등록하고 설정을 열면 **백업이 안 된 것처럼 보인다**
                ⚠️ 인과가 아직 추론이다 — 코드로 확정하고 고칠 것
                기기 불필요(코드) · 확인은 기기

B-2  추가 519   내보내기 공유 시트에 연락처 앱이 없다
                ⏳ **그것이 MIME 변경의 대가인지 미확정** — text/vcard 빌드가 한 번 필요
                ⚠️ 재기 전에 대안을 만들지 않는다

B-3  추가 510   GroupsRepository 를 만들자마자 clearLocal() 하면 지운 그룹이 되살아난다
                실사용 위험은 낮다(저장소가 오래 살아 있다) — **미수정**으로 남아 있다
```

---

## C. 🚨 아직 안 잰 것 — **모르는 것을 아는 것으로 바꾼다**

⭐ **B 보다 이쪽이 값질 수 있다.** B 는 고치는 일이고 C 는 **판단의 근거를 만드는 일**이다.

```
C-1  명함 수 ↔ AI 사용 횟수 상관을 잰다
     🚨 08-26 에 정한 **사진 한도 2,000장의 유일한 근거**인데 아무도 안 쟀다
     반대일 수도 있다 — 명함만 쌓고 AI 는 안 쓰는 사람
     테스터 계정 15개에 명함 수와 AI 사용 기록이 다 있다
     기기 불필요 · **결과에 따라 08-26 결정을 뒤집을 수 있다**

C-2  App Check 토큰 반복 실패 · OCR 통계 쓰기 permission-denied
     08-26 logcat 에서 눈에 띄어 "범위 밖"으로 넘겼는데 **어디에도 안 적혔다**
     ⚠️ 무엇인지도 모르는 상태다 — 먼저 적고, 그다음 볼지 정한다
```

---

## D. 사용자 결정이 필요한 것

```
D-1  🚨 auth_gate 계정 전환 잔존물 — 앞 계정 사진을 지울지
     그 자리 주석: *"서버 사진 백업이 꺼져 있어 지우면 되살릴 방법이 없다.
     … 켤 때 이 판단을 다시 봐야 한다"*
     **08-26 에 켰다. 그 예약이 만기가 됐다.**
     ⚠️ 법무 회신(질문 9-④6)에 근거한 결정이라 뒤집으려면 그 판단을 다시 봐야 한다

D-2  알림톡 템플릿 심사 신청 — **사용자만 할 수 있다**
     ⚠️ 심사에 10 영업일. 늦추면 완료가 그만큼 밀린다

D-3  웹·앱 목표일 — 종전 9/30 에서 뒤로 미뤄졌으나 **새 날짜 미정**
     ⚠️ 정해지기 전에는 날짜로 순서를 짜지 않는다
```

---

## E. 밀려 있는 것 (급하지 않음)

```
E-1  회원가입 화면 셋 캔버스 설계 — 이중등록 · 휴대폰번호 변경 · 번호 인증 가입
     여러 번 약속하고 계속 밀렸다. 알림톡 심사 통과 후에 필요해진다
E-2  계정 식별 매트릭스에 「번호 앵커」 열 추가하고 8가지 경우를 다시 돌리기
E-3  추가 516 옆 — 이미 저장된 「담당자」가 몇 건인지 세는 방법
     ⚠️ 진짜 직함이 「담당자」인 사람이 있어 일괄 삭제는 안 된다
```

---

## ⚠️ 이 문서를 쓰면서 지킨 것

**「모른다」를 「모른다」로 적었다.** 520 의 원인, 519 의 대가 여부, 513 의 인과,
C-1 의 상관, C-2 가 무엇인지 — 다섯 곳이다.

📌 2026-08-26 에 반복해서 데인 자리가 그것이다 — *"계산은 틀려도 그럴듯해
보인다"*, *"잰 것도 재현되지 않으면 계산과 같다"*, *"본 것과 결론 사이에
한 칸이 있는데 그 칸을 안 봤다"*.
