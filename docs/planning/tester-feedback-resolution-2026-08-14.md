# 테스터 피드백 조치 결과 통합 문서 (2026-08-14 기준)

빌드6·7 테스터 피드백 **통합본**(`커넥션센스_앱_빌드6_7_테스트_피드백_통합_정리.docx`,
오류 14 · 기능개선 15 · OCR 권고 7 · 확인 2 = 38건)의 각 항목이 **어떻게
수정·개선되었는지**를 한곳에 모은 문서다. 실물 원본은 제3자 개인정보(테스터 실명)를
담고 있어 저장소에 넣지 않는다 — 이 문서는 항목 ID와 조치 내용만 담는다.

- **번호 기준**: 모두 **통합본** ID다. 과거 대조표(backlog 추가 188)와 번호가
  다르므로, 인용할 때는 반드시 "통합본 E-05"처럼 출처를 붙인다(근거: 추가 203).
- **근거**: 각 항목 끝의 `추가 N`은 `docs/planning/backlog.md`의 시간순 상세 기록이다.
  "코드는 맞는데 실물이 틀린" 유형이 반복돼(CLAUDE.md 4절), 조치마다 **무엇으로
  확인했는지**를 함께 적었다.

---

## 한눈에 보기

| 구분 | 총 | 종결 | 부분 | 남음 |
|---|---|---|---|---|
| **오류 (E)** | 14 | **12** | E-01·E-06(부분) | E-04·E-12 |
| **기능 개선 (F)** | 15 | **9**(F-02·F-04·F-05·F-06·F-07·F-09·F-11·F-12·F-15) | F-01(부분) | 5 |
| **OCR 권고 (R)** | 7 | R-01 | R-02·R-04·R-06·R-07(부분) | R-03·R-05 |

**오류 14건 중 12건 종결.** 그중 **E-02·E-13 두 건은 "고칠 것이 없었다"** — 빌드6
이후 이미 고쳐져 있었고 기록만 없었다. 목록을 실물과 맞추지 않으면 없는 문제를
쫓게 된다는 교훈(추가 207).

**측정 방식을 바꾼 것이 이번 개선의 뼈대다.** 명함 인식은 "채움률"이 아니라
**정확도**로 재도록 검수 도구와 채점기를 만들었다(R-01/R-07). 과거 "오분류 0장"은
값의 **모양만** 본 수치였다(정정: 추가 198).

---

## 오류 (E) — 14건 중 12건 종결

### E-01 · 자동 촬영이 빈 공간을 찍음 🟡 부분

- **증상**: 명함이 아닌 빈 벽·책상·모서리가 자동 촬영된다.
- **원인**: 자동 촬영 조건이 **"화면이 흔들리지 않으면"** 하나뿐이었다. 역설적으로
  아무것도 없는 면이 가장 안정적이라 더 잘 찍혔다.
- **조치**: 안정 조건에 **"가이드 안에 볼 것이 있는가"**를 더했다 — 밝기 표준편차
  (민무늬 면 차단)와 지배톤 비율(반반으로 갈린 모서리 차단). 계산을
  `core/utils/frame_contrast.dart`로 빼 7가지 장면을 테스트로 고정.
- **남은 것**: 각이 진 빈 곳은 밝기로 못 가른다(실측: 명함 톤 0.653 vs 책상 0.611,
  차이 0.04). **문서 경계 검출**(R-02)이 필요하다. → R-02와 한 덩어리.
- 근거: 추가 208·209·210.

### E-02 · 뒷면 스캔 버튼 무반응 ✅ 종결 (고칠 것 없었음)

- **결과**: 빌드6 이후 **이미 고쳐져 있었다.** 2026-08-14 실기기에서 정상 확인.
  기록만 없어 남은 일로 보였던 항목. 근거: 추가 207.

### E-03 · 앞·뒷면 결과 덮어씀 ✅ 종결

- **증상**: 뒷면을 스캔하면 앞면 인식 결과가 지워진다.
- **조치**: `ScanConflict` 도입 — 한 명함을 앞면+뒷면으로 다루고, 서로 다른 값이
  들어오면 덮어쓰지 않고 감지·초기화한다. 근거: 추가 190.

### E-04 · OCR 문자 오독 (`길`→`킬`) ⬜ 남음

- **증상**: 인식된 글자 자체가 틀린다(`길`을 `킬`로).
- **성격**: ⚠️ **파싱 규칙으로는 고칠 수 없다.** ML Kit **인식 단계**의 문제다.
  `02-2000-0000`→`022-…` 같은 **형식** 오류는 이미 잡았지만(추가 197), 글자 오독은
  다른 층이다. **R-03(한글·영문 다중 OCR 패스)**이 유일한 길. 근거: 추가 207.

### E-05 · 필드 매핑 오류 ✅ 종결 (대폭 개선)

이번 통합본에서 가장 크게 개선된 항목. 사용자 진단이 출발점이었다 — *"OCR 추출은
정확히 하는데 분리하는 과정에서 잘못되는구나."* 실제로 이름 미검출 17장 중 16장은
원문에 이름이 있었다.

- **원인 1 — 자리를 뺏기면 끝**: 이름 후보 검사가 줄 순회가 끝난 뒤에만 돌아,
  슬로건 조각이 먼저 자리를 차지했다가 지워지면 진짜 이름을 다시 볼 기회가 없었다.
  → **"넣고 → 빈자리 재검증"** 구조로 전환(추가 199).
- **원인 2 — 이름 후보가 선착순**: OCR이 잘못 읽은 슬로건 끝자락이 먼저 이름이 되고
  진짜 이름은 회사 칸으로 밀렸다. → 이름 후보를 **강·약 두 갈래**로 나눠 강이
  있으면 약은 쓰지 않는다. 틀린 이름 14장이 정답으로 교정(추가 200).
- **원인 3 — 한 줄로 뭉친 명함**: 전화·이메일을 뽑은 뒤 `continue`해서 주소·회사·
  이름 검사를 아예 건너뛰었다. → 연락처를 걷어낸 나머지를 **같은 루프에 다시**
  넣는다(추가 201).
- **개선 측정(103장)**: 이름 82→**100**, 주소 76→**85**, 휴대폰 97→**100**.
  회사는 89~93 사이에서 **틀린 값(사람 이름)이 빠지고 정답이 채워지는** 방향으로
  이동 — 채움 장수가 아니라 정확도로 읽어야 한다.
- 근거: 추가 199·200·201·202.

### E-06 · 초점 안 맞은 채 OCR 🟡 부분

- **조치(완료분)**: 카메라 흰 화면 제거(뷰파인더 배경을 검정으로 + "카메라를
  준비하는 중이에요" 안내), 크롭 여백·흔들림 기준 실측 교정.
- **남은 것**: 초점 판정 자체는 **R-02 촬영 품질 게이트**에서. 근거: 추가 208.

### E-07 · 프로필 사진 미리보기가 안 바뀜 ✅ 종결 (실기기 확인)

- **증상**: 갤러리에서 사진을 골라도 미리보기가 그대로. **껐다 켜면 정상.**
- **원인**: `FileImage`의 **캐시 키가 파일 경로**다. 프로필 사진은 매번 같은
  파일명(`my_profile_avatar.jpg`)으로 덮어써서, 내용이 바뀌어도 캐시가 옛 픽셀을
  돌려줬다. `setState`가 소용없던 이유.
- **조치**: `evictImageFileCache(path)` — 덮어쓴 직후 그 경로를 캐시에서 비운다.
  같은 함정이 **SNS 로그인 시 프로필 내려받기**에도 있어 함께 고쳤다.
- **검증**: 캐시에서 실제로 빠지는지 보는 테스트(`test/image_file_cache_test.dart`),
  **사용자 실기기 확인 완료**("미리보기도 잘 나와"). 근거: 추가 206.

### E-08 · 직업과 무관한 AI 태그 ✅ 종결 (실기기 확인)

- **증상**: 회계사 명함을 등록했는데 태그에 `AI`, `IT`가 자동으로 들어갔다.
- **원인**: 명함 등록 화면 태그 입력칸 **기본값이 `'AI, IT'`로 하드코딩**돼 있었다.
  신규 등록이면 업종과 무관하게 무조건 박혔다 — 데모용 값이 남은 것.
  "가짜 데이터를 만들지 않는다"(CLAUDE.md 4절)에 정면으로 걸리는 결함.
- **조치**: 기본값을 빈 값으로. 회귀 방지로 **소스를 훑는 자동 테스트**
  (`test/no_seeded_form_defaults_test.dart`) 추가.
- **기존 데이터 정리**: 관리자가 눌러 실행하는 일회성 정리(태그가 정확히 `AI`+`IT`
  둘뿐인 명함만). 실기기 실행 결과 **등록 14명 중 13건**이 대상이었다.
  근거: 추가 204.

### E-09 · 카카오톡 실행 실패 ✅ 종결 (사용자 실기기 확인)

- **증상**: 카카오톡 연락 시 "카카오톡을 열지 못했습니다"만 뜨고 실행 안 됨.
- **원인**: 흔한 원인(매니페스트 스킴 선언)은 **이미 막혀 있었다.** `adb`로 인텐트를
  직접 쏴 보니 `kakaotalk://`(경로 없는 형태)를 카카오톡이 해석하지 못했다 —
  틀린 것은 **주소 문자열 하나**였고, 카카오톡 쪽 사정이라 저장소 어디에도 근거가
  없었다("코드는 맞는데 실물이 틀린" 유형).
- **조치**: `kakaotalk://launch`(동작 확인됨)를 먼저 시도하고, 실패하면 옛
  `kakaotalk://`로 폴백. 근거: 추가 205.

### E-10 · 메모 후 키보드 안 닫힘 ✅ 종결

- **증상**: 'AI에 보낼 정보' 메모 입력 뒤 키보드가 유지돼 바텀시트 위쪽 스크롤 불가.
- **원인**: 그 칸은 `maxLines: 3`. Android 멀티라인 입력칸은 키보드에 **완료 키
  대신 줄바꿈 키**가 떠서 빠져나올 방법이 없었다.
- **조치**: 스크롤 뷰에 `keyboardDismissBehavior: onDrag`(끌면 닫힘)를 **멀티라인
  칸이 있는 바텀시트 4곳**에 적용 + 제보 화면엔 빈 곳 눌러 닫기 추가. 근거: 추가 206.

### E-11 · 지도 연속 터치 시 멈춤 ✅ 종결

- **조치**: 위치 흐름 재진입 차단. 근거: 추가 173.

### E-12 · 현재 위치 정확도 ⬜ 남음

- **성격**: 위치 정확도·갱신 시각을 함께 봐야 한다. E-13이 정상이 된 지금
  **재현 조건부터 좁혀야 한다.** 근거: 추가 207.

### E-13 · 지도 위치 갱신 실패 ✅ 종결 (고칠 것 없었음)

- **결과**: 추가 174의 수정(측위 시간 + 최근 위치 폴백)이 유효했다. 2026-08-14
  실기기에서 정상 확인. 근거: 추가 207.

### E-14 · '주변' 검색 미작동 ✅ 종결

- **조치**: 범위 안내 + 원인별 빈 상태 — "안 되는 것처럼 보이던" 문제였다.
  근거: 추가 176.

---

## 기능 개선 (F) — 15건

⚠️ **현재까지 F는 본격 착수 전이다.** 아래는 코드로 확인한 현재 상태다.

| ID | 내용 | 상태 |
|---|---|---|
| F-01 | 앞·뒷면 일괄 등록 + **필드별** 충돌 선택 | 🟡 부분 — 다른 명함 감지·초기화는 됨(추가 190). **필드별 선택은 미구현** |
| F-02 | 주소 없는 명함 등록 허용 | ✅ **종결**(2026-08-15 main 실물 대조) — 라벨의 필수 표시(`*`)가 없고, 주소가 비면 **"주소 없이 저장할까요?"** 확인 후 저장된다(`add_card_modal_view.dart:1265~`). 침묵 저장은 안 한다 — 뒷면에 주소가 있는데 앞면만 보고 등록하는 실수를 막기 위해 |
| F-03 | 스캔 이미지 자르기·회전 편집 | ⬜ 남음 — 자동 크롭만. "크게 보기"는 보기 전용 |
| F-04 | 자동 추출 정보와 내 메모 분리 | ✅ **종결**(추가 223) — "명함에서 읽은 정보" / "내가 덧붙이는 정보" 헤더로 분리. ⚠️ 이 과정에서 **메모 칸에 자동 문구가 박히던 결함**을 발견·제거 |
| F-05 | '명함 스캔' → '명함 추가' 명칭 | ✅ **종결(고칠 것 없었음)** — 화면에 노출되는 라벨은 이미 **'명함 등록'**이다. `'명함 스캔'`은 코드 주석에만 남은 옛 버튼 이름(추가 221) |
| F-06 | 삭제 발견성 + 일괄 삭제 | ✅ **종결**(2026-08-15 main 실물 대조) — 지갑에 **선택 모드**가 있다(`wallet_view.dart:41~` `_selectionMode`/`_selectedIds`, 상단 "N개 선택", 하단 삭제 바). 스와이프 삭제도 그대로 남아 단건은 빠르게. 추가 226의 테스터 초기화 안내가 이 경로("지갑 → 선택 → 전체선택 → 삭제")를 전제로 쓰여 있다 |
| F-07 | 대화 포인트 재생성 다양성 | ✅ **종결 — 배포·실기기 확인 완료**(추가 221). 재생성 시 3개 문장이 전부 달라지는 것을 실기기로 확인 |
| F-08 | 'AI에 보낼 정보' 수정·재진입 | ✅ **종결**(추가 237) — 브리핑 화면에 "다시 보기·수정" 통로 신설 + 직전 선택·메모를 기억(기기 저장 안 함, 앱 실행 중에만). **열어 보기만 하면 과금 없음.** 실기기 부분 테스트로 메모 복원·과금 없음 확인 |
| F-09 | 읽기 전용/입력 영역 시각 구분 | ✅ **종결**(추가 223) — 자동 인식이 채운 칸에 옅은 배경 + ✨ 표시, 고치면 사라짐. 실기기 확인 |
| F-10 | AI를 **관계 관리 지원**으로 확장 | ✅ **구현 완료 · 부분 테스트 통과**(추가 234) — A('주변' 상단 "오늘 연락하면 좋은 사람", 기기 안 계산) + C(연락 직후 한 탭 후속) + C→A 되먹임. 스펙: [`f10-reconnect-loop-spec-2026-08-15.md`](./f10-reconnect-loop-spec-2026-08-15.md). ⚠️ **A에 후보가 실제로 뜬 모습은 미확인**(기기 명함이 전부 전날 등록이라 30일 초과 후보 없음) · **저장 구조가 바뀌어 배포 전 전체 테스트 필요**(게이트 G1에 묶음) |
| F-11 | 메인/검색 결과 UI 분리 | ✅ **종결**(추가 225) — 검색 중에는 "지금 가까운 사람" 카드·위치 조작 줄을 감추고 제목을 "검색 결과 (N명)"으로. 실기기 확인 |
| F-12 | 검색어 지우기 + 가까운 인맥 주소 | ✅ **종결**(2026-08-15 main 실물 대조) — X 버튼(추가 194) + **주변 타일에 주소 한 줄**(`radar_view.dart:1529~`, 주소 없으면 줄 자체를 숨김). 묶음 안에서는 머리글에 이미 있어 줄마다 반복하지 않는다(F-15). ⚠️ **지갑 타일에는 주소 텍스트를 넣지 않았다** — 거기엔 "주소 없음"·"위치값 없음" 배지만 있다. F-12 원문이 "가까운 인맥"만 말하므로 범위 밖으로 뒀다. 지갑에도 원하면 별건으로 요청 필요 |
| F-13 | 지도 기준 위치 직접 지정 | ⬜ 남음 |
| F-14 | 지도 제스처·Quick Action 통일 | ⬜ 남음 |
| F-15 | 같은 주소 인맥 묶어 표시 | ✅ **종결**(추가 225) — 도로명까지 같으면 묶고 머리글에 "N명". ⚠️ 실제 데이터에 같은 주소 짝이 없어 **묶음 머리글은 실기기 미확인**(위젯 테스트로 대체) |

**남은 3건**: F-03(스캔 이미지 자르기·회전) · F-13(지도 기준 위치 직접 지정) ·
F-14(지도 제스처·Quick Action 통일).
F-01은 부분(필드별 충돌 선택 미구현). F-10·F-08은 2026-08-15 종결
(추가 234·237). **F-13·F-14는 지도 세션에 인계**
([`handoff-map-session-2026-08-15.md`](./handoff-map-session-2026-08-15.md)).

⚠️ **2026-08-15부터 F 계열은 다른 세션이 맡는다**(사용자 지시). 착수 전
반드시 최신 main을 받을 것 — F-11·F-15가 `radar_view.dart`를, F-04·F-09가
`add_card_modal_view.dart`를 크게 고쳤고, F-08·F-13·F-14가 그 파일들을 건드린다.

### ⚠️ 이 표가 실물보다 뒤처졌던 적이 있다 (2026-08-15 대조)

F-02·F-06·F-12가 **이미 main에 들어가 있는데 표에는 "남음/부분"으로 남아
있었다.** 세 건 다 해당 브랜치(`feat/f02-address-optional`·
`feat/f06-multiselect-delete`·`feat/f12-nearby-address`)가 병합됐는데 표만
안 따라왔다. 담당 세션이 여럿이라 **코드를 넣은 사람과 표를 고치는 사람이
달랐던 것**이 원인이다.

이게 왜 위험한가: 이 저장소에서 낡은 기록("Apple 로그인 미구현") 때문에 **이미
구현된 기능을 다시 검토한 전례**가 있다(CLAUDE.md 4절). F-06을 "남음"으로 믿고
다시 만들면 선택 모드가 두 벌이 된다.

**그래서 F를 착수하기 전에는 표를 믿지 말고 main 실물을 먼저 열어 볼 것.**
이 대조는 2026-08-15 기준이며, 다음 사람은 다시 확인해야 한다.

---

## OCR 개선 권고 (R) — 7건

| ID | 내용 | 상태 |
|---|---|---|
| R-01 | 원문 추출과 필드 매핑 **분리 진단** | ✅ TSV 원문 칸 + 검수 도구(추가 199) |
| R-02 | 촬영 품질 게이트·전처리 | 🟡 부분 — 흰 화면·크롭·흔들림 확정. **문서 경계 검출 미착수** (E-01·E-06 잔여분이 여기 흡수) |
| R-03 | 한글·영문 다중 OCR 패스 | ⬜ 남음 — **E-04를 푸는 유일한 길** |
| R-04 | 도메인 규칙·검증기 후처리 | 🟡 부분 — 지역번호·우편번호·라벨 반영. 정규화/표시값 분리는 미착수 |
| R-05 | 레이아웃(좌표) 기반 분류 | ⬜ 남음 — 지금 파서는 좌표 미사용. `_parse` 입력 형태부터 바꿔야 함 |
| R-06 | 필드별 신뢰도·충돌 검토 | 🟡 부분 — 충돌 감지·빈자리 재검증은 됨. **신뢰도 보존 미착수** |
| R-07 | 데이터셋 + **필드별** 품질 지표 | 🟢 정답지 17장 첫 채점 완료 — **전체 94%**(추가 213). 정답지 축적은 계속 |

**측정 도구가 이번 세션의 핵심 산출물이다:**
- **검수 도구**(`tool/ocr_review/index.html`) — 실물과 대조해 정답지를 만든다.
  로컬 전용(네트워크 없음).
- **재생 하네스** — TSV의 원문(rawLines)을 맥에서 파서에 다시 먹여, 기기 없이
  몇 초 만에 측정. 721개 값 중 710개가 기기와 동일해 충실도 확인.
- **채점기**(`test/ocr_accuracy_score_test.dart`, R-07) — 정답지가 생기면 필드별
  정확도를 집계. 없는 값은 분모에서 제외(둘 다 빔)해, "없는 것을 못 찾았다"고
  점수를 깎지 않는다.

---

## 남은 일 (근거 있는 순서)

1. **검수 도구로 정답지 만들기(R-07)** — 이후 모든 인식 수정을 **정확도로** 재는
   전제. (실물 명함 대조라 사람 손 검수가 필요)
2. **R-02 문서 경계 검출** — E-01·E-06 잔여분을 흡수한다.
3. **E-12 위치 정확도** — 재현 조건부터 좁힌다.
4. **E-04** — R-03(다중 OCR 패스)로만 풀린다.
5. **기능 개선(F) 착수** — F-02 → F-12 순.

---

*이 문서는 backlog 추가 173~211과 HANDOFF 0-16·0-17을 근거로 정리했다. 세부 경위·
코드 위치는 각 항목의 `추가 N`을 `docs/planning/backlog.md`에서 찾으면 된다.*
