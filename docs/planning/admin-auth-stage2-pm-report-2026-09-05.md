# 관리자 인증 2단계 — 완료 보고 (PM 전달용)

작성: **개발C(터미널)**, 2026-09-05
브랜치: `feat/admin-auth-stage2` (본체 워크트리
`/Volumes/Work/Claude/connection-trace-ai-v2-flutter`)
설계: [`specs/관리자-인증-강화와-명함-조회-설계-2026-09-05.md`](./specs/관리자-인증-강화와-명함-조회-설계-2026-09-05.md) §5

---

## 1. 한 줄

**설계 2단계는 코드로 끝났다. 다만 스위치가 꺼져 있고, 그것은 의도다.**

---

## 2. 무엇을 했나

개발A 가 Callable 셋까지 하고 중단한 것을 이어받아 `firestore.rules` 쪽을 마쳤다.

```
① adminSessions/{uid}    allow read, write: if false     ✅ (개발A 가 써 둔 것을 커밋)
② config/admins          allow read, write: if false     ✅ (같음) 🚨 읽기도 막는다
③ isAdmin() 에 세션 신선도                                ✅ 개발C 가 넣음 — 스위치 뒤에
   + rules 테스트 11건                                    ✅ 개발C
```

⚠️ **①② 는 개발C 가 쓴 것이 아니다.** 인수인계 문서에는 *"rules 를 한 줄도
안 건드렸다"* 라고 적혀 있었는데, 실물에는 **미커밋 상태로 이미 써 있었다.**
개발C 가 한 것은 **커밋한 것**이고, 새로 쓴 것은 ③ 과 테스트다.
📌 출처를 갈라 적는다 — 뭉개면 다음 사람이 개발C 가 다 쓴 줄 안다.

---

## 3. 🚨 PM 이 알아야 할 것 — 「끝났다」가 「켜졌다」가 아니다

```
function adminSessionRequired() { return false; }   ← firestore.rules 안의 스위치
```

**켜는 것은 설계 4단계(콘솔 인증 화면) 배포와 같은 배포여야 한다.** 지금 켜면
관리자가 아무것도 못 한다 — 인증할 화면이 없기 때문이다.

### 왜 주석이 아니라 스위치인가

인수인계에는 *"③ 은 화면과 함께 배포해야 한다"* 라고만 적혀 있었는데,
**그것을 지킬 수단이 없었다.**

```
firebase deploy --only firestore:rules  는 이 파일을 통째로 올린다
   → cardSources 규칙이 미배포라 누군가 곧 이 명령을 돌린다
   → 그때 ③ 이 딸려 나가면 관리자가 전부 잠긴다
```

⭐ **「안 나가게」는 지킬 수 없으므로 「나가도 안 켜지게」로 바꿨다.**
꺼져 있는 동안은 `||` 가 앞에서 끊겨 `get()` 이 아예 안 돈다(조회 비용 0).

📌 **그래서 이 브랜치는 지금 배포해도 안전하다.** ①② 는 아무도 안 쓰는
컬렉션을 잠그는 것이고, ③ 은 꺼져 있다. 그 「안전함」자체를 테스트로 고정했다
(*"꺼져 있는 동안은 세션 없이도 관리자 조작이 된다"* ALLOW 케이스).

---

## 4. 🚨 인수인계가 틀렸던 곳 하나 — 실물이 잡았다

```
문서   rules 는  request.time < expiresAt  한 줄로 비교만 한다
실물   expiresAt 은 Timestamp 가 아니라 epoch 밀리초 「숫자」다
       functions/src/index.ts writeAdminSession → adminAuth.ts:167 이 number 를 돌려준다
```

- **증상**: 관리자가 아무 조작도 못 한다
- **원인**: 타입이 달라 비교가 **오류**가 되고, rules 의 오류는 곧 **거부**다

로그에는 「거부」로만 남고 **왜** 거부인지는 안 남는다.
→ `request.time.toMillis() < expiresAt` 로 썼다.

### ⚠️ 이것이 왜 안 잡힐 뻔했나 (PM 이 다른 세션에 옮길 값이 있는 대목)

음성 대조군으로 틀린 규칙을 일부러 넣어 봤다.

```
DENY 케이스 셋   전부 통과했다  ← 오류 → 거부라서 기대와 결과가 같다
ALLOW 케이스 하나 여기서만 터졌다
```

> **「막히는지」만 재면 「잘못 막히는 것」은 안 보인다.**

---

## 5. 검증 (전부 실측, 2026-09-05)

```
verify_rules.py        86/86 통과   Firebase 규칙 평가 API (신규 11건 포함)
                       ⭐ 켠 상태 5건은 「스위치만 켠 사본」으로 잰다 — 파일은 안 고친다
check_admin_sync.py    일치 (2개)
flutter analyze        info 11 · error·warning 0     기준선과 같음
flutter test           +1988 ~4                      로컬 macOS · Flutter 3.44.8
                       ⚠️ CI(ubuntu) 기준선 1,944 와 잰 곳이 다르다
```

**⬜ 안 한 것**: `npm test`(functions) 는 이 세션에서 안 돌렸다 — 서버 코드를
안 건드렸다. 개발A 가 260건 통과를 실측해 뒀다.

---

## 6. PM 이 정해 줄 것

```
① backlog 번호      아직 안 썼다. 4-2절대로 PM 배정을 기다린다
                    ⚠️ 개발C 가 「다음 빈 번호」를 스스로 집지 않았다
② PR 을 열까         커밋만 하고 멈춘 상태다 (globe2030님 지시로 중지)
③ rules 배포 시점    🚨 아래
```

### 🚨 ③ 은 시간에 민감하다

`cardSources` 규칙이 `main`(#833)에 있는데 **서버에 안 올라갔다.** 그동안
등록되는 명함은 **파싱 원본이 안 남고, 나중에 배포해도 되살릴 수 없다.**
⚠️ `firebase deploy --only firestore:rules` 는 **rules 전체**를 올리므로 다른
세션의 미배포 변경도 함께 나간다 — **배포 자체는 globe2030님 결정**이다.

📌 **이 브랜치가 배포에 얹히는 것은 이제 안전하다**(§3). 막고 있던 것이
하나 줄었다.

---

## 7. 🚨 아직 안 갈린 것 — 2단계와 무관하지만 함께 넘긴다

**계정 안내 갈래가 두 갈래다.** 개발A 가 적어 둔 것을 그대로 옮긴다.

```
globe2030님이 개발 세션에 직접   "그래도 다른 계정을 하겠다면 어쩔 수 없지만"
                                → 설계: 「새 계정으로 계속하기」를 막지 않음
PM 이 전한 「병」안               선택지는 「잇기」뿐
```

**화면 하나가 아니라 방침의 차이다.** 설계 문서 §4 는 아직 「막지 않는다」로
쓰여 있다 — 안 갈린 채로 5단계에 들어가면 **설계서를 그대로 구현하게 된다.**

**그리고 C안 1단계는 아직 아무도 안 잡고 있다.** PM 이 전한 순서가 「C안 먼저」
였으므로, *"누가 하고 있겠지"* 로 읽히기 쉽다 — 실제로는 비어 있다.

---

## 8. 점유 상태

```
워크트리   본체 — 개발C 가 잡고 있었다. 커밋까지 하고 **손 뗀다**
브랜치     feat/admin-auth-stage2 (푸시함)
만진 파일   firestore.rules · test/firestore_rules/verify_rules.py
           docs/planning/RESUME.md · 이 파일
기기       안 썼다
```
