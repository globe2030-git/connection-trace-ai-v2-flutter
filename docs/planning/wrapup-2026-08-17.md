# 마무리 — 2026-08-17 (인식률·촬영 담당 세션)

이 문서는 **오늘 한 일 · 남은 일 · 최근 3일 흐름**을 한자리에 모은 것이다.
다음에 이 저장소로 돌아오는 사람이 **여기부터 읽으면** 된다.

---

## 0. ⭐ 오늘의 한 줄

> **파서와 정답지가 같은 실수를 함께 하면 채점은 만점을 준다.**

사무실 전화가 **98%**로 보였다. 파서가 팩스를 사무실 칸에 넣고 있었는데,
**정답지도 팩스를 사무실 칸에 적어 놓아서** 서로 맞아떨어지고 있었다. 자를
고치니 같은 파서가 **91%**로 내려갔다.

⚠️ **내려간 것이 나빠진 것이 아니다.** 오늘 그 자리를 **네 번** 만났다.

---

## 1. 오늘 한 일

`analyze 19`(기준선 유지) · `test 692`(아침 646 → +46) · 커밋 28개

### 인식 정확도

| | | 결과 |
|---|---|---|
| **추가 279** | 팩스·홈페이지를 인식이 뽑게 함 | 홈페이지 **100%** · 팩스 틀림 0 |
| **추가 280** | 103장 전수 측정 + 원인 분류 | 사용자가 138곳 검수 |
| **추가 278** | 회사 칸 고르기 | 60% → **68%** |
| **추가 282** | 팩스 라벨을 번호마다 본다 | 팩스 35% → **97%** · 사무실 91% → **96%** |
| **추가 286** | 채점 자를 고치고 기준선 확정 | 전체 **86%** |
| **추가 288** | E-04 다중 OCR | ⚠️ **잴 길이 없어 보류** |

### 촬영·등록

| | | |
|---|---|---|
| **추가 290** | F-03 손으로 자르기 | 자동이 틀렸을 때 고치는 유일한 길 |
| **추가 293** | 검출을 빈 화면 막기에 씀 | **E-01·E-06 닫힘** |
| 〃 | 검출 테두리로 자르기(B′) | 배경 없앰 — 출발점이던 문제 |
| 〃 | 발열 | 검출만 초당 8 → 3회 |
| 〃 | X 터치 불가 | ⚠️ **오늘 내가 만든 결함** |
| 〃 | 가이드 크기·표시 | 0.86 → 0.74 · 상자 하나로 · 고정 |
| 〃 | 면 관리 | 빼기 + **세 장째 확인** |
| 〃 | 넘침 | 원문 칩 폭 제한 |

### ⭐ 오래 못 풀던 물음을 풀었다 — B′ vs 고정 가이드

같은 명함 **13장을 두 번씩** 실물로 찍어 쟀다.

```
고정 가이드   94 / 121  →  77%
B′ 검출       92 / 121  →  76%
```

**121칸 중 2칸 — 사실상 같다.** 갈린 6칸은 **전부 OCR 오독**이었다
(`최태응 ↔ 최태웅`). **같은 명함을 두 번 찍으면 글자가 다르게 읽힌다.**

📌 그래서 B′는 **인식률 때문이 아니라 배경 때문에** 켠다. 결정 축이 다르다.

⚠️ 추가 277에서 세 번 재고도 판정을 못 낸 이유가 이제 분명하다 —
**표본 5~6장에 정답지가 틀려 있었다.**

### 그 밖에

- main에서 사라진 backlog 항목 **여섯을 복구**했다(다른 세션의 병합 사고)
- `~/card-ocr-data/`·검수 폴더 정리 + README 신설(권한 700·600)

---

## 2. 남은 일

### 지금 바로 할 수 있는 것

| | 무엇 | 왜 지금 |
|---|---|---|
| **회사 66% · 직함 58%** | 가장 낮은 두 칸 | **정답지가 진짜가 됐으니** 이제 제대로 잰다 |
| 배터리·발열 실측 | 검출을 켠 상태로 몇 분 | ⚠️ 아직 **안 쟀다** |

⚠️ **직함은 규칙부터 정해야 한다.** 오늘 세 번 시도해 셋 다 0 이하였고, 남은
오류의 상당수가 *"파서가 맞힐 수 없는 정답"*이었다(추가 286).

### 조건이 갖춰지면

| | 무엇 | 막고 있는 것 |
|---|---|---|
| **E-04 오독** (추가 288) | 다중 OCR 패스 | ⚠️ **잴 길이 없다** — 채점기가 OCR을 다시 돌리지 않는다. 103장 재스캔이 필요하다 |
| R-05 좌표 분류 (추가 278) | 회사 칸 | 회사 오류 26건이 *"읽었는데 못 고름"* |
| 아이폰 측정 | profile에서 측정본 저장 | ⚠️ `kDebugMode` 조건 때문에 안 돈다. 넓히려면 **평문 사진 경로를 넓히는 판단**이 필요하다 |

### 지우기로 예정된 것

```
measure_sample_sink.dart + 검사 + 스위치     ⚠️ 측정이 끝나면 통째로 지운다
                                            (평문 명함 사진을 남기는 길이다)
[측정] 스위치 넷                             결정이 굳으면 정리
~/card-ocr-data/보관-2026-08-17/ (29개)      사용자가 "며칠 두고 보자"고 함
```

### 열려 있는 물음 — 사용자 결정 대기

```
직함 정답 규칙        부서·구분자·영문 병기를 어디까지 넣나
로고 규칙            이미 정함("인쇄된 그대로") — 정답지 통일 완료
배포 시점            테스터 배포에 무엇을 넣나
```

---

## 3. 최근 3일 에피소드

### 8월 15일 — 정책과 배포 준비

법무 검토 회신을 정책·구현과 대조했고(추가 232), 탈퇴 후 서버에 남던 명함
사진을 트리거로 지웠다(235). 재연락 루프 방향을 정하고 구현했다(227·234).
스토어 소개글·스크린샷 계획도 이 날이다(230·231).

📌 **세 세션 병행에서 번호가 충돌**하기 시작했다(233). 오늘 또 같은 일이 났다.

⚠️ 그리고 **정리 도구를 실기기에서 돌려 보니 문구가 남아 있었다**(226) —
*"검사가 통과한다"*와 *"실제로 지워진다"*가 다르다는 첫 신호였다.

### 8월 16일 — 자르기를 정하고 만들다

리멤버보다 자르기가 떨어진다는 지적(259)에서 시작해, A안(기성 스캐너)을
확정했다가(264·266) **B′(우리 화면 + OS 검출)로 방향을 바꿨다**(272).
그날 아이폰에 B′를 구현했고(273), 안드로이드는 **dartcv4로 갈아 끼우면서
x86_64를 뺐다**.

⚠️ **269가 이 날의 교훈이다.** 새 스캐너 정리 코드가 **없는 폴더를 보고
있었다** — 검사는 통과했고, 기기는 그대로였다.

### 8월 17일 (오늘) — 자를 고치는 데 하루를 썼다

아침에 *"정답지 103장"*으로 알고 시작했다. **86행이 인식 결과 복사본**이었다
(277). 그때부터 온종일 **자를 고치는 일**이었다.

```
94%  →  85%  →  86%  →  87%  →  88%  →  86%
```

숫자가 오르내린 것처럼 보이지만 **잴 때마다 자가 정확해진 것**이다. 오늘 마지막
86%가 **처음으로 통일된 자로 잰 값**이다.

#### 📌 오늘 네 번 만난 같은 모양

```
280  정답지가 인식 결과 복사본
278  로고 표기가 장마다 다름
282  팩스 칸이 비고, 사무실 칸에 팩스가 적힘
286  직함의 자세함이 장마다 다름
```

**넷 다 파서와 정답지가 같은 실수를 함께 해서 만점이 나오던 자리다.**
①②③은 **재고 나서** 알았고, ④는 **재기 전에** 멈췄다. 그게 이번의 소득이다.

#### ⚠️ 내가 틀린 것들 — 남겨 두는 이유

| | |
|---|---|
| **X 터치 불가를 두 번 헛짚었다** | 코드만 보고 짐작했다. 세 번째에 **기기에서 직접 눌러 보고서야** 원인이 나왔다 — 그리고 **원인은 내가 아침에 만든 것**이었다 |
| **재현이 곧 실측이 아니다 — 두 번** | 내가 만든 입력에서는 또렷하게 재현됐는데 실제 103장에서는 한 장도 안 바뀌었다. 앞 여섯 줄만 떼어 만든 입력에서도 같은 일이 났다 |
| **설치만 하고 앱을 안 껐다 켰다** | 문서에 *"`Success`는 코드가 돈다는 뜻이 아니다"*라고 **적혀 있는 그대로** 밟았다 |
| **남의 스태시를 꺼냈다** | `git stash`가 빈 트리에서 아무것도 안 담고, 뒤이은 `pop`이 다른 세션 것을 꺼냈다. 손실은 없었다 |
| **끝내는 시점을 내가 말했다** | *"오늘은 여기까지 하셔도 충분합니다"* — **그건 사용자가 정한다** |

📌 이걸 적는 이유는 **문서를 믿을 수 있게 하려는 것**이다. 자기가 만든 결함을
자기가 기록하지 않으면, 다음 사람은 기록 전체를 의심해야 한다.

#### 그리고 오늘 세 번 병합 사고가 있었다

```
아침   다른 세션이 내 기록 18개를 덮어씀        → 내가 세어서 찾아 복구
낮     내가 도구 변경을 되돌릴 뻔했다는 오해      → 실제로는 뒤처진 브랜치였다
저녁   번호 291이 겹침                        → 293으로 바꿈
```

⚠️ 셋 다 **같은 파일 맨 위에 두 세션이 쓰는 구조** 때문이다. 막는 방법은
하나뿐이었다.

```bash
grep -c '^### ' docs/planning/backlog.md    # 병합 전후로 센다
```

**줄어들면 무언가 사라진 것이다.**

---

## 4. 지금 상태

```
브랜치   feat/manual-crop-290  (추가 290·293)   병합 대기
analyze  19 (기준선)
test     692
기기     폴드·아이폰 둘 다 같은 빌드 설치됨

정답지   ~/card-ocr-data/ocr_truth.tsv
         103장 전수 검수 · 팩스 65장 · 규칙 통일 완료
기준선   전체 86% (808/937)
```

### 칸별 (2026-08-17 기준)

```
홈페이지 100% · 휴대폰 98% · 팩스 97% · 사무실 96%
이메일 94% · 우편번호 92% · 주소 92%
이름 85% · 상세주소 86%
⚠️ 회사 66% · 직함 58%
```

## 5. 관련 문서

- `field-accuracy-full-2026-08-17.md` — 103장 전수 측정(판정 규칙·한계 포함)
- `company-field-278-2026-08-17.md` — 회사 칸
- `fax-label-282-2026-08-17.md` — 팩스·사무실
- `title-field-286-2026-08-17.md` — 직함(왜 손댈 수 없었나)
- `~/card-ocr-data/README.txt` — 정답지 쓰는 법과 함정 (저장소 밖)
