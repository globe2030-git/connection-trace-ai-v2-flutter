# 커넥션센스 대표 사이트 — 모두 열어 둔다.
#
# 🚨 robots.txt 는 **접근을 막는 장치가 아니다.** 크롤러에게 하는 부탁이라
# 주소를 아는 사람에게는 그대로 열린다. 감추고 싶은 것이 있으면 여기 적을
# 것이 아니라 **애초에 배포하지 않아야 한다**(추가 650 — 법무 검토 메모가
# 링크가 없다는 이유로 공개된 채 있었다).
#
# 그래서 이 파일에 Disallow 를 쓰지 않는다. 이 사이트에 있는 것은 전부
# 사람이 봐 주기를 바라는 것뿐이다.

User-agent: *
Allow: /

Sitemap: https://connectionsense.co.kr/sitemap.xml
